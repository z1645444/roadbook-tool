---
phase: 04-amap-link-construction
plan: "02"
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - .planning/phases/04-amap-link-construction/04-AMAP-LINK-EXAMPLES.md
  - .planning/phases/04-amap-link-construction/04-AMAP-LINK-VALIDATION-CHECKLIST.md
autonomous: true
requirements:
  - AMAP-01
  - AMAP-02
  - AMAP-03
  - AMAP-04
must_haves:
  truths:
    - "示例集必须覆盖 L0 总段和 L1..Ln 分段链接的完整输出结构。"
    - "验证清单必须可脚本化检查 `https://uri.amap.com/...`、`callnative=1`、URL 编码和 fallback。"
    - "坐标优先与无坐标回退文本的规则必须在示例中并列呈现。"
    - "scheme 仅作为可选附加输出，且每条 scheme 都要配套 https 兜底。"
  artifacts:
    - path: ".planning/phases/04-amap-link-construction/04-AMAP-LINK-EXAMPLES.md"
      provides: "Phase 4 规范化输出示例（含多途经点与缺失场景）"
      contains: "L0/L1..Ln 示例、坐标优先示例、scheme+https 双链示例"
    - path: ".planning/phases/04-amap-link-construction/04-AMAP-LINK-VALIDATION-CHECKLIST.md"
      provides: "可执行校验清单（供执行/验证阶段复用）"
      contains: "逐条检查项、命令示例、通过/失败判定条件"
  key_links:
    - from: ".planning/phases/04-amap-link-construction/04-AMAP-LINK-EXAMPLES.md"
      to: ".planning/phases/04-amap-link-construction/04-LINK-BUILD-ALGORITHM.md"
      via: "示例按算法顺序生成 L0/L1..Ln"
      pattern: "L0|L1"
    - from: ".planning/phases/04-amap-link-construction/04-AMAP-LINK-VALIDATION-CHECKLIST.md"
      to: ".planning/phases/04-amap-link-construction/04-AMAP-URI-CONTRACT.md"
      via: "检查项对应参数契约"
      pattern: "callnative=1"
---

<objective>
产出可直接用于验收的 Phase 4 示例与校验清单，确保执行阶段不仅“会生成链接”，还可证明链接符合 AMAP-01..04。

Purpose: 把 Phase 4 规则转成可复核样例与检查步骤，降低执行阶段返工。
Output: `04-AMAP-LINK-EXAMPLES.md` 与 `04-AMAP-LINK-VALIDATION-CHECKLIST.md`。
</objective>

<execution_context>
@/Users/franklin/.codex/get-shit-done/workflows/execute-plan.md
@/Users/franklin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-amap-link-construction/04-CONTEXT.md
@.planning/phases/04-amap-link-construction/04-PATTERNS.md
@.planning/phases/04-amap-link-construction/04-01-PLAN.md
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 编写 Phase 4 链接示例集</name>
  <files>.planning/phases/04-amap-link-construction/04-AMAP-LINK-EXAMPLES.md</files>
  <read_first>
    - .planning/phases/04-amap-link-construction/04-AMAP-LINK-EXAMPLES.md
    - .planning/phases/04-amap-link-construction/04-LINK-BUILD-ALGORITHM.md
    - .planning/phases/04-amap-link-construction/04-AMAP-URI-CONTRACT.md
    - .planning/phases/04-amap-link-construction/04-CONTEXT.md
    - .planning/REQUIREMENTS.md
  </read_first>
  <action>
    Create `04-AMAP-LINK-EXAMPLES.md` with at least four canonical examples:
    (1) standard `https://uri.amap.com/navigation` output with URL-encoded params and `callnative=1`;
    (2) multi-stop confirmed order `A1 -> A3 -> A2 -> B3 -> B2 -> B1` showing `L0` plus `L1..L5`;
    (3) coordinate-present case where GCJ-02 coordinate values are used as nav anchor while text remains
    supplementary; (4) scheme requested case showing pair output: scheme link + mandatory https fallback.
    For each example include covered requirement IDs (`AMAP-01..04`), input assumptions, expected output,
    and a short validation hint line that maps to checklist keys.
  </action>
  <acceptance_criteria>
    - `rg -n "示例 1|示例 2|示例 3|示例 4|L0|L1|L2|L3|L4|L5" .planning/phases/04-amap-link-construction/04-AMAP-LINK-EXAMPLES.md` returns matches.
    - `rg -n "https://uri.amap.com/navigation|callnative=1|GCJ-02|scheme|https 兜底|URL 编码" .planning/phases/04-amap-link-construction/04-AMAP-LINK-EXAMPLES.md` returns matches.
    - `rg -n "AMAP-01|AMAP-02|AMAP-03|AMAP-04" .planning/phases/04-amap-link-construction/04-AMAP-LINK-EXAMPLES.md` returns matches.
  </acceptance_criteria>
  <verify>
    <automated>rg -n "示例 1|示例 2|示例 3|示例 4|L0|L1|L2|L3|L4|L5|https://uri.amap.com/navigation|callnative=1|GCJ-02|scheme|https 兜底|URL 编码|AMAP-01|AMAP-02|AMAP-03|AMAP-04" .planning/phases/04-amap-link-construction/04-AMAP-LINK-EXAMPLES.md</automated>
  </verify>
  <done>示例集可作为执行与验证阶段的统一参考，不再由执行者自行发明例子。</done>
</task>

<task type="auto">
  <name>Task 2: 编写可执行验证清单</name>
  <files>.planning/phases/04-amap-link-construction/04-AMAP-LINK-VALIDATION-CHECKLIST.md</files>
  <read_first>
    - .planning/phases/04-amap-link-construction/04-AMAP-LINK-VALIDATION-CHECKLIST.md
    - .planning/phases/04-amap-link-construction/04-AMAP-URI-CONTRACT.md
    - .planning/phases/04-amap-link-construction/04-AMAP-LINK-EXAMPLES.md
    - .planning/REQUIREMENTS.md
  </read_first>
  <action>
    Create `04-AMAP-LINK-VALIDATION-CHECKLIST.md` with a deterministic checklist table including columns:
    `check_id`, `rule`, `input`, `expected`, `verify_command`, `pass_condition`, `mapped_requirement`.
    Include at least checks: `CHK-04-01` link starts with `https://uri.amap.com/`; `CHK-04-02`
    includes `callnative=1`; `CHK-04-03` verifies URL-encoded reserved characters in parameters;
    `CHK-04-04` verifies coordinate-present example keeps GCJ-02 coordinate precedence;
    `CHK-04-05` verifies scheme example includes paired https fallback. Provide concrete `rg` commands
    that can be run against `04-AMAP-LINK-EXAMPLES.md` to determine pass/fail.
  </action>
  <acceptance_criteria>
    - `rg -n "CHK-04-01|CHK-04-02|CHK-04-03|CHK-04-04|CHK-04-05" .planning/phases/04-amap-link-construction/04-AMAP-LINK-VALIDATION-CHECKLIST.md` returns all check IDs.
    - `rg -n "https://uri.amap.com/|callnative=1|URL 编码|GCJ-02|https 兜底" .planning/phases/04-amap-link-construction/04-AMAP-LINK-VALIDATION-CHECKLIST.md` returns matches.
    - `rg -n "AMAP-01|AMAP-02|AMAP-03|AMAP-04" .planning/phases/04-amap-link-construction/04-AMAP-LINK-VALIDATION-CHECKLIST.md` returns matches.
    - `rg -n "verify_command|pass_condition|mapped_requirement" .planning/phases/04-amap-link-construction/04-AMAP-LINK-VALIDATION-CHECKLIST.md` returns matches.
  </acceptance_criteria>
  <verify>
    <automated>rg -n "CHK-04-01|CHK-04-02|CHK-04-03|CHK-04-04|CHK-04-05|https://uri.amap.com/|callnative=1|URL 编码|GCJ-02|https 兜底|AMAP-01|AMAP-02|AMAP-03|AMAP-04|verify_command|pass_condition|mapped_requirement" .planning/phases/04-amap-link-construction/04-AMAP-LINK-VALIDATION-CHECKLIST.md</automated>
  </verify>
  <done>验证清单可被执行脚本直接消费，明确链接输出是否满足 Phase 4 验收标准。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| contract_algorithm -> examples | 示例可能偏离契约导致执行阶段误实现 |
| examples -> validation_checklist | 校验项若不可执行将导致“看起来通过”但无法真实证明合规 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-06 | T | examples | mitigate | 示例全部绑定 `AMAP-01..04` 与契约来源，禁止未定义参数混入。 |
| T-04-07 | R | checklist evidence | mitigate | 每个检查项给出 `check_id` 与 `verify_command`，确保结果可复核可追责。 |
| T-04-08 | I | sample payload | mitigate | 示例仅保留导航所需地点字段，不包含敏感个人身份信息。 |
| T-04-09 | D | validation drift | mitigate | `pass_condition` 必须可由 grep 命令判断，避免主观“看起来正确”导致误判。 |
| T-04-10 | E | scheme-only output | mitigate | 检查项强制要求 scheme 场景同时含 https 兜底，避免单链路唤起失败。 |
</threat_model>

<verification>
1. 两个目标文件存在并通过 check ID 与关键规则命中校验。
2. 示例覆盖 `L0/L1..Ln`、GCJ-02、scheme+https fallback 三类关键场景。
3. 验证清单中的命令可直接对示例文档执行并得到明确通过条件。
</verification>

<success_criteria>
1. Phase 4 输出规则具备“示例 + 校验”双重可执行依据。
2. 执行与验证阶段可使用同一清单判断 AMAP-01..04 是否满足。
3. 兼容策略与降级策略可被重复验证，不依赖人工主观判断。
</success_criteria>

<output>
After completion, create `.planning/phases/04-amap-link-construction/04-02-SUMMARY.md`
</output>

