---
phase: 06-split-roadbook-tool-into-reusable-skill
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/skill/roadbook-skill.contract.ts
  - src/skill/roadbook-skill.service.ts
  - src/skill/roadbook-skill.types.ts
  - src/conversation/intake.controller.ts
  - src/app.module.ts
  - tests/skill/roadbook-skill.service.test.ts
  - tests/conversation/intake-skill-parity.test.ts
autonomous: true
requirements:
  - TBD
must_haves:
  truths:
    - "Skill entry can run planning flow without Nest controller context."
    - "Controller output remains backward compatible while delegating to skill service."
    - "Skill output preserves ready/clarification/fallback status semantics."
  artifacts:
    - path: "src/skill/roadbook-skill.contract.ts"
      provides: "Transport-agnostic input/output contract for reusable skill invocation."
      exports: ["RoadbookSkillInput", "RoadbookSkillOutput", "RoadbookSkillStatus"]
    - path: "src/skill/roadbook-skill.service.ts"
      provides: "Core skill execution entry that composes repository, orchestrator, and renderer."
      exports: ["RoadbookSkillService"]
    - path: "tests/conversation/intake-skill-parity.test.ts"
      provides: "Regression checks that controller behavior stays aligned after delegation."
      contains: "phase-06 parity"
  key_links:
    - from: "src/skill/roadbook-skill.service.ts"
      to: "src/routing/routing-orchestrator.service.ts"
      via: "Skill delegates route planning to existing orchestrator boundary."
      pattern: "planRouteForSession"
    - from: "src/conversation/intake.controller.ts"
      to: "src/skill/roadbook-skill.service.ts"
      via: "Controller delegates confirmation-ready flow to skill service."
      pattern: "RoadbookSkillService"
    - from: "tests/conversation/intake-skill-parity.test.ts"
      to: "src/conversation/intake.controller.ts"
      via: "Parity tests assert non-regression of API-facing response fields."
      pattern: "routing_ready|routing_fallback|need_clarification"
---

<objective>
Create the first executable split of roadbook planning into a reusable skill service and contract,
while preserving current intake API behavior.

Purpose: make core planning callable by external agents without app-specific runtime assumptions.
Output: skill contract + service, controller delegation, and parity regression coverage.
</objective>

<execution_context>
@/Users/franklin/.codex/get-shit-done/workflows/execute-plan.md
@/Users/franklin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-split-roadbook-tool-into-reusable-skill/06-CONTEXT.md
@src/conversation/intake.controller.ts
@src/routing/routing-orchestrator.service.ts
@src/constraints/constraint-draft.repository.ts
@src/roadbook/markdown-roadbook.renderer.ts
@src/app.module.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define skill-facing planning contract and add failing skill tests</name>
  <read_first>
    - src/conversation/intake.controller.ts
    - src/routing/routing-orchestrator.service.ts
    - src/constraints/constraint-draft.repository.ts
    - .planning/phases/06-split-roadbook-tool-into-reusable-skill/06-CONTEXT.md
  </read_first>
  <files>src/skill/roadbook-skill.contract.ts, src/skill/roadbook-skill.types.ts, tests/skill/roadbook-skill.service.test.ts</files>
  <action>Define `RoadbookSkillInput` to include session identifier and normalized draft payload,
define `RoadbookSkillOutput` with status union `ready|needs_clarification|fallback`, and include
canonical fields `routePlan`, `routeMetadata`, `fallbackMessage`, `roadbookMarkdown`.
Add failing unit tests for `RoadbookSkillService` covering three outcomes:
1) returns `ready` with markdown when orchestrator succeeds,
2) returns `needs_clarification` with prompt and slot,
3) returns `fallback` with user-safe fallback message.</action>
  <acceptance_criteria>
    - `src/skill/roadbook-skill.contract.ts` exports input/output contract types.
    - `tests/skill/roadbook-skill.service.test.ts` contains three status-path tests.
    - `pnpm vitest run tests/skill/roadbook-skill.service.test.ts` fails before Task 2 and passes after Task 2.
  </acceptance_criteria>
  <verify>
    <automated>pnpm vitest run tests/skill/roadbook-skill.service.test.ts</automated>
  </verify>
  <done>Skill contract is explicit and tests describe expected core behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement skill service and delegate intake controller to it</name>
  <read_first>
    - src/conversation/intake.controller.ts
    - src/app.module.ts
    - src/routing/routing-orchestrator.service.ts
    - src/roadbook/markdown-roadbook.renderer.ts
    - tests/skill/roadbook-skill.service.test.ts
  </read_first>
  <files>src/skill/roadbook-skill.service.ts, src/conversation/intake.controller.ts, src/app.module.ts, tests/skill/roadbook-skill.service.test.ts</files>
  <action>Implement `RoadbookSkillService` that performs: save canonical draft, call
`planRouteForSession`, map orchestrator result to skill output, and render markdown in `ready`
state using existing renderer. Update `AppModule` providers to register the skill service and
inject it into `IntakeController`. Refactor controller confirmation-ready branch to delegate to
skill service, while keeping endpoint route, DTO validation, and response fields unchanged.</action>
  <acceptance_criteria>
    - `src/skill/roadbook-skill.service.ts` exists and exports executable service.
    - `src/conversation/intake.controller.ts` delegates ready-path orchestration to skill service.
    - `pnpm vitest run tests/skill/roadbook-skill.service.test.ts` passes.
  </acceptance_criteria>
  <verify>
    <automated>pnpm vitest run tests/skill/roadbook-skill.service.test.ts</automated>
  </verify>
  <done>Skill service is executable and controller uses it without breaking response contract.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Add intake parity regression tests and verify non-regression</name>
  <read_first>
    - tests/conversation/intake-confirmation.test.ts
    - src/conversation/intake.controller.ts
    - src/skill/roadbook-skill.service.ts
  </read_first>
  <files>tests/conversation/intake-skill-parity.test.ts, tests/conversation/intake-confirmation.test.ts</files>
  <action>Add parity regression coverage that validates controller responses still include expected
fields and status transitions after delegation:
`routing_ready`, `need_clarification`, and `routing_fallback`.
Run existing intake confirmation tests plus new parity tests to prove no behavior drift in API
shape and status mapping.</action>
  <acceptance_criteria>
    - `tests/conversation/intake-skill-parity.test.ts` exists with three status-path assertions.
    - Existing `tests/conversation/intake-confirmation.test.ts` still passes.
    - `pnpm vitest run tests/conversation/intake-confirmation.test.ts tests/conversation/intake-skill-parity.test.ts` passes.
  </acceptance_criteria>
  <verify>
    <automated>pnpm vitest run tests/conversation/intake-confirmation.test.ts tests/conversation/intake-skill-parity.test.ts</automated>
  </verify>
  <done>Behavior parity is proven and skill split is safe for follow-up extraction work.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Controller DTO -> skill contract | User-derived request content crosses into reusable core entry. |
| Skill output -> external agent callers | Reusable entry may be consumed by non-HTTP runtimes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | T | `src/skill/roadbook-skill.service.ts` | mitigate | Validate and map outputs through typed contract; reject impossible status combinations. |
| T-06-02 | I | `src/conversation/intake.controller.ts` | mitigate | Keep fallback messages user-safe and avoid leaking provider internals. |
| T-06-03 | R | `tests/conversation/intake-skill-parity.test.ts` | mitigate | Add deterministic parity tests over status and response-shape paths. |
</threat_model>

<verification>
- `pnpm vitest run tests/skill/roadbook-skill.service.test.ts`
- `pnpm vitest run tests/conversation/intake-confirmation.test.ts tests/conversation/intake-skill-parity.test.ts`
</verification>

<success_criteria>
- Reusable skill contract exists and is transport-agnostic.
- Intake controller delegates planning execution to skill service with no API contract regression.
- Parity tests cover ready/clarification/fallback behavior after split.
</success_criteria>

<output>
After completion, create `.planning/phases/06-split-roadbook-tool-into-reusable-skill/06-01-SUMMARY.md`
</output>
