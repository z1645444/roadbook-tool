---
phase: 04-lodging-recommendation-policy
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/map-provider/map-provider.port.ts
  - src/map-provider/amap-error.mapper.ts
  - src/map-provider/amap-lodging.adapter.ts
  - src/shared/validation/multiday-route-artifact.schema.ts
  - tests/map-provider/amap-lodging.adapter.test.ts
  - tests/shared/validation/multiday-lodging-artifact.schema.test.ts
autonomous: true
requirements:
  - LODG-01
must_haves:
  truths:
    - "Multi-day day stages have a typed lodging payload contract for downstream policy and chat output."
    - "AMap lodging retrieval runs through the provider boundary with fixed query envelope and safe errors."
    - "Provider-side retrieval constraints are hard-limited (radius, page cap, timeout, no retry)."
  artifacts:
    - path: "src/map-provider/map-provider.port.ts"
      provides: "Lodging retrieval DTO contracts on the MapProvider boundary."
      exports: ["MapProvider"]
    - path: "src/map-provider/amap-lodging.adapter.ts"
      provides: "AMap lodging around-search adapter with deterministic query and parsing."
      exports: ["AmapLodgingAdapter"]
    - path: "src/shared/validation/multiday-route-artifact.schema.ts"
      provides: "Runtime schema boundary for day-level lodging payload."
      exports: ["multidayRouteArtifactSchema", "safeParseMultidayRouteArtifact"]
  key_links:
    - from: "src/map-provider/amap-lodging.adapter.ts"
      to: "src/map-provider/map-provider.port.ts"
      via: "adapter returns `LodgingSearchResult` for provider abstraction consumers."
      pattern: "searchLodgingAround"
    - from: "src/map-provider/amap-lodging.adapter.ts"
      to: "src/map-provider/amap-error.mapper.ts"
      via: "all transport and payload failures map through `mapAmapError`."
      pattern: "mapAmapError"
    - from: "src/shared/validation/multiday-route-artifact.schema.ts"
      to: "src/routing/day-stage-splitter.service.ts"
      via: "day-stage artifacts keep schema-validated shape before orchestration response."
      pattern: "safeParseMultidayRouteArtifact"
---

<objective>
Define Phase 4 foundation contracts so lodging retrieval and payload validation are deterministic
before policy and orchestration wiring.

Purpose: Lock interface and schema boundaries for LODG-01 with D-01..D-12 retrieval constraints.
Output: Provider lodging contracts, AMap lodging adapter, and schema-level lodging payload support.
</objective>

<execution_context>
@/Users/franklin/.codex/get-shit-done/workflows/execute-plan.md
@/Users/franklin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-lodging-recommendation-policy/04-CONTEXT.md
@.planning/phases/04-lodging-recommendation-policy/04-RESEARCH.md
@.planning/phases/04-lodging-recommendation-policy/04-PATTERNS.md
@.planning/phases/04-lodging-recommendation-policy/04-UI-SPEC.md
@.planning/phases/03-multi-day-optimization-and-stage-split/03-03-SUMMARY.md
@.planning/phases/02-routing-baseline-and-reliability/02-02-SUMMARY.md

<interfaces>
From src/map-provider/map-provider.port.ts:
```typescript
export interface MapProvider {
  geocodePoint(input: GeocodePointInput): Promise<GeocodePointResult>;
  routeBicyclingSegment(input: BicyclingSegmentInput): Promise<BicyclingSegmentResult>;
}
```

From src/shared/validation/multiday-route-artifact.schema.ts:
```typescript
export const multidayDayStageSchema = dayBoundarySchema.extend({
  totalDistanceMeters: z.number().nonnegative(),
  totalDurationSeconds: z.number().nonnegative(),
  segments: z.array(routeSegmentSchema)
});
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend provider and schema contracts for lodging pipeline</name>
  <read_first>
    - src/map-provider/map-provider.port.ts
    - src/shared/validation/multiday-route-artifact.schema.ts
    - src/shared/validation/route-artifact.schema.ts
    - .planning/phases/04-lodging-recommendation-policy/04-CONTEXT.md
  </read_first>
  <files>src/map-provider/map-provider.port.ts, src/shared/validation/multiday-route-artifact.schema.ts, tests/shared/validation/multiday-lodging-artifact.schema.test.ts</files>
  <behavior>
    - Lodging DTOs include minimum output fields required by D-24.
    - Day-level schema supports grouped category output per D-23 and D-25.
  </behavior>
  <action>Add concrete lodging contracts per D-09 and D-24:
`LodgingCategory = 'hostel' | 'guesthouse' | 'hotel'`,
`LodgingPolicyStage = 'strict' | 'radius_12km' | 'radius_20km' | 'price_relaxed_20'`,
`LodgingSearchInput` with `anchor{lng,lat,providerId}`, `radiusMeters`, `page`, `sessionId`,
`dayIndex`, `category`,
`LodgingCandidate` with `providerId,name,type,distanceMeters,rating,priceCny,policyStage`.
Extend `MapProvider` with `searchLodgingAround?(input: LodgingSearchInput): Promise<LodgingSearchResult>`
so existing non-lodging tests remain compile-stable while still extending provider capability.
In multiday schema, extend day stage with `lodging` object:
`policyStatus: compliant|relaxed|no_match`,
`fallbackTrace: string[]`,
`categories.hostel|guesthouse|hotel` arrays of lodging entries with `.max(3)`.</action>
  <acceptance_criteria>
    - `rg -n "LodgingCategory|LodgingSearchInput|searchLodgingAround" src/map-provider/map-provider.port.ts` returns matches.
    - `rg -n "policyStatus|fallbackTrace|categories|lodging" src/shared/validation/multiday-route-artifact.schema.ts` returns matches.
    - `rg -n "safeParseMultidayRouteArtifact|lodging" tests/shared/validation/multiday-lodging-artifact.schema.test.ts` returns matches.
    - `pnpm vitest run tests/shared/validation/multiday-lodging-artifact.schema.test.ts` passes.
  </acceptance_criteria>
  <verify>
    <automated>pnpm vitest run tests/shared/validation/multiday-lodging-artifact.schema.test.ts</automated>
  </verify>
  <done>Lodging contracts and schema boundary exist and reject malformed day-level lodging payloads.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement AMap lodging around-search adapter with strict envelope</name>
  <read_first>
    - src/map-provider/amap-geocode.adapter.ts
    - src/map-provider/amap-bicycling.adapter.ts
    - src/map-provider/amap-error.mapper.ts
    - src/map-provider/map-provider.port.ts
    - .planning/phases/04-lodging-recommendation-policy/04-CONTEXT.md
  </read_first>
  <files>src/map-provider/amap-lodging.adapter.ts, src/map-provider/amap-error.mapper.ts, tests/map-provider/amap-lodging.adapter.test.ts</files>
  <behavior>
    - Adapter only queries fixed type whitelists and uses distance sorting.
    - Adapter enforces timeout and page cap before network call.
  </behavior>
  <action>Create `AmapLodgingAdapter` implementing `Pick<MapProvider, 'searchLodgingAround'>` with
endpoint `https://restapi.amap.com/v3/place/around`.
Hard-code category type whitelist constants per D-10:
hostel=`100103|100104`, guesthouse=`100105`, hotel=`100301|100302|100303`.
Build query with exact keys per D-03/D-04/D-05:
`key,location,radius,types,sortrule=distance,page,offset=20`.
Clamp `page` to max `2` and radius stages to `8000|12000|20000`.
Apply `AbortController` timeout `3000` ms per D-11 and do not retry.
Update `mapAmapError` endpoint union to include `'lodging'`.
Parse AMap POI fields into typed candidates; non-numeric `rating` or `priceCny` become `null` and
remain filterable by downstream policy.
Add adapter tests for query envelope, timeout mapping, error mapping, and page-cap behavior.</action>
  <acceptance_criteria>
    - `rg -n "AMAP_LODGING_ENDPOINT|sortrule|types|AbortController|3000" src/map-provider/amap-lodging.adapter.ts` returns matches.
    - `rg -n "endpoint: 'geocode' \\| 'bicycling' \\| 'lodging'" src/map-provider/amap-error.mapper.ts` returns the expanded union.
    - `rg -n "sortrule=distance|page|timeout|mapAmapError" tests/map-provider/amap-lodging.adapter.test.ts` returns assertions.
    - `pnpm vitest run tests/map-provider/amap-lodging.adapter.test.ts` passes.
  </acceptance_criteria>
  <verify>
    <automated>pnpm vitest run tests/map-provider/amap-lodging.adapter.test.ts</automated>
  </verify>
  <done>Provider boundary supports deterministic AMap lodging retrieval with fixed envelope and safe failures.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| AMap API -> `amap-lodging.adapter` | Untrusted external payload enters internal routing domain. |
| Adapter output -> schema validation | Untrusted parsed values become typed day-stage artifacts. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | T | `src/map-provider/amap-lodging.adapter.ts` | mitigate | Enforce fixed query params (`types`, `sortrule=distance`, page/radius clamps) and reject malformed payload fields. |
| T-04-02 | I | `src/map-provider/amap-error.mapper.ts` | mitigate | Map all adapter errors to `RoutingFallbackError` and avoid leaking API key/raw endpoint response details. |
| T-04-03 | D | `src/map-provider/amap-lodging.adapter.ts` | mitigate | Apply `AbortController` 3s timeout with no retry to bound provider-induced request blocking. |
| T-04-04 | R | `tests/map-provider/amap-lodging.adapter.test.ts` | mitigate | Requirement-tagged adapter tests assert deterministic envelope and fallback mappings. |
</threat_model>

<verification>
- `pnpm vitest run tests/map-provider/amap-lodging.adapter.test.ts`
- `pnpm vitest run tests/shared/validation/multiday-lodging-artifact.schema.test.ts`
</verification>

<success_criteria>
- Provider abstraction exposes typed lodging retrieval capability.
- AMap lodging adapter enforces D-03/D-04/D-05/D-10/D-11 query constraints.
- Multiday schema accepts valid lodging payload and rejects malformed lodging records.
</success_criteria>

<output>
After completion, create `.planning/phases/04-lodging-recommendation-policy/04-01-SUMMARY.md`
</output>
