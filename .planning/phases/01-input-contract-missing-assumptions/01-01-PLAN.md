---
phase: 01-input-contract-missing-assumptions
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/01-input-contract-missing-assumptions/01-INPUT-CONTRACT.md
  - .planning/phases/01-input-contract-missing-assumptions/01-ASSUMPTIONS-TEMPLATE.md
autonomous: true
requirements:
  - INT-01
  - INT-02
  - INT-03
must_haves:
  truths:
    - "用户输入自然语言或结构化字段后，可得到统一输入契约定义。"
    - "缺失与冲突规则可被明确复述，不会由执行者临时猜测。"
    - "A 段假设输出可直接复用且逐条编号。"
  artifacts:
    - path: ".planning/phases/01-input-contract-missing-assumptions/01-INPUT-CONTRACT.md"
      provides: "Phase 1 输入契约、优先级、缺失/冲突处理规则"
      contains: "D-01 到 D-15 的落地规则"
    - path: ".planning/phases/01-input-contract-missing-assumptions/01-ASSUMPTIONS-TEMPLATE.md"
      provides: "A 段假设与缺失声明模板"
      contains: "编号假设、降级说明、可机读补充位"
  key_links:
    - from: ".planning/phases/01-input-contract-missing-assumptions/01-INPUT-CONTRACT.md"
      to: ".planning/phases/01-input-contract-missing-assumptions/01-ASSUMPTIONS-TEMPLATE.md"
      via: "A 段字段与编号规则"
      pattern: "A[0-9]+"
    - from: ".planning/phases/01-input-contract-missing-assumptions/01-INPUT-CONTRACT.md"
      to: ".planning/phases/01-input-contract-missing-assumptions/01-NORMALIZED-OUTPUT.schema.json"
      via: "固定字段顺序与候选结构约定"
      pattern: "candidates\\[\\]"
---

<objective>
固化 Phase 1 的输入契约与假设模板，消除“缺失字段时如何继续生成”的执行歧义。

Purpose: 让后续 POI/路线/链接阶段消费一致的前置结构，不再重复讨论输入语义。
Output: `01-INPUT-CONTRACT.md` 与 `01-ASSUMPTIONS-TEMPLATE.md` 两个规范文档。
</objective>

<execution_context>
@/Users/franklin/.codex/get-shit-done/workflows/execute-plan.md
@/Users/franklin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-input-contract-missing-assumptions/01-CONTEXT.md
@.planning/phases/01-input-contract-missing-assumptions/01-PATTERNS.md
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 编写输入契约主文档并锁定决策映射</name>
  <files>.planning/phases/01-input-contract-missing-assumptions/01-INPUT-CONTRACT.md</files>
  <read_first>
    - .planning/phases/01-input-contract-missing-assumptions/01-INPUT-CONTRACT.md
    - .planning/phases/01-input-contract-missing-assumptions/01-CONTEXT.md
    - .planning/phases/01-input-contract-missing-assumptions/01-PATTERNS.md
    - .planning/REQUIREMENTS.md
    - AGENTS.md
  </read_first>
  <action>
    Create `01-INPUT-CONTRACT.md` with fixed sections and concrete defaults: (1) dual-channel input
    (`natural_language` + `structured_input`) per D-01; (2) conflict precedence
    `structured_input > natural_language` per D-02; (3) location representation as
    `name/address/city` text-first with optional `coordinates` per D-03; (4) missing/default rules:
    missing origin/destination must be explicitly declared (D-04), default transport mode = `cycling`
    (D-05), missing time budget = `unbounded` hard constraint with explicit note (D-06), QR unavailable
    must degrade to address-only statement (D-07); (5) disambiguation and multi-city rules per D-08,
    D-09, D-10, D-11; (6) normalized output contract with fixed key order and explicit `candidates[]`
    requirement per D-12 and D-14; (7) output view strategy `human-first + machine-readable appendix`
    per D-15. Add a direct decision trace table `D-01..D-15 -> section`. Follow
    `.planning/phases/01-input-contract-missing-assumptions/01-PATTERNS.md` mapping for
    `.planning/phases/01-input-contract-missing-assumptions/01-CONTEXT.md`:
    copy the "Decision block pattern" and "Canonical reference pattern" so `D-xx` grouping
    and downstream references match established phase artifacts.
  </action>
  <acceptance_criteria>
    - `rg -n "D-01|D-02|D-03|D-04|D-05|D-06|D-07|D-08|D-09|D-10|D-11|D-12|D-13|D-14|D-15" .planning/phases/01-input-contract-missing-assumptions/01-INPUT-CONTRACT.md` returns all decision IDs.
    - `rg -n "structured_input > natural_language|default transport mode = cycling|time budget = unbounded|candidates\\[\\]|human-first \\+ machine-readable" .planning/phases/01-input-contract-missing-assumptions/01-INPUT-CONTRACT.md` returns matches.
    - `rg -n "INT-01|INT-02|INT-03" .planning/phases/01-input-contract-missing-assumptions/01-INPUT-CONTRACT.md` returns matches.
  </acceptance_criteria>
  <verify>
    <automated>rg -n "D-0[1-9]|D-1[0-5]|structured_input > natural_language|default transport mode = cycling|time budget = unbounded|candidates\\[\\]|human-first \\+ machine-readable|INT-01|INT-02|INT-03" .planning/phases/01-input-contract-missing-assumptions/01-INPUT-CONTRACT.md</automated>
  </verify>
  <done>输入契约文档具备完整决策映射与可执行规则，后续阶段无需再解释优先级或默认值。</done>
</task>

<task type="auto">
  <name>Task 2: 产出 A 段假设模板并固定编号规范</name>
  <files>.planning/phases/01-input-contract-missing-assumptions/01-ASSUMPTIONS-TEMPLATE.md</files>
  <read_first>
    - .planning/phases/01-input-contract-missing-assumptions/01-ASSUMPTIONS-TEMPLATE.md
    - .planning/phases/01-input-contract-missing-assumptions/01-INPUT-CONTRACT.md
    - .planning/phases/01-input-contract-missing-assumptions/01-CONTEXT.md
    - .planning/REQUIREMENTS.md
  </read_first>
  <action>
    Create `01-ASSUMPTIONS-TEMPLATE.md` as a reusable A-section template with numbered assumptions
    `A1, A2, ...` per D-13. Include mandatory lines for: missing origin/destination declaration (D-04),
    default mode fallback to `cycling` (D-05), no time-limit note when budget missing (D-06), QR
    unavailable degrade-to-address-only statement (D-07), location ambiguity candidate disclosure with
    same-city-first rule (D-09), and coordinate-over-text conflict notice (D-11). The template must end
    with a machine-readable supplement block placeholder to align with D-15. Follow
    `.planning/phases/01-input-contract-missing-assumptions/01-PATTERNS.md` mapping for
    `.planning/REQUIREMENTS.md`: copy the "Requirement-ID pattern" and
    "Fallback/degrade wording pattern" so checklist statements and degradation wording stay
    consistent across reusable skill docs.
  </action>
  <acceptance_criteria>
    - `rg -n "^A1\\.|^A2\\.|^A3\\." .planning/phases/01-input-contract-missing-assumptions/01-ASSUMPTIONS-TEMPLATE.md` returns at least 3 numbered lines.
    - `rg -n "起点|终点|cycling|未限制时长|仅地址输出|同城优先|坐标为准|machine-readable" .planning/phases/01-input-contract-missing-assumptions/01-ASSUMPTIONS-TEMPLATE.md` returns matches.
    - `rg -n "INT-02|INT-03" .planning/phases/01-input-contract-missing-assumptions/01-ASSUMPTIONS-TEMPLATE.md` returns matches.
  </acceptance_criteria>
  <verify>
    <automated>rg -n "^A[0-9]+\\.|起点|终点|cycling|未限制时长|仅地址输出|同城优先|坐标为准|machine-readable|INT-02|INT-03" .planning/phases/01-input-contract-missing-assumptions/01-ASSUMPTIONS-TEMPLATE.md</automated>
  </verify>
  <done>A 段模板可直接复用，且覆盖缺失、冲突、降级和编号规范的强制信息。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user_input -> input_contract | 不受信任的自然语言/结构化输入进入解析规则 |
| input_contract -> downstream_phase | 下游阶段按契约执行，若契约含糊将放大错误 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | T | 01-INPUT-CONTRACT.md | mitigate | 在契约中固定优先级与冲突处理（D-02、D-11），避免被提示词篡改解释路径。 |
| T-01-02 | R | 01-ASSUMPTIONS-TEMPLATE.md | mitigate | 强制 `A1..An` 编号并要求每条可追溯到缺失/冲突来源（D-13）。 |
| T-01-03 | I | assumptions output | mitigate | 模板仅输出路线生成所需信息，不包含敏感凭据字段；禁止 secrets 字段进入模板。 |
| T-01-04 | D | missing-field handling | mitigate | 明确“缺失不阻断”与默认值策略（D-04~D-07），防止输入不完整导致流程中断。 |
| T-01-05 | E | downstream consumers | accept | 本阶段仅文档与契约，不执行外部调用；权限提升面低，风险接受并在后续执行阶段复核。 |
</threat_model>

<verification>
1. `01-INPUT-CONTRACT.md` 与 `01-ASSUMPTIONS-TEMPLATE.md` 文件存在且通过 `rg` 规则命中。
2. D-01 到 D-15 在本计划产物中都有明确落实语句。
3. INT-01/02/03 在文档中被显式追踪。
</verification>

<success_criteria>
1. 执行者能不依赖口头解释，直接按文档实现输入归一化。
2. A 段假设输出格式可跨 agent 复用且编号一致。
3. 缺失与冲突处理在文档层面无歧义。
</success_criteria>

<output>
After completion, create `.planning/phases/01-input-contract-missing-assumptions/01-01-SUMMARY.md`
</output>
