---
phase: 05-markdown-roadbook-delivery
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/roadbook/markdown-roadbook.renderer.ts
  - src/roadbook/roadbook-render.types.ts
  - tests/roadbook/markdown-roadbook.renderer.test.ts
autonomous: true
requirements:
  - BOOK-01
  - BOOK-02
must_haves:
  truths:
    - "Roadbook output is Markdown and grouped by day sections."
    - "Each day section includes route overview, distance, estimated ride time, and waypoint sequence."
  artifacts:
    - path: "src/roadbook/roadbook-render.types.ts"
      provides: "Typed render contract for deterministic markdown generation."
      exports: ["RoadbookRenderInput", "RoadbookRenderDay", "RoadbookRenderOptions"]
    - path: "src/roadbook/markdown-roadbook.renderer.ts"
      provides: "Pure renderer that transforms canonical route data into day-grouped markdown."
      exports: ["renderMarkdownRoadbook"]
    - path: "tests/roadbook/markdown-roadbook.renderer.test.ts"
      provides: "Requirement-labeled renderer regression coverage for BOOK-01 and BOOK-02."
      contains: "BOOK-01, BOOK-02"
  key_links:
    - from: "src/roadbook/markdown-roadbook.renderer.ts"
      to: "src/routing/routing-orchestrator.service.ts"
      via: "Route day structure mirrors orchestrator day payload fields."
      pattern: "dayIndex|segments|totalDistanceMeters|totalDurationSeconds"
    - from: "tests/roadbook/markdown-roadbook.renderer.test.ts"
      to: "src/roadbook/markdown-roadbook.renderer.ts"
      via: "Unit tests assert heading, summary, and waypoint-chain rendering."
      pattern: "BOOK-01|BOOK-02"
---

<objective>
Create the deterministic markdown rendering foundation for Phase 5 so canonical day route artifacts
can be projected into a user-readable roadbook.

Purpose: Satisfy BOOK-01 and BOOK-02 without changing route computation behavior.
Output: Typed renderer contracts, renderer implementation, and requirement-labeled unit tests.
</objective>

<execution_context>
@/Users/franklin/.codex/get-shit-done/workflows/execute-plan.md
@/Users/franklin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-markdown-roadbook-delivery/05-RESEARCH.md
@.planning/phases/05-markdown-roadbook-delivery/05-VALIDATION.md
@src/routing/routing-orchestrator.service.ts
@src/conversation/intake.controller.ts
@tests/conversation/intake-confirmation.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define renderer contracts and add failing BOOK-01/BOOK-02 tests</name>
  <read_first>
    - src/routing/routing-orchestrator.service.ts
    - src/conversation/intake.controller.ts
    - tests/conversation/intake-confirmation.test.ts
    - .planning/phases/05-markdown-roadbook-delivery/05-RESEARCH.md
    - .planning/phases/05-markdown-roadbook-delivery/05-VALIDATION.md
  </read_first>
  <files>src/roadbook/roadbook-render.types.ts, tests/roadbook/markdown-roadbook.renderer.test.ts</files>
  <action>Create `RoadbookRenderInput` contracts that capture recap summary/assumptions, route days,
route metadata, and rendering options. Add requirement-labeled failing tests for BOOK-01 and
BOOK-02 that assert markdown day grouping (`## Day 1`, `## Day 2`), per-day overview lines,
distance/time formatting, and deterministic waypoint sequence derived from `startPoint + segments.to`.
Keep tests fixture-based and deterministic; do not rely on snapshots.</action>
  <acceptance_criteria>
    - `tests/roadbook/markdown-roadbook.renderer.test.ts` exists with BOOK-01 and BOOK-02 test names.
    - `src/roadbook/roadbook-render.types.ts` exports typed input/output contracts for renderer use.
    - `pnpm vitest run tests/roadbook/markdown-roadbook.renderer.test.ts -t "BOOK-01|BOOK-02"` fails before Task 2 and passes after Task 2.
  </acceptance_criteria>
  <verify>
    <automated>pnpm vitest run tests/roadbook/markdown-roadbook.renderer.test.ts -t "BOOK-01|BOOK-02"</automated>
  </verify>
  <done>Renderer contracts and failing BOOK-01/BOOK-02 tests are committed and reproducible.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement deterministic markdown renderer for day-grouped route output</name>
  <read_first>
    - src/roadbook/roadbook-render.types.ts
    - tests/roadbook/markdown-roadbook.renderer.test.ts
    - src/routing/routing-orchestrator.service.ts
    - src/shared/validation/route-artifact.schema.ts
  </read_first>
  <files>src/roadbook/markdown-roadbook.renderer.ts, tests/roadbook/markdown-roadbook.renderer.test.ts</files>
  <action>Implement `renderMarkdownRoadbook(input)` as a pure function that renders:
title + recap block, day sections in ascending `dayIndex`, route overview line, distance and
duration lines, waypoint sequence line, and segment bullet list. Include markdown escaping helper
for point names and recap text. Keep ordering stable and formatting deterministic to avoid flaky
output. Do not recalculate routing or lodging policy inside renderer.</action>
  <acceptance_criteria>
    - `src/roadbook/markdown-roadbook.renderer.ts` exports `renderMarkdownRoadbook`.
    - Unit tests for BOOK-01/BOOK-02 pass and assert content derives from supplied route days.
    - Rendering result is deterministic for identical input across repeated calls.
  </acceptance_criteria>
  <verify>
    <automated>pnpm vitest run tests/roadbook/markdown-roadbook.renderer.test.ts -t "BOOK-01|BOOK-02"</automated>
  </verify>
  <done>Deterministic day-grouped renderer implementation passes BOOK-01/BOOK-02 assertions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Canonical route payload -> markdown renderer | Route/point strings become formatted markdown content. |
| Recap summary/assumptions -> markdown sections | User-derived text can introduce markdown-control characters. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | T | `src/roadbook/markdown-roadbook.renderer.ts` | mitigate | Escape markdown-sensitive characters before writing point names and recap text. |
| T-05-02 | R | `tests/roadbook/markdown-roadbook.renderer.test.ts` | mitigate | Requirement-labeled deterministic tests prove output sections are stable and complete for BOOK-01/BOOK-02. |
| T-05-03 | D | `src/roadbook/markdown-roadbook.renderer.ts` | mitigate | Keep renderer pure and linear over provided day payload; avoid network or provider calls in render path. |
</threat_model>

<verification>
- `pnpm vitest run tests/roadbook/markdown-roadbook.renderer.test.ts -t "BOOK-01|BOOK-02"`
</verification>

<success_criteria>
- Renderer emits day-grouped markdown sections for all provided route days.
- Each day section includes route overview, distance, estimated ride duration, and waypoint sequence.
- Output is deterministic and safe against markdown-control text corruption.
</success_criteria>

<output>
After completion, create `.planning/phases/05-markdown-roadbook-delivery/05-01-SUMMARY.md`
</output>
