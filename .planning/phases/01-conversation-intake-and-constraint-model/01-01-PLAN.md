---
phase: 01-conversation-intake-and-constraint-model
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - vitest.config.ts
  - src/main.ts
  - src/constraints/constraint-draft.model.ts
  - src/shared/validation/constraint-draft.schema.ts
  - src/shared/time/time-window.parser.ts
  - src/constraints/constraint-patch.service.ts
  - tests/constraints/date-range.test.ts
  - tests/constraints/ride-window.test.ts
autonomous: true
requirements:
  - CONV-03
  - CONV-04
must_haves:
  truths:
    - "User-provided date range and trip days are captured in canonical form."
    - "User-provided daily ride window is accepted only when syntactically and semantically valid."
    - "Field-level revisions preserve unrelated accepted constraint values."
  artifacts:
    - path: "src/constraints/constraint-draft.model.ts"
      provides: "Canonical draft aggregate with raw + normalized slot values."
      contains: "ConstraintDraft"
    - path: "src/shared/time/time-window.parser.ts"
      provides: "24-hour local-time parser with strict validation."
      exports: ["parseRideWindow"]
    - path: "src/constraints/constraint-patch.service.ts"
      provides: "Field-level patch semantics with revision metadata recording."
      exports: ["applyConstraintPatch"]
  key_links:
    - from: "src/shared/time/time-window.parser.ts"
      to: "src/shared/validation/constraint-draft.schema.ts"
      via: "parser output is validated into canonical ride-window schema shape."
      pattern: "parseRideWindow"
    - from: "src/constraints/constraint-patch.service.ts"
      to: "src/constraints/constraint-draft.model.ts"
      via: "merge patch into slots without clearing unrelated values."
      pattern: "revisionLog"
---

<objective>
Create the Phase 1 technical foundation for canonical constraint capture and patch safety.

Purpose: Establish deterministic, testable contracts for dates and ride windows before
conversation orchestration is built.
Output: Runtime/test bootstrap + canonical constraint model + validation/time parsing + patch
service.
</objective>

<execution_context>
@/Users/franklin/.codex/get-shit-done/workflows/execute-plan.md
@/Users/franklin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-conversation-intake-and-constraint-model/01-CONTEXT.md
@.planning/phases/01-conversation-intake-and-constraint-model/01-RESEARCH.md
@.planning/phases/01-conversation-intake-and-constraint-model/01-VALIDATION.md
@.planning/phases/01-conversation-intake-and-constraint-model/01-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bootstrap Nest/Vitest runtime and strict request validation boundary</name>
  <read_first>
    - package.json
    - .planning/phases/01-conversation-intake-and-constraint-model/01-PATTERNS.md
    - .planning/phases/01-conversation-intake-and-constraint-model/01-RESEARCH.md
  </read_first>
  <files>package.json, vitest.config.ts, src/main.ts</files>
  <action>Initialize a TypeScript NestJS baseline using Fastify-compatible bootstrap settings, add
  scripts `test`, `test:conversation`, and `test:phase1`, and configure `src/main.ts` to register
  `ValidationPipe` with `whitelist: true`, `forbidNonWhitelisted: true`, and `transform: true`
  exactly (per D-01 input contract strictness and D-08 confidence-gate safety boundary).</action>
  <acceptance_criteria>
    - `rg -n "ValidationPipe" src/main.ts` returns one bootstrap usage.
    - `rg -n "whitelist:\\s*true|forbidNonWhitelisted:\\s*true|transform:\\s*true" src/main.ts`
      returns all three options.
    - `rg -n "\"test:phase1\"|\"test:conversation\"|\"test\"" package.json` returns all scripts.
  </acceptance_criteria>
  <verify>
    <automated>pnpm vitest --version</automated>
  </verify>
  <done>Project has executable test tooling and a strict validation boundary for all intake DTO
  inputs.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Define canonical draft schema and date/time normalization contracts</name>
  <read_first>
    - src/main.ts
    - .planning/phases/01-conversation-intake-and-constraint-model/01-CONTEXT.md
    - .planning/phases/01-conversation-intake-and-constraint-model/01-VALIDATION.md
  </read_first>
  <files>src/constraints/constraint-draft.model.ts, src/shared/validation/constraint-draft.schema.ts, src/shared/time/time-window.parser.ts, tests/constraints/date-range.test.ts, tests/constraints/ride-window.test.ts</files>
  <behavior>
    - Test 1: Invalid date ranges are rejected and valid ranges are normalized to local ISO dates.
    - Test 2: Ride windows must be strict `HH:mm-HH:mm` and `start` must be earlier than `end`.
    - Test 3: Canonical schema retains both `raw` and `normalized` values for constrained slots.
  </behavior>
  <action>Create `ConstraintDraft` and related slot types as the sole machine-readable source of
  truth (per D-04), implement schema parsing that requires paired `raw` + `normalized` fields where
  acceptance is claimed (per D-05), and normalize date/time values into 24-hour local formats at
  write-time (per D-06). Reject semantically invalid windows (`17:00-08:00`) and malformed ranges
  before draft write.</action>
  <acceptance_criteria>
    - `rg -n "raw:|normalized:|revisionLog" src/constraints/constraint-draft.model.ts` confirms
      source-of-truth shape.
    - `rg -n "safeParse|z\\.object|ConstraintDraft" src/shared/validation/constraint-draft.schema.ts`
      confirms deterministic schema parsing.
    - `pnpm vitest run tests/constraints/date-range.test.ts tests/constraints/ride-window.test.ts`
      passes.
  </acceptance_criteria>
  <verify>
    <automated>pnpm vitest run tests/constraints/date-range.test.ts tests/constraints/ride-window.test.ts</automated>
  </verify>
  <done>Date range and ride-window constraints are represented canonically and validated before
  acceptance.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement field-level patch semantics with auditable revision metadata</name>
  <read_first>
    - src/constraints/constraint-draft.model.ts
    - src/shared/validation/constraint-draft.schema.ts
    - .planning/phases/01-conversation-intake-and-constraint-model/01-CONTEXT.md
  </read_first>
  <files>src/constraints/constraint-patch.service.ts</files>
  <behavior>
    - Test 1: Patching one slot leaves unrelated accepted slots unchanged.
    - Test 2: Every accepted patch appends revision metadata with field, source turn, and timestamp.
  </behavior>
  <action>Implement patch merge logic that applies `Partial<ConstraintDraft.slots>` updates without
  object replacement (per D-10). Record revision metadata `{ field, turnId, timestampIso }` for
  each changed field (per D-12).</action>
  <acceptance_criteria>
    - `rg -n "Partial<|revisionLog|timestampIso|turnId" src/constraints/constraint-patch.service.ts`
      confirms patch+audit semantics.
    - `pnpm vitest run tests/constraints/date-range.test.ts tests/constraints/ride-window.test.ts`
      passes.
  </acceptance_criteria>
  <verify>
    <automated>pnpm vitest run tests/constraints/date-range.test.ts tests/constraints/ride-window.test.ts</automated>
  </verify>
  <done>Patch behavior is deterministic, auditable, and safe for downstream planning while leaving
  slot-specific mapping delivery to the intake orchestration plan.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| chat input -> intake DTO | Untrusted user text enters structured validation path. |
| DTO/schema layer -> canonical model | Parsed input becomes persisted constraint state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | T | `src/main.ts` validation boundary | mitigate | Enforce `whitelist`, `forbidNonWhitelisted`, `transform` to block overposting and payload tampering. |
| T-01-02 | D | `src/shared/time/time-window.parser.ts` | mitigate | Reject invalid or inverted time windows before write, returning explicit validation errors. |
| T-01-03 | R | `src/constraints/constraint-patch.service.ts` | mitigate | Append revision audit entries `{field, turnId, timestampIso}` for each accepted patch. |
| T-01-04 | E | `src/constraints/constraint-draft.model.ts` | mitigate | Keep slot updates scoped to validated patch keys only; never allow arbitrary object injection into draft state. |
</threat_model>

<verification>
- `pnpm vitest run tests/constraints/date-range.test.ts`
- `pnpm vitest run tests/constraints/ride-window.test.ts`
</verification>

<success_criteria>
- Canonical model files exist and compile with strict validation at input boundaries.
- Date range and ride-window behaviors are covered by passing automated tests.
- Patch semantics preserve unrelated fields and record revision metadata for auditability.
</success_criteria>

<output>
After completion, create `.planning/phases/01-conversation-intake-and-constraint-model/01-01-SUMMARY.md`
</output>
