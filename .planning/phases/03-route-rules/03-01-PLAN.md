---
phase: 03-route-rules
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/03-route-rules/03-ROUTE-RULES.md
  - .planning/phases/03-route-rules/03-ROUTE-NOTE-TEMPLATE.md
autonomous: true
requirements:
  - ROUTE-01
  - ROUTE-02
  - ROUTE-03
must_haves:
  truths:
    - "用户能看到完整路线顺序：起点 -> 途经点 -> 终点。"
    - "每一段路线都明确交通方式与简要耗时。"
    - "用户可用自然语言优先描述分段交通方式，并被规则显式解析与追踪。"
    - "起点或终点缺失时，A 段与正文都显式声明兜底假设来源。"
  artifacts:
    - path: ".planning/phases/03-route-rules/03-ROUTE-RULES.md"
      provides: "路线顺序、分段方式、缺失兜底、回路处理与预算优先规则"
      contains: "D-01 到 D-13（含 D-05 自然语言优先）与 ROUTE-01/02/03、A-G 结构映射"
    - path: ".planning/phases/03-route-rules/03-ROUTE-NOTE-TEMPLATE.md"
      provides: "路线说明文案模板与风险提示模板"
      contains: "A-G 各段模板、A 段假设声明、正文提示、machine-readable 附录"
  key_links:
    - from: ".planning/phases/03-route-rules/03-ROUTE-RULES.md"
      to: ".planning/phases/03-route-rules/03-ROUTE-NOTE-TEMPLATE.md"
      via: "规则 ID 驱动说明模板 ID"
      pattern: "D-0[1-9]|D-1[0-3]"
    - from: ".planning/phases/03-route-rules/03-ROUTE-RULES.md"
      to: ".planning/phases/02-poi/02-POI-OUTPUT.schema.json"
      via: "消费候选顺序生成路线链路"
      pattern: "rank|same_city_first"
---

<objective>
固化 Phase 3 的路线编排规则与说明模板，确保在无真实请求条件下仍可生成可执行路线描述。

Purpose: 让执行器按固定决策 D-01..D-13 输出可追溯的路线规则，不再临场猜测。
Output: `03-ROUTE-RULES.md` 与 `03-ROUTE-NOTE-TEMPLATE.md`。
</objective>

<execution_context>
@/Users/franklin/.codex/get-shit-done/workflows/execute-plan.md
@/Users/franklin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-route-rules/03-CONTEXT.md
@.planning/phases/03-route-rules/03-PATTERNS.md
@.planning/phases/02-poi/02-POI-OUTPUT.schema.json
@.planning/phases/01-input-contract-missing-assumptions/01-ASSUMPTIONS-TEMPLATE.md
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 编写路线规则主文档并锁定 D-01..D-13</name>
  <files>.planning/phases/03-route-rules/03-ROUTE-RULES.md</files>
  <read_first>
    - .planning/phases/03-route-rules/03-CONTEXT.md
    - .planning/phases/03-route-rules/03-PATTERNS.md
    - .planning/phases/02-poi/02-POI-STRATEGY.md
    - .planning/phases/02-poi/02-POI-OUTPUT.schema.json
    - .planning/phases/01-input-contract-missing-assumptions/01-ASSUMPTIONS-TEMPLATE.md
    - .planning/REQUIREMENTS.md
    - AGENTS.md
  </read_first>
  <action>
    创建 `03-ROUTE-RULES.md`，并按 `.planning/phases/02-poi/02-POI-STRATEGY.md` 的
    “5 组规则 + 执行要点 + 决策追踪表”结构编排，避免章节漂移。分 5 个固定章节落地锁定决策：
    1) 路线顺序与分段生成（按 起点 -> 候选途经点 -> 终点，引用候选顺序；覆盖 D-10、D-11、D-12）；
    2) 分段交通方式输入与分配（分段交通方式输入采用自然语言优先，先解析自然语言到段级 mode，再执行
    用户指定优先、部分未指定补 `cycling`、冲突不改写仅加风险提示；覆盖 D-01、D-02、D-03、D-04、D-05）；
    3) 起终点缺失兜底（起点缺失=城市中心，终点缺失=最后候选，双缺失同时补位，并要求 A 段与正文双重声明；
    覆盖 D-06、D-07、D-08、D-09）；4) 回路与重复点处理（仅合并连续重复、非连续重复保留并说明；
    覆盖 D-10、D-11、D-12）；5) 时间预算冲突处理（回路意图与预算冲突时预算优先，允许弱化回路并追加说明；
    覆盖 D-13）。文档末尾添加 Decision Traceability 表，逐条映射 D-01..D-13 -> ROUTE-01/02/03 -> 章节；
    另加 `A-G 输出结构映射` 小节，固定 A..G 各段在 Phase 3 的责任：A=假设声明，B=路线摘要，
    C=分段明细，D=风险提示，E=预算/回路调整说明，F=边界声明，G=machine-readable 附录。
    明确写入边界：本阶段禁止真实 API 请求、禁止 URI 构造、禁止二维码生成。
  </action>
  <acceptance_criteria>
    - `rg -n "D-01|D-02|D-03|D-04|D-05|D-06|D-07|D-08|D-09|D-10|D-11|D-12|D-13" .planning/phases/03-route-rules/03-ROUTE-RULES.md` 命中全部决策 ID（含 D-05）。
    - `rg -n "自然语言优先|按路段|解析自然语言|用户指定优先|默认骑行|城市中心|最后一个候选 POI|A 段|正文|连续重复|非连续重复|可能绕路|时间预算优先" .planning/phases/03-route-rules/03-ROUTE-RULES.md` 命中规则关键语句。
    - `rg -n "02-POI-STRATEGY|5 组规则|执行要点|决策追踪表|A-G 输出结构映射|A=|B=|C=|D=|E=|F=|G=" .planning/phases/03-route-rules/03-ROUTE-RULES.md` 命中 analog 对齐与 A-G 映射内容。
    - `rg -n "ROUTE-01|ROUTE-02|ROUTE-03|不执行真实 API 请求|不构造 URI|不生成二维码" .planning/phases/03-route-rules/03-ROUTE-RULES.md` 命中需求与范围边界。
  </acceptance_criteria>
  <verify>
    <automated>rg -n "D-01|D-02|D-03|D-04|D-05|D-06|D-07|D-08|D-09|D-10|D-11|D-12|D-13|自然语言优先|按路段|解析自然语言|用户指定优先|默认骑行|城市中心|最后一个候选 POI|A 段|正文|连续重复|非连续重复|可能绕路|时间预算优先|02-POI-STRATEGY|5 组规则|执行要点|决策追踪表|A-G 输出结构映射|A=|B=|C=|D=|E=|F=|G=|ROUTE-01|ROUTE-02|ROUTE-03|不执行真实 API 请求|不构造 URI|不生成二维码" .planning/phases/03-route-rules/03-ROUTE-RULES.md</automated>
  </verify>
  <done>路线规则文档可独立指导执行，且 D-01..D-13（含 D-05）与 ROUTE-01/02/03、A-G 结构均可追溯。</done>
</task>

<task type="auto">
  <name>Task 2: 编写路线说明模板并固定风险提示口径</name>
  <files>.planning/phases/03-route-rules/03-ROUTE-NOTE-TEMPLATE.md</files>
  <read_first>
    - .planning/phases/03-route-rules/03-ROUTE-RULES.md
    - .planning/phases/03-route-rules/03-CONTEXT.md
    - .planning/phases/01-input-contract-missing-assumptions/01-ASSUMPTIONS-TEMPLATE.md
    - .planning/phases/02-poi/02-POI-REASON-TEMPLATE.md
    - .planning/REQUIREMENTS.md
  </read_first>
  <action>
    创建 `03-ROUTE-NOTE-TEMPLATE.md`，定义固定模板 ID（N1..N8），至少包含：N1 起点缺失声明
    （per D-06、D-09）、N2 终点缺失声明（per D-07、D-09）、N3 双缺失声明（per D-08、D-09）、
    N4 用户指定方式与常识冲突提示（per D-04）、N5 非连续重复可能绕路提示（per D-12）、
    N6 回路保留声明（per D-11）、N7 预算优先导致回路弱化声明（per D-13）、N8 默认骑行补位声明
    （per D-03）。新增自然语言输入解析模板 N9（per D-05）：把用户自然语言交通方式指令映射到段级
    mode，并给出“原句 + 解析结果 + 未命中段默认骑行”的固定句式。模板必须区分 A 段声明与正文声明各自
    的固定句式，并新增 `A-G 固定结构模板映射`（A..G 每段至少 1 个模板入口）。附 machine-readable
    appendix JSON 样例字段：`note_template_id`、`decision_refs`、`assumption_refs`、
    `segment_mode_input`、`loop_flag`、`budget_override`、`risk_level`。文本中显式写明本阶段
    不输出 URI/二维码。
  </action>
  <acceptance_criteria>
    - `rg -n "^N1\\.|^N2\\.|^N3\\.|^N4\\.|^N5\\.|^N6\\.|^N7\\.|^N8\\.|^N9\\." .planning/phases/03-route-rules/03-ROUTE-NOTE-TEMPLATE.md` 命中 9 个模板 ID（含 D-05）。
    - `rg -n "自然语言优先|原句|解析结果|未命中段默认骑行|A 段|正文|城市中心|最后一个候选 POI|默认骑行|可能绕路|预算优先|回路弱化" .planning/phases/03-route-rules/03-ROUTE-NOTE-TEMPLATE.md` 命中固定文案要素。
    - `rg -n "A-G 固定结构模板映射|A 段|B 段|C 段|D 段|E 段|F 段|G 段|note_template_id|decision_refs|assumption_refs|segment_mode_input|loop_flag|budget_override|risk_level|ROUTE-03|不输出 URI|不输出二维码" .planning/phases/03-route-rules/03-ROUTE-NOTE-TEMPLATE.md` 命中 A-G 与机读字段、边界声明。
  </acceptance_criteria>
  <verify>
    <automated>rg -n "^N1\\.|^N2\\.|^N3\\.|^N4\\.|^N5\\.|^N6\\.|^N7\\.|^N8\\.|^N9\\.|自然语言优先|原句|解析结果|未命中段默认骑行|A-G 固定结构模板映射|A 段|B 段|C 段|D 段|E 段|F 段|G 段|城市中心|最后一个候选 POI|默认骑行|可能绕路|预算优先|回路弱化|note_template_id|decision_refs|assumption_refs|segment_mode_input|loop_flag|budget_override|risk_level|ROUTE-03|不输出 URI|不输出二维码" .planning/phases/03-route-rules/03-ROUTE-NOTE-TEMPLATE.md</automated>
  </verify>
  <done>说明模板可直接用于生成 A-G 固定结构输出（含 D-05 自然语言输入解析提示），且风险提示与决策追踪字段一致。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user_intent -> route_rules | 不受信任的路线偏好进入规则决策 |
| route_rules -> output_notes | 规则结果被模板文本消费，若丢失决策 ID 将不可追溯 |
| phase3_output -> phase4_uri | Phase 4 依赖分段方式和顺序，字段漂移会导致 URI 组装错误 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | S | transport mode assignment | mitigate | 在规则中固定“用户指定优先、未指定补骑行”（D-01/D-03），禁止隐式改写输入。 |
| T-03-02 | T | fallback assumptions | mitigate | 要求 A 段和正文同时声明兜底来源（D-09），并通过模板 ID 与 decision_refs 固化。 |
| T-03-03 | R | loop/budget adjustments | mitigate | 回路保留与预算优先调整必须写明触发条件与解释语句（D-11/D-13），保留可审计文本。 |
| T-03-04 | I | phase boundary leakage | mitigate | 文档明确“不执行真实 API 请求/不构造 URI/不生成二维码”，阻断越界输出。 |
| T-03-05 | D | missing endpoint handling | mitigate | 缺失起终点时强制兜底策略（D-06/D-07/D-08），避免因缺字段导致流程中断。 |
| T-03-06 | E | downstream privilege misuse | accept | 本计划仅产出文档模板，无执行权限操作；权限提升面低，后续执行期继续校验。 |
</threat_model>

<verification>
1. `03-ROUTE-RULES.md` 与 `03-ROUTE-NOTE-TEMPLATE.md` 文件存在。
2. D-01..D-13 在规则或模板中均有对应条目与映射。
3. ROUTE-01/02/03 与 phase scope boundary 在文档中可 grep 命中。
</verification>

<success_criteria>
1. ROUTE-01：顺序规则可直接生成起点-途经-终点完整链路。
2. ROUTE-02：每段交通方式分配和耗时说明模板已固化。
3. ROUTE-03：缺失起终点时的兜底及显式声明模板可直接执行。
</success_criteria>

<output>
After completion, create `.planning/phases/03-route-rules/03-01-SUMMARY.md`
</output>
