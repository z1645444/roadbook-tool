---
phase: 01-conversation-intake-and-constraint-model
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - src/shared/validation/intake-turn.dto.ts
  - src/conversation/constraint-extractor.interface.ts
  - src/conversation/clarification-policy.service.ts
  - src/conversation/slot-resolver.service.ts
  - src/constraints/constraint-normalizer.service.ts
  - src/conversation/intake.controller.ts
  - tests/conversation/intake-confirmation.test.ts
  - tests/constraints/waypoint-normalization.test.ts
  - tests/constraints/intensity-profile.test.ts
autonomous: true
requirements:
  - CONV-01
  - CONV-02
  - CONV-05
must_haves:
  truths:
    - "User can provide origin and destination and receives structured confirmation gating before planning."
    - "User can provide waypoints that are normalized into accepted route points or blocked as ambiguous."
    - "Conversation asks only missing or uncertain slots instead of repeating accepted fields."
  artifacts:
    - path: "src/conversation/slot-resolver.service.ts"
      provides: "Slot-state progression for required intake fields."
      exports: ["resolveNextSlotAction"]
    - path: "src/conversation/clarification-policy.service.ts"
      provides: "Confidence-gated ambiguity decisions and clarification prompts."
      exports: ["evaluateClarificationNeed"]
    - path: "src/conversation/intake.controller.ts"
      provides: "Chat intake endpoint enforcing confirmation gate."
      exports: ["IntakeController"]
  key_links:
    - from: "src/conversation/intake.controller.ts"
      to: "src/conversation/slot-resolver.service.ts"
      via: "controller delegates turn processing to slot resolver."
      pattern: "slotResolver"
    - from: "src/conversation/slot-resolver.service.ts"
      to: "src/conversation/clarification-policy.service.ts"
      via: "ambiguity decisions route to blocking clarification flow."
      pattern: "clarification"
---

<objective>
Implement conversation-intake orchestration so all required slots are captured with blocking
ambiguity handling and explicit confirmation gating.

Purpose: Deliver deterministic chat intake behavior for origin/destination/waypoints prior to
downstream routing phases.
Output: Intake DTO + controller + slot resolver + clarification policy + requirement-linked tests.
</objective>

<execution_context>
@/Users/franklin/.codex/get-shit-done/workflows/execute-plan.md
@/Users/franklin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-conversation-intake-and-constraint-model/01-CONTEXT.md
@.planning/phases/01-conversation-intake-and-constraint-model/01-RESEARCH.md
@.planning/phases/01-conversation-intake-and-constraint-model/01-PATTERNS.md
@.planning/phases/01-conversation-intake-and-constraint-model/01-01-SUMMARY.md

<interfaces>
From `src/constraints/constraint-draft.model.ts` (created in 01-01):
```typescript
export type SlotStatus = 'missing' | 'accepted' | 'ambiguous';
export interface ConstraintDraft {
  id: string;
  slots: {
    origin?: { raw: string; status: SlotStatus; normalized?: CanonicalPoint };
    destination?: { raw: string; status: SlotStatus; normalized?: CanonicalPoint };
    waypoints: Array<{ raw: string; status: 'accepted' | 'ambiguous'; normalized?: CanonicalPoint }>;
    dateRange?: { raw: string; normalized?: { startDate: string; endDate: string } };
    tripDays?: { raw: string; normalized?: number };
    rideWindow?: { raw: string; normalized?: { start: string; end: string; minutes: number } };
    intensity?: { raw: string; normalized?: 'easy' | 'standard' | 'challenge' };
  };
  revisionLog: Array<{ field: string; turnId: string; timestampIso: string }>;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build intake DTO contract and slot-driven turn resolver</name>
  <read_first>
    - src/constraints/constraint-draft.model.ts
    - .planning/phases/01-conversation-intake-and-constraint-model/01-CONTEXT.md
    - .planning/phases/01-conversation-intake-and-constraint-model/01-PATTERNS.md
  </read_first>
  <files>src/shared/validation/intake-turn.dto.ts, src/conversation/slot-resolver.service.ts, tests/conversation/intake-confirmation.test.ts</files>
  <behavior>
    - Test 1: Resolver asks for origin/destination/waypoints only when missing or uncertain.
    - Test 2: Accepted fields are not re-asked in subsequent turns.
    - Test 3: Resolver emits `ready_for_confirmation` only when all required slots are accepted.
  </behavior>
  <action>Define DTO fields for turn payload keys `{ sessionId, turnId, message, proposedSlots }`
  with class-validator decorators and strict typing, add `ConstraintExtractor` interface and default
  deterministic parser-first adapter wiring (resolved Open Question #2), then implement resolver
  logic that evaluates slot completeness in this order: origin, destination, waypoints, dateRange,
  tripDays, rideWindow, intensity (per D-01). Ensure accepted slots are skipped in follow-up prompts
  (per D-03), and emit a structured state machine output (`need_slot`, `need_clarification`,
  `ready_for_confirmation`).</action>
  <acceptance_criteria>
    - `rg -n "sessionId|turnId|proposedSlots" src/shared/validation/intake-turn.dto.ts` returns DTO fields.
    - `rg -n "export interface ConstraintExtractor|extractConstraints" src/conversation/constraint-extractor.interface.ts`
      confirms extraction contract for future LLM adapter integration.
    - `rg -n "ready_for_confirmation|need_slot|need_clarification" src/conversation/slot-resolver.service.ts`
      returns all three action states.
    - `pnpm vitest run tests/conversation/intake-confirmation.test.ts -t "CONV-01"` passes.
  </acceptance_criteria>
  <verify>
    <automated>pnpm vitest run tests/conversation/intake-confirmation.test.ts -t "CONV-01"</automated>
  </verify>
  <done>Turn resolver deterministically drives slot collection and stops re-asking accepted values.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce blocking ambiguity and confidence-gated clarification for points</name>
  <read_first>
    - src/conversation/slot-resolver.service.ts
    - src/constraints/constraint-draft.model.ts
    - .planning/phases/01-conversation-intake-and-constraint-model/01-CONTEXT.md
  </read_first>
  <files>src/conversation/clarification-policy.service.ts, src/conversation/slot-resolver.service.ts, src/constraints/constraint-normalizer.service.ts, tests/constraints/waypoint-normalization.test.ts, tests/constraints/intensity-profile.test.ts</files>
  <behavior>
    - Test 1: Ambiguous waypoint candidates force a blocking clarification response.
    - Test 2: Low-confidence values below configured threshold trigger clarification.
    - Test 3: Unavoidable inference is tagged as assumption and surfaced for one-turn correction.
    - Test 4: Intensity `easy|standard|challenge` maps to deterministic distance/duration caps.
  </behavior>
  <action>Implement clarification policy service with explicit threshold constant
  `MIN_ACCEPT_CONFIDENCE` (discretionary value) and decision results `{ accepted, ambiguous,
  assumed }`. Wire resolver so ambiguous points are never promoted to accepted slots without user
  disambiguation (per D-07), and low-confidence parse outcomes always return clarification prompts
  (per D-08). If resolver must infer value, record `assumption` metadata for recap rendering and
  one-turn correction (per D-09). Implement deterministic intensity mapping in
  `constraint-normalizer.service.ts` for `easy|standard|challenge` caps to complete D-01 slot
  coverage with D-06 normalization-at-write semantics.</action>
  <acceptance_criteria>
    - `rg -n "MIN_ACCEPT_CONFIDENCE|accepted|ambiguous|assumed" src/conversation/clarification-policy.service.ts`
      returns all required policy elements.
    - `rg -n "assumption|clarification" src/conversation/slot-resolver.service.ts` confirms
      inference + clarification wiring.
    - `rg -n "easy|standard|challenge" src/constraints/constraint-normalizer.service.ts`
      confirms explicit intensity mapping.
    - `pnpm vitest run tests/constraints/waypoint-normalization.test.ts -t "CONV-02"` passes.
    - `pnpm vitest run tests/constraints/intensity-profile.test.ts -t "CONV-05"` passes.
  </acceptance_criteria>
  <verify>
    <automated>pnpm vitest run tests/constraints/waypoint-normalization.test.ts tests/constraints/intensity-profile.test.ts</automated>
  </verify>
  <done>Ambiguous and low-confidence points are blocking until clarified, with explicit assumption
  handling in resolver output.</done>
</task>

<task type="auto">
  <name>Task 3: Add intake controller endpoint enforcing confirmation gate contract</name>
  <read_first>
    - src/shared/validation/intake-turn.dto.ts
    - src/conversation/slot-resolver.service.ts
    - .planning/phases/01-conversation-intake-and-constraint-model/01-CONTEXT.md
  </read_first>
  <files>src/conversation/intake.controller.ts, tests/conversation/intake-confirmation.test.ts</files>
  <action>Create `POST /conversation/intake/turn` endpoint that validates `IntakeTurnDto`,
  delegates to resolver, and returns structured response keys `{ status, missingSlots,
  clarificationPrompt, recap, confirmationRequired }`. Force `confirmationRequired: true` and deny
  any route-generation transition until recap is explicitly confirmed (per D-02).</action>
  <acceptance_criteria>
    - `rg -n "POST|intake/turn|confirmationRequired" src/conversation/intake.controller.ts` returns endpoint and gate fields.
    - `rg -n "cannot proceed|confirmation" tests/conversation/intake-confirmation.test.ts` confirms
      guard assertions.
    - `pnpm vitest run tests/conversation/intake-confirmation.test.ts` passes.
  </acceptance_criteria>
  <verify>
    <automated>pnpm vitest run tests/conversation/intake-confirmation.test.ts</automated>
  </verify>
  <done>Controller enforces explicit confirmation gate and never allows planning progression
  without confirmation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user turn payload -> intake endpoint | Untrusted chat payload enters API contract. |
| resolver output -> downstream planner gate | Intake decision controls whether planning can proceed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-05 | T | `src/shared/validation/intake-turn.dto.ts` | mitigate | Reject non-whitelisted turn payload keys and invalid types before resolver execution. |
| T-01-06 | S | `src/conversation/intake.controller.ts` | mitigate | Require `sessionId` in DTO and scope operations to that session for all reads/writes. |
| T-01-07 | E | `src/conversation/slot-resolver.service.ts` | mitigate | Block all transitions to planning state unless `confirmationRequired` is satisfied by explicit user confirmation. |
| T-01-08 | I | `src/conversation/clarification-policy.service.ts` | accept | Clarification prompts may expose candidate place names needed for disambiguation; no sensitive PII in scope for Phase 1. |
</threat_model>

<verification>
- `pnpm vitest run tests/conversation/intake-confirmation.test.ts`
- `pnpm vitest run tests/constraints/waypoint-normalization.test.ts`
- `pnpm vitest run tests/constraints/intensity-profile.test.ts`
</verification>

<success_criteria>
- Origin/destination/waypoint intake follows slot-driven flow with no re-asking accepted slots.
- Ambiguous and low-confidence points are blocked and clarified before acceptance.
- Intensity profile constraints are mapped deterministically to normalized caps.
- Controller enforces explicit recap confirmation as a hard gate to planning progression.
</success_criteria>

<output>
After completion, create `.planning/phases/01-conversation-intake-and-constraint-model/01-02-SUMMARY.md`
</output>
