---
phase: 05-markdown-roadbook-delivery
plan: 03
type: execute
wave: 3
depends_on:
  - 05-02
files_modified:
  - src/roadbook/markdown-roadbook.renderer.ts
  - tests/roadbook/markdown-roadbook.renderer.test.ts
  - tests/conversation/intake-confirmation.test.ts
  - tests/routing/lodging-integration.test.ts
autonomous: true
requirements:
  - BOOK-04
must_haves:
  truths:
    - "Roadbook includes explicit assumptions and constraint summary for user validation."
    - "Roadbook includes validation context from route metadata for reproducibility checks."
    - "Lodging fallback/no-match scenarios remain explainable in markdown."
  artifacts:
    - path: "src/roadbook/markdown-roadbook.renderer.ts"
      provides: "BOOK-04 sections for assumptions, constraint summary, and metadata footer."
      exports: ["renderMarkdownRoadbook"]
    - path: "tests/roadbook/markdown-roadbook.renderer.test.ts"
      provides: "Renderer coverage for assumptions, constraint summary, metadata, and fallback wording."
      contains: "BOOK-04"
    - path: "tests/conversation/intake-confirmation.test.ts"
      provides: "Controller-level BOOK-04 assertions for route-ready markdown output."
      contains: "BOOK-04"
  key_links:
    - from: "src/roadbook/markdown-roadbook.renderer.ts"
      to: "src/recap/recap-projection.service.ts"
      via: "Recap summary and assumptions are surfaced as validation section in markdown."
      pattern: "summary|assumptions"
    - from: "src/roadbook/markdown-roadbook.renderer.ts"
      to: "src/conversation/intake.controller.ts"
      via: "Route metadata fields are rendered in output footer for operator traceability."
      pattern: "generatedAtIso|requestFingerprint|responseHash"
    - from: "tests/roadbook/markdown-roadbook.renderer.test.ts"
      to: "tests/routing/lodging-integration.test.ts"
      via: "No-match fallback wording aligns with existing routing fallback trace behavior."
      pattern: "no_match|fallbackTrace"
---

<objective>
Harden phase coverage by adding validation-facing sections to the markdown output and closing the
remaining BOOK-04 contract with deterministic tests.

Purpose: Ensure users can validate plan correctness from assumptions, constraints, and metadata.
Output: Renderer enhancements and comprehensive BOOK-04 regression coverage.
</objective>

<execution_context>
@/Users/franklin/.codex/get-shit-done/workflows/execute-plan.md
@/Users/franklin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-markdown-roadbook-delivery/05-RESEARCH.md
@.planning/phases/05-markdown-roadbook-delivery/05-VALIDATION.md
@src/recap/recap-projection.service.ts
@src/conversation/intake.controller.ts
@src/roadbook/markdown-roadbook.renderer.ts
@tests/conversation/intake-confirmation.test.ts
@tests/routing/lodging-integration.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing BOOK-04 tests for assumptions, constraints, and metadata context</name>
  <read_first>
    - tests/roadbook/markdown-roadbook.renderer.test.ts
    - tests/conversation/intake-confirmation.test.ts
    - src/recap/recap-projection.service.ts
    - src/conversation/intake.controller.ts
    - .planning/phases/05-markdown-roadbook-delivery/05-VALIDATION.md
  </read_first>
  <files>tests/roadbook/markdown-roadbook.renderer.test.ts, tests/conversation/intake-confirmation.test.ts</files>
  <action>Add BOOK-04 labeled tests asserting markdown contains:
constraint summary block populated from recap summary,
assumptions list rendered from `recap.assumptions`,
route metadata footer (`generatedAtIso`, provider/endpoint, `requestFingerprint`, `responseHash`),
and fallback trace wording when day lodging policy is `no_match`.
Add one single-day assertion that no lodging section is emitted while constraint summary remains present.</action>
  <acceptance_criteria>
    - Test names include `BOOK-04` and cover unit plus integration paths.
    - Assertions verify content, not implementation details or private helpers.
    - `pnpm vitest run tests/roadbook/markdown-roadbook.renderer.test.ts tests/conversation/intake-confirmation.test.ts -t "BOOK-04"` fails before Task 2 and passes after Task 2.
  </acceptance_criteria>
  <verify>
    <automated>pnpm vitest run tests/roadbook/markdown-roadbook.renderer.test.ts tests/conversation/intake-confirmation.test.ts -t "BOOK-04"</automated>
  </verify>
  <done>Failing BOOK-04 tests exist for assumptions/constraints/metadata sections and fallback context coverage.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement BOOK-04 validation sections and finalize phase regression suite</name>
  <read_first>
    - src/roadbook/markdown-roadbook.renderer.ts
    - tests/roadbook/markdown-roadbook.renderer.test.ts
    - tests/conversation/intake-confirmation.test.ts
    - tests/routing/lodging-integration.test.ts
  </read_first>
  <files>src/roadbook/markdown-roadbook.renderer.ts, tests/roadbook/markdown-roadbook.renderer.test.ts, tests/conversation/intake-confirmation.test.ts, tests/routing/lodging-integration.test.ts</files>
  <action>Update renderer to include dedicated sections:
`## 约束摘要` using recap summary,
`## 假设与修正` listing assumptions and correction guidance,
`## 校验上下文` with metadata fingerprint/hash fields.
For lodging `no_match` days, include fallback trace narrative to preserve explainability from Phase 4.
Keep output deterministic and additive; do not alter route plan structure or routing behavior.
Update/align tests so BOOK-04 assertions are stable and Phase 4 lodging integration tests remain green.</action>
  <acceptance_criteria>
    - Markdown output includes assumptions/constraints/metadata sections in BOOK-04 cases.
    - Multiday `no_match` fallback context appears in markdown without breaking BOOK-03 formatting.
    - `pnpm vitest run tests/roadbook/markdown-roadbook.renderer.test.ts tests/conversation/intake-confirmation.test.ts -t "BOOK-04"` passes.
    - `pnpm vitest run tests/routing/lodging-integration.test.ts` passes (no regression).
  </acceptance_criteria>
  <verify>
    <automated>pnpm vitest run tests/roadbook/markdown-roadbook.renderer.test.ts tests/conversation/intake-confirmation.test.ts tests/routing/lodging-integration.test.ts</automated>
  </verify>
  <done>Renderer and tests satisfy BOOK-04 with deterministic validation sections and no lodging integration regression.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Recap/assumption content -> validation markdown sections | Free-form text becomes authoritative validation guidance for users. |
| Route metadata -> user-visible footer | Internal diagnostic fields are exposed externally and must stay constrained. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-07 | I | `src/roadbook/markdown-roadbook.renderer.ts` | mitigate | Expose only explicit metadata keys needed for validation; do not render provider raw payloads. |
| T-05-08 | T | `src/roadbook/markdown-roadbook.renderer.ts` | mitigate | Escape recap and assumption text before rendering to prevent markdown control injection. |
| T-05-09 | R | `tests/roadbook/markdown-roadbook.renderer.test.ts` | mitigate | BOOK-04 tests assert presence of constraints/assumptions/metadata sections and fallback trace behavior. |
</threat_model>

<verification>
- `pnpm vitest run tests/roadbook/markdown-roadbook.renderer.test.ts tests/conversation/intake-confirmation.test.ts -t "BOOK-04"`
- `pnpm vitest run tests/routing/lodging-integration.test.ts`
- `pnpm vitest run tests/roadbook tests/conversation tests/routing`
</verification>

<success_criteria>
- Users can inspect assumptions and constraints directly in markdown output to validate correctness.
- Validation context includes reproducibility metadata fields for auditability.
- BOOK-04 tests pass without regressing existing lodging/routing integration behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/05-markdown-roadbook-delivery/05-03-SUMMARY.md`
</output>
