---
phase: 02-routing-baseline-and-reliability
plan: 04
type: execute
wave: 4
depends_on:
  - 02-01
  - 02-02
  - 02-03
files_modified:
  - src/conversation/intake.controller.ts
  - tests/conversation/intake-confirmation.test.ts
  - tests/routing/phase2-routing-integration.test.ts
autonomous: true
requirements:
  - ROUT-01
  - ROUT-02
  - ROUT-05
  - RELY-01
  - RELY-03
must_haves:
  truths:
    - "Chat intake flow can invoke Phase 2 routing and return deterministic route/fallback state."
    - "Route generation path preserves ambiguity blocking, single-day guard, and fallback handling together."
    - "Operators can inspect metadata fields in integration outputs for reproducibility diagnostics."
  artifacts:
    - path: "src/conversation/intake.controller.ts"
      provides: "Controller integration path exposing routing result alongside confirmation semantics."
      exports: ["IntakeController"]
    - path: "tests/routing/phase2-routing-integration.test.ts"
      provides: "End-to-end regression tests covering all Phase 2 requirements."
      contains: "ROUT-01, ROUT-02, ROUT-05, RELY-01, RELY-03"
    - path: "tests/conversation/intake-confirmation.test.ts"
      provides: "Conversation flow assertions for routing handoff safety."
      contains: "confirmationRequired"
  key_links:
    - from: "src/conversation/intake.controller.ts"
      to: "src/routing/routing-orchestrator.service.ts"
      via: "controller delegates confirmed drafts to route orchestrator."
      pattern: "RoutingOrchestratorService"
    - from: "tests/routing/phase2-routing-integration.test.ts"
      to: "src/reliability/repro-metadata.service.ts"
      via: "integration asserts metadata fields present in route result."
      pattern: "requestFingerprint|responseHash"
---

<objective>
Finalize Phase 2 with intake-to-routing integration and end-to-end requirement regression coverage.

Purpose: Prove all Phase 2 requirements behave together in the chat-first flow expected by product.
Output: Controller integration wiring and phase-level integration test suite.
</objective>

<execution_context>
@/Users/franklin/.codex/get-shit-done/workflows/execute-plan.md
@/Users/franklin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-routing-baseline-and-reliability/02-RESEARCH.md
@.planning/phases/02-routing-baseline-and-reliability/02-VALIDATION.md
@.planning/phases/02-routing-baseline-and-reliability/02-PATTERNS.md
@.planning/phases/02-routing-baseline-and-reliability/02-03-SUMMARY.md
@.planning/phases/01-conversation-intake-and-constraint-model/01-03-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire intake controller to routing orchestrator after confirmation gate</name>
  <read_first>
    - src/conversation/intake.controller.ts
    - src/routing/routing-orchestrator.service.ts
    - tests/conversation/intake-confirmation.test.ts
  </read_first>
  <files>src/conversation/intake.controller.ts, tests/conversation/intake-confirmation.test.ts</files>
  <behavior>
    - Test 1: Unconfirmed intake state does not trigger route generation.
    - Test 2: Confirmed state triggers routing orchestrator and returns route payload or fallback.
  </behavior>
  <action>Update intake controller flow so confirmed drafts call `RoutingOrchestratorService` and
  include routing result payload keys in response (`routePlan`, `routingStatus`, `fallbackMessage`,
  `routeMetadata`). Preserve existing `confirmationRequired` behavior before confirmation and keep
  ambiguity clarification as a hard blocker for routing trigger.</action>
  <acceptance_criteria>
    - `rg -n "RoutingOrchestratorService|routePlan|routingStatus|fallbackMessage|routeMetadata" src/conversation/intake.controller.ts` confirms integration response fields.
    - `rg -n "does not trigger route generation|confirmed state triggers" tests/conversation/intake-confirmation.test.ts` confirms gate assertions.
    - `pnpm vitest run tests/conversation/intake-confirmation.test.ts` passes.
  </acceptance_criteria>
  <verify>
    <automated>pnpm vitest run tests/conversation/intake-confirmation.test.ts</automated>
  </verify>
  <done>Chat intake controller delegates to routing only at confirmed state and returns structured routing outputs.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create phase-level integration tests for all Phase 2 requirement IDs</name>
  <read_first>
    - .planning/phases/02-routing-baseline-and-reliability/02-VALIDATION.md
    - src/conversation/intake.controller.ts
    - src/reliability/repro-metadata.service.ts
  </read_first>
  <files>tests/routing/phase2-routing-integration.test.ts</files>
  <behavior>
    - Test 1 (ROUT-01): Ambiguous geocode prevents route generation and returns clarification state.
    - Test 2 (ROUT-02): Ordered points yield deterministic ordered segments.
    - Test 3 (ROUT-05): One-day trip yields one-day route artifact.
    - Test 4 (RELY-01): Quota/auth/rate/provider failures return mapped fallback messages.
    - Test 5 (RELY-03): Route response contains timestamp/provider/request hash/response hash.
  </behavior>
  <action>Add `tests/routing/phase2-routing-integration.test.ts` using provider/repository doubles to
  exercise intake-confirmation through orchestration end-to-end. Name each assertion block with
  requirement IDs in test descriptions and include explicit checks for route metadata and fallback
  category fields.</action>
  <acceptance_criteria>
    - `rg -n "ROUT-01|ROUT-02|ROUT-05|RELY-01|RELY-03" tests/routing/phase2-routing-integration.test.ts` returns all required IDs.
    - `rg -n "requestFingerprint|responseHash|generatedAtIso|fallback" tests/routing/phase2-routing-integration.test.ts` confirms reliability assertions.
    - `pnpm vitest run tests/routing/phase2-routing-integration.test.ts` passes.
  </acceptance_criteria>
  <verify>
    <automated>pnpm vitest run tests/routing/phase2-routing-integration.test.ts</automated>
  </verify>
  <done>All Phase 2 requirements are covered by integration-level, requirement-traceable tests.</done>
</task>

<task type="auto">
  <name>Task 3: Stabilize regression command surface for phase execution and verification</name>
  <read_first>
    - .planning/phases/02-routing-baseline-and-reliability/02-VALIDATION.md
    - tests/routing/phase2-routing-integration.test.ts
    - tests/reliability/amap-fallback-errors.test.ts
  </read_first>
  <files>tests/routing/phase2-routing-integration.test.ts, tests/conversation/intake-confirmation.test.ts</files>
  <action>Ensure test naming and filtering align with phase execution expectations: add stable
  describe labels for requirement IDs, avoid flaky timing/network dependencies by fully mocking
  provider transport, and add a fast smoke command (<30s target) for immediate feedback. Reserve
  the full routing+reliability sweep for wave-end verification only.</action>
  <acceptance_criteria>
    - `rg -n "describe\\(|ROUT-|RELY-" tests/routing/phase2-routing-integration.test.ts tests/conversation/intake-confirmation.test.ts` confirms stable labels.
    - `pnpm vitest run tests/routing/phase2-routing-integration.test.ts -t "ROUT-01|ROUT-02" --reporter=dot` completes as smoke check (<30s target).
    - Wave-end verification still runs full sweep: `pnpm vitest run tests/routing tests/reliability tests/conversation/intake-confirmation.test.ts --reporter=dot`.
  </acceptance_criteria>
  <verify>
    <automated>pnpm vitest run tests/routing/phase2-routing-integration.test.ts -t "ROUT-01|ROUT-02" --reporter=dot</automated>
  </verify>
  <done>Phase 2 regression surface is stable, deterministic, and execution-ready for verification workflows.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| conversation endpoint -> routing orchestration | User-driven intake transitions into external-provider route generation. |
| routing diagnostics -> operator-visible outputs | Internal metadata crosses into support/diagnostic surfaces. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-13 | E | `src/conversation/intake.controller.ts` | mitigate | Enforce confirmation and clarification gates before any routing invocation to prevent unauthorized state transitions. |
| T-02-14 | T | `tests/routing/phase2-routing-integration.test.ts` | mitigate | Lock ordered-segment and single-day invariants in integration tests to prevent behavioral regressions. |
| T-02-15 | I | `src/conversation/intake.controller.ts` | mitigate | Restrict route response payload to safe fields; do not expose provider secrets or raw trace internals. |
| T-02-16 | R | `tests/routing/phase2-routing-integration.test.ts` | mitigate | Requirement-tagged tests establish auditable evidence for ROUT/RELY coverage before phase sign-off. |
</threat_model>

<verification>
- `pnpm vitest run tests/routing/phase2-routing-integration.test.ts`
- `pnpm vitest run tests/routing tests/reliability tests/conversation/intake-confirmation.test.ts --reporter=dot`
</verification>

<success_criteria>
- Intake-to-routing integration is wired without violating confirmation and ambiguity gates.
- All Phase 2 requirements have explicit integration regression coverage.
- Reliability metadata and fallback surfaces are test-verified in the chat-first execution path.
</success_criteria>

<output>
After completion, create `.planning/phases/02-routing-baseline-and-reliability/02-04-SUMMARY.md`
</output>
