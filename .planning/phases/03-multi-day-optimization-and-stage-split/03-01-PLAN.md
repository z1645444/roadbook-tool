---
phase: 03-multi-day-optimization-and-stage-split
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/routing/waypoint-optimizer.service.ts
  - tests/routing/waypoint-optimization.test.ts
  - src/shared/validation/multiday-route-artifact.schema.ts
autonomous: true
requirements:
  - ROUT-03
must_haves:
  truths:
    - "Waypoint optimization preserves fixed start/end points."
    - "Intermediate waypoints are reordered deterministically for route feasibility."
    - "Invalid optimization artifacts are rejected with explicit error paths before orchestration."
  artifacts:
    - path: "src/routing/waypoint-optimizer.service.ts"
      provides: "Endpoint-preserving deterministic waypoint optimizer."
      exports: ["optimizeWaypointSequence", "type WaypointOptimizationResult"]
    - path: "tests/routing/waypoint-optimization.test.ts"
      provides: "ROUT-03 deterministic optimization regression coverage."
      contains: "ROUT-03"
    - path: "src/shared/validation/multiday-route-artifact.schema.ts"
      provides: "Runtime schema boundary for optimized sequence + day artifacts."
      exports: ["safeParseMultidayRouteArtifact"]
  key_links:
    - from: "src/routing/waypoint-optimizer.service.ts"
      to: "src/map-provider/map-provider.port.ts"
      via: "optimizer uses provider segment estimates to score candidate orders."
      pattern: "routeBicyclingSegment"
    - from: "src/routing/waypoint-optimizer.service.ts"
      to: "src/shared/validation/multiday-route-artifact.schema.ts"
      via: "optimized output validated before return."
      pattern: "safeParseMultidayRouteArtifact"
---

<objective>
Deliver the ROUT-03 core: deterministic waypoint optimization that preserves start/end points and
produces a validated optimized sequence for downstream stage splitting.
</objective>

<execution_context>
@/Users/franklin/.codex/get-shit-done/workflows/execute-plan.md
@/Users/franklin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-multi-day-optimization-and-stage-split/03-RESEARCH.md
@.planning/phases/03-multi-day-optimization-and-stage-split/03-VALIDATION.md
@.planning/phases/03-multi-day-optimization-and-stage-split/03-PATTERNS.md
@.planning/phases/02-routing-baseline-and-reliability/02-04-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add deterministic endpoint-preserving waypoint optimizer service</name>
  <read_first>
    - src/routing/segment-routing.service.ts
    - src/map-provider/map-provider.port.ts
    - src/constraints/constraint-draft.model.ts
    - .planning/phases/03-multi-day-optimization-and-stage-split/03-PATTERNS.md
  </read_first>
  <files>src/routing/waypoint-optimizer.service.ts</files>
  <behavior>
    - Test fixture with origin + 3 waypoints + destination returns same optimized order on repeated runs.
    - Optimized sequence keeps index 0 and last index identical to input origin/destination.
  </behavior>
  <action>Create `optimizeWaypointSequence(points, provider)` that:
1) treats `points[0]` as fixed origin and `points[points.length-1]` as fixed destination,
2) scores candidate intermediate waypoint orders with provider leg distance+duration estimates,
3) returns the minimum-score deterministic order with stable tie-break on `providerId` lexical sort,
4) falls back to original order when there are fewer than 2 intermediate waypoints.</action>
  <acceptance_criteria>
    - `rg -n "export const optimizeWaypointSequence|fixed origin|fixed destination|tie-break" src/routing/waypoint-optimizer.service.ts` finds deterministic optimizer logic.
    - `rg -n "ROUT-03|preserves start/end|deterministic" tests/routing/waypoint-optimization.test.ts` confirms requirement-tagged assertions.
    - `pnpm vitest run tests/routing/waypoint-optimization.test.ts -t "ROUT-03"` passes.
  </acceptance_criteria>
  <verify>
    <automated>pnpm vitest run tests/routing/waypoint-optimization.test.ts -t "ROUT-03"</automated>
  </verify>
  <done>ROUT-03 optimization behavior is deterministic and endpoint-safe.</done>
</task>

<task type="auto">
  <name>Task 2: Add multiday route artifact schema boundary for optimized sequence payloads</name>
  <read_first>
    - src/shared/validation/route-artifact.schema.ts
    - src/routing/waypoint-optimizer.service.ts
    - .planning/phases/03-multi-day-optimization-and-stage-split/03-PATTERNS.md
  </read_first>
  <files>src/shared/validation/multiday-route-artifact.schema.ts, src/routing/waypoint-optimizer.service.ts</files>
  <action>Create `multiday-route-artifact.schema.ts` with zod schemas for:
`optimizedPoints`, `days[]`, `dayBoundary`, and totals.
Export `safeParseMultidayRouteArtifact(payload)`.
In optimizer output path, validate produced optimized sequence object before returning it.
Reject invalid payloads with explicit error message including zod issue paths.</action>
  <acceptance_criteria>
    - `rg -n "safeParseMultidayRouteArtifact|optimizedPoints|days|dayBoundary" src/shared/validation/multiday-route-artifact.schema.ts` confirms schema shape.
    - `rg -n "safeParseMultidayRouteArtifact" src/routing/waypoint-optimizer.service.ts` confirms validation boundary call.
    - `pnpm vitest run tests/routing/waypoint-optimization.test.ts` passes.
  </acceptance_criteria>
  <verify>
    <automated>pnpm vitest run tests/routing/waypoint-optimization.test.ts</automated>
  </verify>
  <done>Optimizer payload has a strict runtime validation boundary.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| accepted points -> optimization engine | Canonical points are transformed into reordered candidates and scored. |
| optimization output -> downstream orchestrator | Reordered output crosses into route orchestration and must be validated. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-01 | T | `src/routing/waypoint-optimizer.service.ts` | mitigate | Preserve fixed origin/destination and validate reordered payload shape before return. |
| T-3-02 | D | `src/routing/waypoint-optimizer.service.ts` | mitigate | Cap search branch size and use deterministic fallback for small/degenerate inputs. |
| T-3-03 | R | `tests/routing/waypoint-optimization.test.ts` | mitigate | Requirement-tagged deterministic regression tests for ROUT-03 evidence. |
| T-3-04 | I | `src/shared/validation/multiday-route-artifact.schema.ts` | mitigate | Block malformed optimized artifacts from leaking into downstream planning. |
</threat_model>

<verification>
- `pnpm vitest run tests/routing/waypoint-optimization.test.ts`
</verification>

<success_criteria>
- ROUT-03 optimizer preserves start/end and deterministically reorders intermediate waypoints.
- Optimized payload is schema-validated before orchestration use.
</success_criteria>

<output>
After completion, create `.planning/phases/03-multi-day-optimization-and-stage-split/03-01-SUMMARY.md`
</output>
