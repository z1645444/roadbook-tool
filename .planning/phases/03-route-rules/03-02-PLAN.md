---
phase: 03-route-rules
plan: "02"
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - .planning/phases/03-route-rules/03-ROUTE-OUTPUT.schema.json
  - .planning/phases/03-route-rules/03-ROUTE-OUTPUT.example.json
autonomous: true
requirements:
  - ROUTE-01
  - ROUTE-02
  - ROUTE-03
must_haves:
  truths:
    - "用户可得到机器可校验的有序路线输出。"
    - "每段都包含 from/to/mode/duration_note，可直接阅读与复核。"
    - "分段交通方式输入支持自然语言优先，并以结构化字段沉淀解析结果。"
    - "缺失起终点、回路、预算冲突会在输出中显式标注，而不是静默处理。"
  artifacts:
    - path: ".planning/phases/03-route-rules/03-ROUTE-OUTPUT.schema.json"
      provides: "Phase 3 路线输出 JSON Schema"
      contains: "segments、segment_mode_input、fallback_assumptions、loop_flags、budget_priority、A-G 输出结构映射"
    - path: ".planning/phases/03-route-rules/03-ROUTE-OUTPUT.example.json"
      provides: "normal 与 degraded 两个示例"
      contains: "完整顺序链路、分段方式+耗时、自然语言输入解析结果、兜底声明、A-G 段输出"
  key_links:
    - from: ".planning/phases/03-route-rules/03-ROUTE-OUTPUT.example.json"
      to: ".planning/phases/03-route-rules/03-ROUTE-OUTPUT.schema.json"
      via: "Ajv schema validation"
      pattern: "segments|duration_note|fallback_assumptions"
    - from: ".planning/phases/03-route-rules/03-ROUTE-OUTPUT.schema.json"
      to: ".planning/phases/03-route-rules/03-ROUTE-RULES.md"
      via: "D-01..D-13 rules encoded as schema constraints"
      pattern: "allOf|if|then|decision_refs"
---

<objective>
把 Phase 3 规则固化为可校验 schema 与可执行 example，确保下游阶段消费一致接口。

Purpose: 防止路线输出在不同执行器间漂移，保证 ROUTE-01/02/03 可自动验证。
Output: `03-ROUTE-OUTPUT.schema.json` 与 `03-ROUTE-OUTPUT.example.json`。
</objective>

<execution_context>
@/Users/franklin/.codex/get-shit-done/workflows/execute-plan.md
@/Users/franklin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-route-rules/03-CONTEXT.md
@.planning/phases/03-route-rules/03-PATTERNS.md
@.planning/phases/03-route-rules/03-01-SUMMARY.md
@.planning/phases/02-poi/02-POI-OUTPUT.schema.json
@AGENTS.md

<interfaces>
From .planning/phases/03-route-rules/03-ROUTE-RULES.md (plan 03-01 output):
- Ordered chain: origin -> waypoints -> destination (per D-10/D-11/D-12).
- Segment mode: natural-language-first input parsing, then user specified first + fallback to cycling (per D-01/D-02/D-03/D-05).
- Missing endpoints: city-center / last-candidate fallback with explicit A+body declaration (per D-06/D-07/D-08/D-09).
- Loop vs budget: budget precedence with explicit adjustment note (per D-13).
- Output contract: A-G fixed structure mapping must be preserved in output views.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 定义路线输出 schema 并编码关键条件分支</name>
  <files>.planning/phases/03-route-rules/03-ROUTE-OUTPUT.schema.json</files>
  <read_first>
    - .planning/phases/03-route-rules/03-ROUTE-RULES.md
    - .planning/phases/03-route-rules/03-CONTEXT.md
    - .planning/phases/03-route-rules/03-PATTERNS.md
    - .planning/phases/02-poi/02-POI-OUTPUT.schema.json
    - .planning/REQUIREMENTS.md
  </read_first>
  <action>
    创建 `03-ROUTE-OUTPUT.schema.json`（draft 2020-12），顶层 required 键固定为
    `trip_context`, `route_plan`, `fallback_assumptions`, `output_views`, `metadata`，
    并启用 `additionalProperties:false`。在 `route_plan.segments[]` 强制字段：
    `segment_index`, `from`, `to`, `mode`, `duration_note`, `mode_source`, `loop_flag`,
    `risk_note`，其中 `mode` 至少包含 `walking|cycling|driving|transit`，`mode_source` 为
    `user|fallback|nl_parse`。新增 `segment_mode_input` 对象（per D-05）记录自然语言优先输入：
    `raw_text`、`parsed_segments[]`、`unmatched_segments_default_mode`（固定 `cycling`）、
    `parse_confidence`。通过 `allOf/if/then` 条件分支编码：当 `segment_mode_input.raw_text`
    存在时，必须同时出现 `parsed_segments` 或 `unmatched_segments_default_mode`（per D-05）；
    当 `fallback_assumptions.origin_applied=true`
    时必须出现 `origin_source: city_center`（per D-06）；当 `destination_applied=true` 时必须出现
    `destination_source: last_candidate_poi`（per D-07）；当 `loop_detected=true` 且
    `budget_conflict=true` 时 `route_adjustment` 必须为 `budget_precedence`（per D-13）。
    约束 `decision_refs` 必含 D-01..D-13 中的相关 ID，且明确 `no_live_request=true`、
    `no_uri_construction=true`、`no_qr_generation=true` 作为 phase 边界标记。`output_views`
    新增 `ag_sections`，固定 required 为 `A`,`B`,`C`,`D`,`E`,`F`,`G`，用于锁定 AGENTS.md
    要求的 A-G 输出结构。
  </action>
  <acceptance_criteria>
    - `node -e "JSON.parse(require('fs').readFileSync('.planning/phases/03-route-rules/03-ROUTE-OUTPUT.schema.json','utf8')); console.log('schema-json-ok')"` 输出 `schema-json-ok`。
    - `rg -n "\"trip_context\"|\"route_plan\"|\"fallback_assumptions\"|\"output_views\"|\"metadata\"|\"segments\"|\"duration_note\"|\"mode_source\"|\"segment_mode_input\"|\"raw_text\"|\"parsed_segments\"|\"unmatched_segments_default_mode\"|\"ag_sections\"|\"A\"|\"B\"|\"C\"|\"D\"|\"E\"|\"F\"|\"G\"|\"loop_flag\"|\"budget_precedence\"" .planning/phases/03-route-rules/03-ROUTE-OUTPUT.schema.json` 命中结构字段。
    - `rg -n "D-01|D-02|D-03|D-04|D-05|D-06|D-07|D-08|D-09|D-10|D-11|D-12|D-13|ROUTE-01|ROUTE-02|ROUTE-03|no_live_request|no_uri_construction|no_qr_generation" .planning/phases/03-route-rules/03-ROUTE-OUTPUT.schema.json` 命中决策、需求与边界标签。
  </acceptance_criteria>
  <verify>
    <automated>node -e "JSON.parse(require('fs').readFileSync('.planning/phases/03-route-rules/03-ROUTE-OUTPUT.schema.json','utf8')); console.log('schema-json-ok')" && rg -n "\"trip_context\"|\"route_plan\"|\"fallback_assumptions\"|\"output_views\"|\"metadata\"|\"segments\"|\"duration_note\"|\"mode_source\"|\"segment_mode_input\"|\"raw_text\"|\"parsed_segments\"|\"unmatched_segments_default_mode\"|\"ag_sections\"|\"A\"|\"B\"|\"C\"|\"D\"|\"E\"|\"F\"|\"G\"|\"loop_flag\"|\"budget_precedence\"|D-01|D-02|D-03|D-04|D-05|D-06|D-07|D-08|D-09|D-10|D-11|D-12|D-13|ROUTE-01|ROUTE-02|ROUTE-03|no_live_request|no_uri_construction|no_qr_generation" .planning/phases/03-route-rules/03-ROUTE-OUTPUT.schema.json</automated>
  </verify>
  <done>Schema 对顺序、分段方式（含 D-05 自然语言优先）、耗时说明、A-G 结构、兜底与预算冲突规则均建立可机读校验约束。</done>
</task>

<task type="auto">
  <name>Task 2: 编写 normal/degraded 路线示例并完成 schema 验证</name>
  <files>.planning/phases/03-route-rules/03-ROUTE-OUTPUT.example.json</files>
  <read_first>
    - .planning/phases/03-route-rules/03-ROUTE-OUTPUT.schema.json
    - .planning/phases/03-route-rules/03-ROUTE-RULES.md
    - .planning/phases/03-route-rules/03-ROUTE-NOTE-TEMPLATE.md
    - .planning/phases/02-poi/02-POI-OUTPUT.example.json
    - .planning/REQUIREMENTS.md
  </read_first>
  <action>
    创建 `03-ROUTE-OUTPUT.example.json`，包含 `normal_case` 与 `degraded_case` 两个对象。
    `normal_case` 必须展示完整链路（起点 + 至少 2 段途经 + 终点）、每段 `mode` 与
    `duration_note`，并包含一个用户指定方式段和一个 fallback 骑行段（per D-02/D-03）。
    另外，`normal_case` 必须提供 `segment_mode_input.raw_text` 与 `parsed_segments[]`，
    演示“自然语言优先”分段输入如何映射到结构化段配置（per D-05）。
    `degraded_case` 必须展示起点缺失或终点缺失触发兜底（城市中心或最后候选 POI），A 段与正文均含
    明示假设文本（per D-06/D-07/D-08/D-09），同时包含非连续重复点触发“可能绕路”提示（per D-12），
    以及回路与预算冲突触发 `budget_precedence` 调整说明（per D-13）。`degraded_case` 还需演示
    `segment_mode_input.unmatched_segments_default_mode=cycling`（自然语言未命中段默认骑行，per D-05）。
    两种 case 都要写
    `decision_refs`、`no_live_request=true`、`no_uri_construction=true`、`no_qr_generation=true`。
    两种 case 的 `output_views` 都必须包含完整 `ag_sections`（A..G）文本，防止输出结构漂移。
    示例中禁止出现 `uri.amap.com`、`amapuri://`、二维码字段。
  </action>
  <acceptance_criteria>
    - `node -e "JSON.parse(require('fs').readFileSync('.planning/phases/03-route-rules/03-ROUTE-OUTPUT.example.json','utf8')); console.log('example-json-ok')"` 输出 `example-json-ok`。
    - `rg -n "\"normal_case\"|\"degraded_case\"|\"segments\"|\"duration_note\"|\"mode_source\"|\"segment_mode_input\"|\"raw_text\"|\"parsed_segments\"|\"unmatched_segments_default_mode\"|\"fallback_assumptions\"|\"budget_precedence\"|\"decision_refs\"|\"ag_sections\"|\"A\"|\"B\"|\"C\"|\"D\"|\"E\"|\"F\"|\"G\"" .planning/phases/03-route-rules/03-ROUTE-OUTPUT.example.json` 命中结构字段。
    - `node -e "const Ajv=require('ajv/dist/2020'); const fs=require('fs'); const base='.planning/phases/03-route-rules'; const schema=JSON.parse(fs.readFileSync(base+'/03-ROUTE-OUTPUT.schema.json','utf8')); const data=JSON.parse(fs.readFileSync(base+'/03-ROUTE-OUTPUT.example.json','utf8')); const ajv=new Ajv({allErrors:true,strict:false}); const validate=ajv.compile(schema); for (const k of ['normal_case','degraded_case']) { if (!validate(data[k])) { console.error(k, validate.errors); process.exit(1);} } console.log('schema-validate-ok');"` 输出 `schema-validate-ok`。
    - `rg -n "uri\\.amap\\.com|amapuri://|androidamap://|二维码|qr_" .planning/phases/03-route-rules/03-ROUTE-OUTPUT.example.json` 无命中。
  </acceptance_criteria>
  <verify>
    <automated>node -e "JSON.parse(require('fs').readFileSync('.planning/phases/03-route-rules/03-ROUTE-OUTPUT.example.json','utf8')); console.log('example-json-ok')" && rg -n "\"normal_case\"|\"degraded_case\"|\"segments\"|\"duration_note\"|\"mode_source\"|\"segment_mode_input\"|\"raw_text\"|\"parsed_segments\"|\"unmatched_segments_default_mode\"|\"fallback_assumptions\"|\"budget_precedence\"|\"decision_refs\"|\"ag_sections\"|\"A\"|\"B\"|\"C\"|\"D\"|\"E\"|\"F\"|\"G\"" .planning/phases/03-route-rules/03-ROUTE-OUTPUT.example.json && node -e "const Ajv=require('ajv/dist/2020'); const fs=require('fs'); const base='.planning/phases/03-route-rules'; const schema=JSON.parse(fs.readFileSync(base+'/03-ROUTE-OUTPUT.schema.json','utf8')); const data=JSON.parse(fs.readFileSync(base+'/03-ROUTE-OUTPUT.example.json','utf8')); const ajv=new Ajv({allErrors:true,strict:false}); const validate=ajv.compile(schema); for (const k of ['normal_case','degraded_case']) { if (!validate(data[k])) { console.error(k, validate.errors); process.exit(1);} } console.log('schema-validate-ok');" && ! rg -n "uri\\.amap\\.com|amapuri://|androidamap://|二维码|qr_" .planning/phases/03-route-rules/03-ROUTE-OUTPUT.example.json</automated>
  </verify>
  <done>示例完整展示正常与降级场景（含 D-05 自然语言优先输入与 A-G 固定结构），且通过同一 schema 校验并严格遵守本阶段边界。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| route_rules -> schema | 规则被编码为结构约束时可能被弱化 |
| schema -> example | 示例若不受约束会误导下游实现 |
| phase3_json -> phase4_builder | Phase 4 会消费路线 JSON 构造 URI，字段错误会级联 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-07 | T | schema conditional logic | mitigate | 使用 `allOf/if/then` 显式约束兜底来源与预算优先分支（D-06/D-07/D-13）。 |
| T-03-08 | R | decision traceability | mitigate | 强制 `decision_refs` 覆盖关键 D-xx，保证输出可回溯到已锁定决策。 |
| T-03-09 | I | out-of-scope leakage | mitigate | schema/example 固定 `no_live_request/no_uri_construction/no_qr_generation` 标记并验证。 |
| T-03-10 | D | missing endpoint degrade | mitigate | degraded_case 必须可校验通过，保证缺失起终点时仍产出有效路线说明。 |
| T-03-11 | E | downstream misuse of weak fields | mitigate | segments 关键字段设 required + `additionalProperties:false`，降低越权扩展字段风险。 |
| T-03-12 | S | transport mode source spoofing | accept | 本阶段不做真实交通校验，仅声明 `mode_source`；真实性校验留给执行阶段。 |
</threat_model>

<verification>
1. schema 与 example 都能被 JSON.parse 解析。
2. `normal_case` 与 `degraded_case` 均通过同一 schema 验证。
3. 示例不含 URI 构造、二维码字段、真实请求相关内容。
</verification>

<success_criteria>
1. ROUTE-01：示例体现完整起点-途经-终点顺序并可机读校验。
2. ROUTE-02：每段 `mode + duration_note` 为强制字段并在示例中完整出现。
3. ROUTE-03：缺失起终点兜底、显式假设、回路与预算冲突处理均可检验。
</success_criteria>

<output>
After completion, create `.planning/phases/03-route-rules/03-02-SUMMARY.md`
</output>
