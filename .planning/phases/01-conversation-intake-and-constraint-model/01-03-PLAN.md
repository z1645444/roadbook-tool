---
phase: 01-conversation-intake-and-constraint-model
plan: 03
type: execute
wave: 3
depends_on:
  - 01-01
  - 01-02
files_modified:
  - src/constraints/constraint-draft.repository.ts
  - src/recap/recap-projection.service.ts
  - tests/recap/canonical-projection.test.ts
  - tests/conversation/intake-confirmation.test.ts
autonomous: true
requirements:
  - RELY-02
must_haves:
  truths:
    - "Recap output is always regenerated from canonical draft state, never used as source-of-truth."
    - "Revisions preserve previously accepted constraints and produce updated full recap consistency."
    - "Canonical draft retains raw, normalized, and revision metadata for reproducibility."
  artifacts:
    - path: "src/constraints/constraint-draft.repository.ts"
      provides: "Session-scoped draft persistence interface and implementation."
      exports: ["ConstraintDraftRepository"]
    - path: "src/recap/recap-projection.service.ts"
      provides: "Deterministic recap renderer from canonical draft."
      exports: ["buildConstraintRecap"]
    - path: "tests/recap/canonical-projection.test.ts"
      provides: "Regression guard for canonical-to-recap projection behavior."
      contains: "RELY-02"
  key_links:
    - from: "src/constraints/constraint-draft.repository.ts"
      to: "src/recap/recap-projection.service.ts"
      via: "recap renderer receives persisted canonical draft."
      pattern: "getBySessionId"
    - from: "src/recap/recap-projection.service.ts"
      to: "tests/recap/canonical-projection.test.ts"
      via: "tests assert recap generated from draft data after patch."
      pattern: "buildConstraintRecap"
---

<objective>
Finalize Phase 1 by wiring canonical draft persistence and deterministic recap projection behavior.

Purpose: Satisfy RELY-02 and ensure recap consistency after field-level revisions.
Output: Draft repository + recap projection service + integration regression coverage.
</objective>

<execution_context>
@/Users/franklin/.codex/get-shit-done/workflows/execute-plan.md
@/Users/franklin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-conversation-intake-and-constraint-model/01-CONTEXT.md
@.planning/phases/01-conversation-intake-and-constraint-model/01-RESEARCH.md
@.planning/phases/01-conversation-intake-and-constraint-model/01-PATTERNS.md
@.planning/phases/01-conversation-intake-and-constraint-model/01-01-SUMMARY.md
@.planning/phases/01-conversation-intake-and-constraint-model/01-02-SUMMARY.md

<interfaces>
From `src/constraints/constraint-patch.service.ts` (from 01-01):
```typescript
export function applyConstraintPatch(
  draft: ConstraintDraft,
  patch: Partial<ConstraintDraft['slots']>,
  turnId: string,
  timestampIso: string
): ConstraintDraft;
```

From `src/conversation/slot-resolver.service.ts` (from 01-02):
```typescript
export type SlotResolutionState =
  | { status: 'need_slot'; slot: string }
  | { status: 'need_clarification'; slot: string; prompt: string }
  | { status: 'ready_for_confirmation'; recap: RecapPayload; confirmationRequired: true };
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement session-scoped canonical draft repository with revision-safe updates</name>
  <read_first>
    - src/constraints/constraint-draft.model.ts
    - src/constraints/constraint-patch.service.ts
    - .planning/phases/01-conversation-intake-and-constraint-model/01-CONTEXT.md
  </read_first>
  <files>src/constraints/constraint-draft.repository.ts, tests/conversation/intake-confirmation.test.ts</files>
  <behavior>
    - Test 1: Repository create/get/update operations are session-scoped.
    - Test 2: Updating one field preserves unrelated accepted slot data.
    - Test 3: Revision metadata is persisted and retrievable after updates.
  </behavior>
  <action>Create repository contract with explicit methods
  `createDraft(sessionId, initialDraft)`, `getBySessionId(sessionId)`,
  `updateDraft(sessionId, updater)`, and `appendRevision(sessionId, revisionEntry)`. Implement an
  in-phase DB/storage-backed adapter (no memory-backed runtime persistence) so canonical draft
  persistence ownership stays in the Database/Storage tier per Architectural Responsibility Map and
  D-05. Preserve raw+normalized slot payloads (per D-05) and revision metadata fields from D-12.</action>
  <acceptance_criteria>
    - `rg -n "createDraft|getBySessionId|updateDraft|appendRevision" src/constraints/constraint-draft.repository.ts`
      returns all repository methods.
    - `rg -n "sessionId" src/constraints/constraint-draft.repository.ts` confirms session-scoped access.
    - `rg -n "sql|postgres|drizzle|storage" src/constraints/constraint-draft.repository.ts` confirms
      DB/storage-backed adapter implementation.
    - `pnpm vitest run tests/conversation/intake-confirmation.test.ts -t "revision"` passes.
  </acceptance_criteria>
  <verify>
    <automated>pnpm vitest run tests/conversation/intake-confirmation.test.ts -t "revision"</automated>
  </verify>
  <done>Canonical drafts are stored/retrieved by session with deterministic revision-safe update
  semantics.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build deterministic recap projection and consistency regeneration flow</name>
  <read_first>
    - src/constraints/constraint-draft.model.ts
    - src/constraints/constraint-draft.repository.ts
    - .planning/phases/01-conversation-intake-and-constraint-model/01-CONTEXT.md
  </read_first>
  <files>src/recap/recap-projection.service.ts, tests/recap/canonical-projection.test.ts</files>
  <behavior>
    - Test 1: Recap output is generated only from canonical draft payload.
    - Test 2: After any accepted patch, full recap is regenerated and reflects all accepted slots.
    - Test 3: Assumptions and correction path markers appear when slots are inferred.
  </behavior>
  <action>Implement recap projection as a pure function/service `buildConstraintRecap(draft)` that
  derives structured recap sections for all slots and includes assumption notes for inferred values
  (per D-09). Ensure recap regeneration occurs after each accepted revision (per D-11), and never
  store recap text as mutable source state (per D-04 and RELY-02).</action>
  <acceptance_criteria>
    - `rg -n "buildConstraintRecap|assumption|correction" src/recap/recap-projection.service.ts` confirms required projection fields.
    - `pnpm vitest run tests/recap/canonical-projection.test.ts -t "RELY-02"` passes.
    - `rg -n "full recap|regenerate" tests/recap/canonical-projection.test.ts` confirms regression assertions.
  </acceptance_criteria>
  <verify>
    <automated>pnpm vitest run tests/recap/canonical-projection.test.ts -t "RELY-02"</automated>
  </verify>
  <done>Recap becomes a deterministic projection from canonical draft, with full consistency refresh
  on every revision.</done>
</task>

<task type="auto">
  <name>Task 3: Add end-to-end intake consistency regression across confirmation and revision paths</name>
  <read_first>
    - src/conversation/intake.controller.ts
    - src/recap/recap-projection.service.ts
    - tests/conversation/intake-confirmation.test.ts
  </read_first>
  <files>tests/conversation/intake-confirmation.test.ts, tests/recap/canonical-projection.test.ts</files>
  <action>Expand integration coverage to verify this exact sequence: collect all required slots,
  produce recap with `confirmationRequired: true`, apply single-field revision, regenerate full
  recap without dropping unrelated fields, and keep canonical draft as source-of-truth. Include one
  case for ambiguous location blocking and one case for inferred assumption correction path.</action>
  <acceptance_criteria>
    - `rg -n "confirmationRequired|single-field revision|ambiguous|assumption" tests/conversation/intake-confirmation.test.ts`
      shows required scenarios.
    - `pnpm vitest run tests/conversation/intake-confirmation.test.ts tests/recap/canonical-projection.test.ts`
      passes.
  </acceptance_criteria>
  <verify>
    <automated>pnpm vitest run tests/conversation/intake-confirmation.test.ts tests/recap/canonical-projection.test.ts</automated>
  </verify>
  <done>Phase 1 behavior is regression-protected across slot intake, confirmation gate, revisions,
  and recap consistency.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| session-scoped repository -> recap renderer | Persisted canonical state is exposed as user-visible recap. |
| revision updates -> audit trail | State mutation path writes reproducibility metadata. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-09 | R | `src/constraints/constraint-draft.repository.ts` | mitigate | Persist immutable revision entries with field, source turn, and timestamp for every accepted update. |
| T-01-10 | T | `src/recap/recap-projection.service.ts` | mitigate | Generate recap from canonical draft only; reject direct free-text recap mutations as state input. |
| T-01-11 | I | `src/recap/recap-projection.service.ts` | mitigate | Limit recap output to accepted/assumed slot values and avoid exposing internal parser/debug fields. |
| T-01-12 | D | `tests/recap/canonical-projection.test.ts` | accept | Test suite runtime overhead is low and controlled; no production DoS vector introduced. |
</threat_model>

<verification>
- `pnpm vitest run tests/recap/canonical-projection.test.ts`
- `pnpm vitest run tests/conversation/intake-confirmation.test.ts`
- `pnpm vitest run tests/conversation/intake-confirmation.test.ts tests/recap/canonical-projection.test.ts`
</verification>

<success_criteria>
- Canonical draft persistence and recap projection are wired end-to-end by session.
- Recap remains a derived projection and is regenerated after every accepted revision.
- Automated regression tests cover confirmation gate, revision consistency, and ambiguity handling.
</success_criteria>

<output>
After completion, create `.planning/phases/01-conversation-intake-and-constraint-model/01-03-SUMMARY.md`
</output>
