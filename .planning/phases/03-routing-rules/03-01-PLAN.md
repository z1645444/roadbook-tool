---
phase: 03-routing-rules
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/03-routing-rules/03-ROUTE-STRATEGY.md
  - .planning/phases/03-routing-rules/03-DURATION-HEURISTICS.md
autonomous: true
requirements:
  - ROUTE-01
  - ROUTE-02
  - ROUTE-03
must_haves:
  truths:
    - "路线输出必须完整覆盖起点、途经点、终点，不允许出现断链或顺序冲突。"
    - "每一段都必须明确交通方式和简要耗时估算，且估算口径可重复。"
    - "起点或终点缺失时，输出必须给出显式假设而非中断。"
  artifacts:
    - path: ".planning/phases/03-routing-rules/03-ROUTE-STRATEGY.md"
      provides: "Phase 3 路线编排主规则"
      contains: "顺序规则、节点角色、缺失字段假设策略"
    - path: ".planning/phases/03-routing-rules/03-DURATION-HEURISTICS.md"
      provides: "分段方式与耗时估算规则"
      contains: "mode 选择矩阵、分钟估算、置信注记"
  key_links:
    - from: ".planning/phases/02-poi/02-POI-OUTPUT.schema.json"
      to: ".planning/phases/03-routing-rules/03-ROUTE-STRATEGY.md"
      via: "候选 rank 顺序输入"
      pattern: "rank|candidate_count_mode|low_confidence_mode"
    - from: ".planning/phases/03-routing-rules/03-ROUTE-STRATEGY.md"
      to: ".planning/phases/03-routing-rules/03-DURATION-HEURISTICS.md"
      via: "节点顺序映射到分段耗时"
      pattern: "segment|mode|duration_estimate"
---

<objective>
定义 Phase 3 的路线编排与分段耗时规则，确保后续输出可执行、可解释、可机读。

Purpose: 把 Phase 2 候选 POI 转成确定性的路线顺序与分段说明。
Output: `03-ROUTE-STRATEGY.md` 与 `03-DURATION-HEURISTICS.md`。
</objective>

<execution_context>
@/Users/franklin/.codex/get-shit-done/workflows/execute-plan.md
@/Users/franklin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-poi/02-POI-STRATEGY.md
@.planning/phases/02-poi/02-POI-OUTPUT.schema.json
@.planning/phases/02-poi/02-POI-OUTPUT.example.json
@.planning/phases/02-poi/02-01-SUMMARY.md
@.planning/phases/02-poi/02-02-SUMMARY.md
@AGENTS.md

<interfaces>
From 02-POI-OUTPUT.schema.json:
- 输入候选已经按 rank 排序，并带有 same_city_first 与 low_confidence_mode 元信息。
- 候选数量模式可能为 normal(3-5) 或 insufficient(2-3)。

From PROJECT/AGENTS constraints:
- 当前阶段不执行真实请求、不构造高德 URI、不输出二维码图片。
- 只定义路线编排规则与文本/机读输出契约，供后续阶段复用。
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 编写路线顺序编排主规范</name>
  <files>.planning/phases/03-routing-rules/03-ROUTE-STRATEGY.md</files>
  <read_first>
    - .planning/phases/03-routing-rules/03-ROUTE-STRATEGY.md
    - .planning/phases/02-poi/02-POI-STRATEGY.md
    - .planning/phases/02-poi/02-POI-OUTPUT.schema.json
    - .planning/phases/02-poi/02-POI-OUTPUT.example.json
    - .planning/REQUIREMENTS.md
    - AGENTS.md
  </read_first>
  <action>
    Create `03-ROUTE-STRATEGY.md` with fixed sections and concrete rules:
    (1) 输入约束: 接收按 `rank` 排序的 `poi_candidates[]`，并读取
    `candidate_count_mode`、`same_city_first`、`low_confidence_mode`;
    (2) 路线骨架: 强制 `origin -> stop_1..stop_n -> destination`，节点角色仅允许
    `origin|stop|destination`；(3) 节点选取: 默认 `origin` 来自用户输入起点，
    `destination` 来自用户输入终点，停靠点按 rank 依次填充，禁止逆序与跳号；
    (4) 缺失策略: 若 origin 缺失，写入 `assumptions` 条目 `A-origin-1`，
    使用 `same_city_first` 的第一候选所在城市中心作为假设起点；若 destination 缺失，
    写入 `A-destination-1`，使用最高 rank 候选作为临时终点，其余候选降为 stops；
    (5) 冲突处理: 若 origin 与 destination 解析到同一地点，必须保留至少一个 stop，
    并在 `route_notes` 标记 `same_point_conflict=true`；(6) 输出边界: 明确本阶段只输出
    顺序与分段说明，不生成 URI、不执行导航请求。附 ROUTE-01/02/03 决策追踪表。
  </action>
  <acceptance_criteria>
    - `rg -n "origin -> stop_1..stop_n -> destination|origin\|stop\|destination|A-origin-1|A-destination-1|same_point_conflict|不生成 URI|不执行导航请求" .planning/phases/03-routing-rules/03-ROUTE-STRATEGY.md` returns matches.
    - `rg -n "ROUTE-01|ROUTE-02|ROUTE-03|rank|candidate_count_mode|same_city_first|low_confidence_mode" .planning/phases/03-routing-rules/03-ROUTE-STRATEGY.md` returns matches.
    - `rg -n "逆序|跳号|缺失|假设起点|临时终点" .planning/phases/03-routing-rules/03-ROUTE-STRATEGY.md` returns matches.
  </acceptance_criteria>
  <verify>
    <automated>rg -n "origin -> stop_1..stop_n -> destination|origin\|stop\|destination|A-origin-1|A-destination-1|same_point_conflict|不生成 URI|不执行导航请求|ROUTE-01|ROUTE-02|ROUTE-03|rank|candidate_count_mode|same_city_first|low_confidence_mode|逆序|跳号|缺失|假设起点|临时终点" .planning/phases/03-routing-rules/03-ROUTE-STRATEGY.md</automated>
  </verify>
  <done>路线编排文档可直接指导执行者生成完整有序路线，并在缺失信息时稳定降级。</done>
</task>

<task type="auto">
  <name>Task 2: 编写分段交通方式与耗时估算规范</name>
  <files>.planning/phases/03-routing-rules/03-DURATION-HEURISTICS.md</files>
  <read_first>
    - .planning/phases/03-routing-rules/03-DURATION-HEURISTICS.md
    - .planning/phases/03-routing-rules/03-ROUTE-STRATEGY.md
    - .planning/phases/02-poi/02-POI-OUTPUT.schema.json
    - .planning/ROADMAP.md
    - .planning/REQUIREMENTS.md
  </read_first>
  <action>
    Create `03-DURATION-HEURISTICS.md` with deterministic segment rules:
    (1) 为每段输出字段固定为 `segment_index`, `from_node`, `to_node`, `mode`,
    `duration_estimate_min`, `duration_note`; (2) mode 选择矩阵:
    same-city 且估算距离 <=3km 使用 `walking`, >3km 且 <=15km 使用 `cycling`,
    >15km 或跨城使用 `driving`; (3) 耗时估算公式:
    walking 使用 5 km/h, cycling 使用 15 km/h, driving 使用 35 km/h,
    分钟值使用 `Math.ceil(distance_km / speed_kmh * 60)`;
    (4) 低置信处理: 当 `low_confidence_mode=true` 时，`duration_note` 必须包含
    `estimate_confidence=low` 并追加 `需要补充精确起终点`；
    (5) 多段汇总: 输出 `total_estimate_min` 为所有 segment 分钟和，
    并提供 `summary_text` 模板 `第{n}段 {mode} 约{min}分钟`。
    文档中显式说明这些是离线估算规则，不代表实时路况。
  </action>
  <acceptance_criteria>
    - `rg -n "segment_index|from_node|to_node|mode|duration_estimate_min|duration_note|total_estimate_min|summary_text" .planning/phases/03-routing-rules/03-DURATION-HEURISTICS.md` returns matches.
    - `rg -n "<=3km|>3km 且 <=15km|>15km|walking|cycling|driving|5 km/h|15 km/h|35 km/h|Math.ceil" .planning/phases/03-routing-rules/03-DURATION-HEURISTICS.md` returns matches.
    - `rg -n "estimate_confidence=low|需要补充精确起终点|离线估算|不代表实时路况|ROUTE-02" .planning/phases/03-routing-rules/03-DURATION-HEURISTICS.md` returns matches.
  </acceptance_criteria>
  <verify>
    <automated>rg -n "segment_index|from_node|to_node|mode|duration_estimate_min|duration_note|total_estimate_min|summary_text|<=3km|>3km 且 <=15km|>15km|walking|cycling|driving|5 km/h|15 km/h|35 km/h|Math.ceil|estimate_confidence=low|需要补充精确起终点|离线估算|不代表实时路况|ROUTE-02" .planning/phases/03-routing-rules/03-DURATION-HEURISTICS.md</automated>
  </verify>
  <done>分段方式与耗时估算规则可重复执行，并能在低置信场景保持一致输出。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| poi_candidates -> route_strategy | 上游候选顺序输入错误会直接影响路线正确性 |
| route_strategy -> duration_heuristics | 节点顺序与分段估算耦合，错误会级联到总耗时 |
| assumptions -> downstream_consumer | 假设说明缺失会导致用户误解路线有效性 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | T | route ordering | mitigate | 固定路线骨架与节点角色枚举，禁止逆序/跳号并做文本校验。 |
| T-03-02 | R | fallback assumptions | mitigate | 缺失起终点时强制写入 `A-origin-1`/`A-destination-1`，保证可追溯。 |
| T-03-03 | I | route notes | mitigate | 仅输出编排与估算，不包含链接、凭据、外部请求参数。 |
| T-03-04 | D | low-confidence duration | mitigate | 低置信时保留输出并标注 `estimate_confidence=low`，避免流程中断。 |
| T-03-05 | E | mode selection | accept | 本阶段仅文档规则，无特权执行；实时偏差在后续执行阶段复核。 |
</threat_model>

<verification>
1. 两个策略文档存在，且关键规则关键词可被 `rg` 命中。
2. ROUTE-01/02/03 在文档中有显式映射与执行规则。
3. 缺失起终点与低置信场景都有非阻断降级规范。
</verification>

<success_criteria>
1. ROUTE-01: 路线顺序规则可完整覆盖起点、途经点、终点。
2. ROUTE-02: 每段输出包含方式与分钟估算，且有可复用文案模板。
3. ROUTE-03: 起终点缺失场景有显式假设与继续生成策略。
</success_criteria>

<output>
After completion, create `.planning/phases/03-routing-rules/03-01-SUMMARY.md`
</output>
