---
phase: 02-routing-baseline-and-reliability
plan: 03
type: execute
wave: 3
depends_on:
  - 02-01
  - 02-02
files_modified:
  - src/routing/routing-orchestrator.service.ts
  - src/reliability/repro-metadata.service.ts
  - src/constraints/constraint-draft.repository.ts
  - src/constraints/constraint-draft.model.ts
  - src/shared/validation/constraint-draft.schema.ts
  - tests/routing/single-day-guard.test.ts
  - tests/reliability/route-metadata-hash.test.ts
autonomous: true
requirements:
  - ROUT-05
  - RELY-03
  - RELY-01
must_haves:
  truths:
    - "Single-day trip configuration always yields one route-day artifact with no stage split."
    - "Generated route artifacts include timestamp/provider/request hash/response hash metadata."
    - "Route generation persists metadata with session-scoped reproducibility diagnostics."
  artifacts:
    - path: "src/routing/routing-orchestrator.service.ts"
      provides: "Single-day route orchestration across geocode, segment build, and fallback handling."
      exports: ["RoutingOrchestratorService"]
    - path: "src/reliability/repro-metadata.service.ts"
      provides: "Deterministic request/response hashing and generation timestamp assembly."
      exports: ["buildRouteGenerationMetadata"]
    - path: "src/constraints/constraint-draft.model.ts"
      provides: "Canonical draft typing for persisted route generation metadata."
      contains: "routeGeneration"
    - path: "src/shared/validation/constraint-draft.schema.ts"
      provides: "Runtime schema enforcement for route generation metadata persistence."
      exports: ["safeParseConstraintDraft"]
    - path: "tests/routing/single-day-guard.test.ts"
      provides: "ROUT-05 regression tests for one-day behavior."
      contains: "ROUT-05"
    - path: "tests/reliability/route-metadata-hash.test.ts"
      provides: "RELY-03 metadata and hash determinism tests."
      contains: "RELY-03"
  key_links:
    - from: "src/routing/routing-orchestrator.service.ts"
      to: "src/reliability/repro-metadata.service.ts"
      via: "orchestrator appends reproducibility metadata to route artifacts."
      pattern: "buildRouteGenerationMetadata"
    - from: "src/routing/routing-orchestrator.service.ts"
      to: "src/constraints/constraint-draft.repository.ts"
      via: "session-scoped route generation metadata persistence."
      pattern: "updateDraft"
    - from: "src/constraints/constraint-draft.repository.ts"
      to: "src/shared/validation/constraint-draft.schema.ts"
      via: "persisted draft payload is validated with canonical schema before completion."
      pattern: "safeParseConstraintDraft"
---

<objective>
Implement single-day routing orchestration and reproducibility metadata persistence.

Purpose: Deliver ROUT-05 and RELY-03 while preserving RELY-01 fallback behavior through a single
orchestrator service.
Output: Routing orchestrator, metadata hashing service, repository persistence wiring, and tests.
</objective>

<execution_context>
@/Users/franklin/.codex/get-shit-done/workflows/execute-plan.md
@/Users/franklin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-routing-baseline-and-reliability/02-RESEARCH.md
@.planning/phases/02-routing-baseline-and-reliability/02-VALIDATION.md
@.planning/phases/02-routing-baseline-and-reliability/02-PATTERNS.md
@.planning/phases/02-routing-baseline-and-reliability/02-02-SUMMARY.md

<interfaces>
From `src/map-provider/map-provider.port.ts`:
```typescript
export interface MapProvider {
  geocodePoint(input: GeocodePointInput): Promise<GeocodePointResult>;
  routeBicyclingSegment(input: BicyclingSegmentInput): Promise<BicyclingSegmentResult>;
}
```

From `src/constraints/constraint-draft.repository.ts`:
```typescript
export interface ConstraintDraftRepository {
  getBySessionId(sessionId: string): Promise<ConstraintDraft | null>;
  updateDraft(
    sessionId: string,
    updater: (draft: ConstraintDraft) => ConstraintDraft
  ): Promise<ConstraintDraft>;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build single-day routing orchestrator with strict ROUT-05 guard</name>
  <read_first>
    - src/map-provider/map-provider.port.ts
    - src/routing/segment-routing.service.ts
    - src/conversation/slot-resolver.service.ts
    - tests/conversation/intake-confirmation.test.ts
    - src/constraints/constraint-draft.model.ts
    - .planning/phases/02-routing-baseline-and-reliability/02-PATTERNS.md
  </read_first>
  <files>src/routing/routing-orchestrator.service.ts, tests/routing/single-day-guard.test.ts</files>
  <behavior>
    - Test 1: `tripDays.normalized === 1` returns exactly one route-day artifact.
    - Test 2: Orchestrator stops and emits clarification state when geocode is ambiguous.
    - Test 3: Adapter fallback errors are propagated as deterministic RELY-01-safe messages.
  </behavior>
  <action>Implement `RoutingOrchestratorService` flow: load accepted points from canonical draft,
  geocode unresolved points, block on ambiguity, call ordered segment builder, and enforce
  single-day output shape with one `dayIndex: 1` block when `tripDays.normalized` equals `1`.
  Do not perform any waypoint reorder or multi-day stage split in this plan. Follow ordered-gate
  pipeline style from `src/conversation/slot-resolver.service.ts` and gate assertion style from
  `tests/conversation/intake-confirmation.test.ts`, per `02-PATTERNS.md`.</action>
  <acceptance_criteria>
    - `rg -n "tripDays\\.normalized\\s*===\\s*1|dayIndex:\\s*1|single-day" src/routing/routing-orchestrator.service.ts` confirms ROUT-05 guard.
    - `rg -n "ROUT-05|single-day|no stage split" tests/routing/single-day-guard.test.ts` confirms requirement coverage.
    - `pnpm vitest run tests/routing/single-day-guard.test.ts -t "ROUT-05"` passes.
  </acceptance_criteria>
  <verify>
    <automated>pnpm vitest run tests/routing/single-day-guard.test.ts -t "ROUT-05"</automated>
  </verify>
  <done>Single-day trips reliably produce one-day routing artifacts with ambiguity blocking intact.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add reproducibility metadata hashing service for RELY-03</name>
  <read_first>
    - src/shared/validation/route-artifact.schema.ts
    - src/constraints/constraint-patch.service.ts
    - tests/recap/canonical-projection.test.ts
    - .planning/phases/02-routing-baseline-and-reliability/02-RESEARCH.md
    - .planning/phases/02-routing-baseline-and-reliability/02-VALIDATION.md
    - .planning/phases/02-routing-baseline-and-reliability/02-PATTERNS.md
  </read_first>
  <files>src/reliability/repro-metadata.service.ts, tests/reliability/route-metadata-hash.test.ts</files>
  <behavior>
    - Test 1: Metadata includes `generatedAtIso`, provider identifier, request fingerprint, and response hash.
    - Test 2: Same request payload yields same request fingerprint across runs.
  </behavior>
  <action>Create `buildRouteGenerationMetadata({ provider, endpoint, requestPayload,
  responsePayload, infocode })` using `node:crypto` SHA-256 for `requestFingerprint` and
  `responseHash`, with `generatedAtIso` in offset ISO timestamp format. Add tests verifying required
  fields and deterministic request hash behavior for identical payloads. Keep generation explicit
  and append-oriented like `src/constraints/constraint-patch.service.ts`, and assert projection
  consistency using style from `tests/recap/canonical-projection.test.ts`, per `02-PATTERNS.md`.</action>
  <acceptance_criteria>
    - `rg -n "createHash|requestFingerprint|responseHash|generatedAtIso|provider|endpoint" src/reliability/repro-metadata.service.ts` confirms RELY-03 metadata fields.
    - `rg -n "RELY-03|requestFingerprint|responseHash|deterministic" tests/reliability/route-metadata-hash.test.ts` confirms requirement-tagged assertions.
    - `pnpm vitest run tests/reliability/route-metadata-hash.test.ts -t "RELY-03"` passes.
  </acceptance_criteria>
  <verify>
    <automated>pnpm vitest run tests/reliability/route-metadata-hash.test.ts -t "RELY-03"</automated>
  </verify>
  <done>Reproducibility metadata is deterministic, complete, and tested for RELY-03.</done>
</task>

<task type="auto">
  <name>Task 3: Persist route generation metadata by session in repository updates</name>
  <read_first>
    - src/routing/routing-orchestrator.service.ts
    - src/reliability/repro-metadata.service.ts
    - src/constraints/constraint-draft.repository.ts
    - src/constraints/constraint-draft.model.ts
    - src/shared/validation/constraint-draft.schema.ts
    - src/constraints/constraint-patch.service.ts
    - .planning/phases/02-routing-baseline-and-reliability/02-PATTERNS.md
  </read_first>
  <files>src/routing/routing-orchestrator.service.ts, src/constraints/constraint-draft.repository.ts, src/constraints/constraint-draft.model.ts, src/shared/validation/constraint-draft.schema.ts, tests/reliability/route-metadata-hash.test.ts</files>
  <action>Extend repository update flow so completed route generation writes metadata under a
  session-scoped route diagnostics field (for example `draft.routeGeneration` with latest metadata
  and a bounded history array). Update canonical contracts in both
  `src/constraints/constraint-draft.model.ts` and
  `src/shared/validation/constraint-draft.schema.ts` so `routeGeneration` is a typed, schema-valid
  field (not an untyped extension). Keep updates additive and non-destructive to existing slot and
  revision data, following append-style metadata evolution from
  `src/constraints/constraint-patch.service.ts`, per `02-PATTERNS.md`. Add roundtrip tests proving:
  repository write persists `routeGeneration`, subsequent `getBySessionId` returns it unchanged, and
  `safeParseConstraintDraft` succeeds for the reloaded payload.</action>
  <acceptance_criteria>
    - `rg -n "routeGeneration|requestFingerprint|responseHash|generatedAtIso|history|latest" src/constraints/constraint-draft.model.ts src/shared/validation/constraint-draft.schema.ts` confirms canonical contract updates.
    - `rg -n "routeGeneration|updateDraft|getBySessionId" src/constraints/constraint-draft.repository.ts src/routing/routing-orchestrator.service.ts` confirms persistence wiring.
    - `rg -n "safeParseConstraintDraft|roundtrip|getBySessionId|persist" tests/reliability/route-metadata-hash.test.ts` confirms contract roundtrip assertions.
    - `pnpm vitest run tests/reliability/route-metadata-hash.test.ts tests/routing/single-day-guard.test.ts` passes.
  </acceptance_criteria>
  <verify>
    <automated>pnpm vitest run tests/reliability/route-metadata-hash.test.ts tests/routing/single-day-guard.test.ts</automated>
  </verify>
  <done>Route generation metadata is stored session-scoped for reproducibility diagnostics.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| canonical draft -> orchestrator execution | Persisted user constraints drive external API and route generation flow. |
| route metadata -> repository persistence | Diagnostic metadata is stored and later used for operational analysis. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-09 | T | `src/routing/routing-orchestrator.service.ts` | mitigate | Enforce explicit single-day control path and block implicit stage-splitting behavior in Phase 2. |
| T-02-10 | R | `src/reliability/repro-metadata.service.ts` | mitigate | Store timestamp and request/response hashes to support auditability and drift diagnosis. |
| T-02-11 | E | `src/constraints/constraint-draft.repository.ts` | mitigate | Keep metadata writes session-scoped and merge-only to prevent cross-session overwrite. |
| T-02-12 | I | `tests/reliability/route-metadata-hash.test.ts` | mitigate | Assert no secrets are persisted in metadata payload beyond provider/endpoint/hash diagnostics. |
</threat_model>

<verification>
- `pnpm vitest run tests/routing/single-day-guard.test.ts`
- `pnpm vitest run tests/reliability/route-metadata-hash.test.ts`
</verification>

<success_criteria>
- ROUT-05 single-day guard is explicit and regression-tested.
- RELY-03 metadata hashing and persistence are implemented and deterministic.
- RELY-01 fallback behavior remains intact through orchestrator integration paths.
</success_criteria>

<output>
After completion, create `.planning/phases/02-routing-baseline-and-reliability/02-03-SUMMARY.md`
</output>
