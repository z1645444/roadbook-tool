---
phase: 02-routing-baseline-and-reliability
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - src/map-provider/amap-geocode.adapter.ts
  - src/map-provider/amap-bicycling.adapter.ts
  - src/map-provider/amap-error.mapper.ts
  - src/reliability/routing-fallback.error.ts
  - tests/reliability/amap-fallback-errors.test.ts
autonomous: true
requirements:
  - ROUT-01
  - ROUT-02
  - RELY-01
must_haves:
  truths:
    - "AMap geocode and bicycling calls are encapsulated behind adapters, not scattered in orchestration logic."
    - "AMap auth/quota/rate/provider errors map to deterministic user-facing fallback classes."
    - "Routing failures return safe fallback messages instead of raw provider internals."
  artifacts:
    - path: "src/map-provider/amap-geocode.adapter.ts"
      provides: "AMap geocode API adapter returning typed candidate lists."
      exports: ["AmapGeocodeAdapter"]
    - path: "src/map-provider/amap-bicycling.adapter.ts"
      provides: "AMap bicycling API adapter returning typed segment payload."
      exports: ["AmapBicyclingAdapter"]
    - path: "src/map-provider/amap-error.mapper.ts"
      provides: "infocode-to-domain fallback mapping for reliability."
      exports: ["mapAmapError"]
    - path: "src/reliability/routing-fallback.error.ts"
      provides: "Domain error classes with user-safe fallback message payload."
      exports: ["RoutingFallbackError"]
  key_links:
    - from: "src/map-provider/amap-geocode.adapter.ts"
      to: "src/map-provider/amap-error.mapper.ts"
      via: "adapter delegates provider failure classification."
      pattern: "mapAmapError"
    - from: "src/map-provider/amap-bicycling.adapter.ts"
      to: "src/reliability/routing-fallback.error.ts"
      via: "adapter throws classified fallback error on provider failure."
      pattern: "RoutingFallbackError"
---

<objective>
Implement AMap adapters and centralized fallback error mapping for routing reliability.

Purpose: Fulfill ROUT-01/ROUT-02 integration boundaries and RELY-01 fallback behavior without
leaking provider-specific internals.
Output: AMap geocode and bicycling adapters, shared infocode mapper, and reliability tests.
</objective>

<execution_context>
@/Users/franklin/.codex/get-shit-done/workflows/execute-plan.md
@/Users/franklin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-routing-baseline-and-reliability/02-RESEARCH.md
@.planning/phases/02-routing-baseline-and-reliability/02-VALIDATION.md
@.planning/phases/02-routing-baseline-and-reliability/02-PATTERNS.md
@.planning/phases/02-routing-baseline-and-reliability/02-01-SUMMARY.md

<interfaces>
From `src/map-provider/map-provider.port.ts` (from 02-01):
```typescript
export interface MapProvider {
  geocodePoint(input: GeocodePointInput): Promise<GeocodePointResult>;
  routeBicyclingSegment(input: BicyclingSegmentInput): Promise<BicyclingSegmentResult>;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement AMap geocode and bicycling adapters against provider port</name>
  <read_first>
    - src/map-provider/map-provider.port.ts
    - src/conversation/intake.controller.ts
    - src/constraints/constraint-draft.repository.ts
    - .planning/phases/02-routing-baseline-and-reliability/02-RESEARCH.md
    - .planning/phases/02-routing-baseline-and-reliability/02-PATTERNS.md
  </read_first>
  <files>src/map-provider/amap-geocode.adapter.ts, src/map-provider/amap-bicycling.adapter.ts</files>
  <behavior>
    - Test 1: Geocode adapter parses AMap `/v3/geocode/geo` response into typed candidates.
    - Test 2: Bicycling adapter parses `/v4/direction/bicycling` response into typed segment metrics.
  </behavior>
  <action>Create `AmapGeocodeAdapter` and `AmapBicyclingAdapter` implementing `MapProvider`
  contracts with `fetch`-based calls to AMap endpoints, pulling API key from environment only.
  Normalize provider responses into typed DTOs with required fields (`providerId`, coordinates,
  confidence, distance, duration) and include raw `infocode`/`info` only in internal error mapping
  path, not in user-facing output. Follow typed response boundary approach from
  `src/conversation/intake.controller.ts` and async adapter/repository method style from
  `src/constraints/constraint-draft.repository.ts`, per `02-PATTERNS.md`.</action>
  <acceptance_criteria>
    - `rg -n "class AmapGeocodeAdapter|/v3/geocode/geo|implements MapProvider" src/map-provider/amap-geocode.adapter.ts` confirms geocode adapter contract.
    - `rg -n "class AmapBicyclingAdapter|/v4/direction/bicycling|routeBicyclingSegment" src/map-provider/amap-bicycling.adapter.ts` confirms bicycling adapter contract.
    - `rg -n "process\\.env|AMAP" src/map-provider/amap-geocode.adapter.ts src/map-provider/amap-bicycling.adapter.ts` confirms env-key usage.
  </acceptance_criteria>
  <verify>
    <automated>pnpm vitest run tests/routing/geocode-disambiguation.test.ts tests/routing/bicycling-segments.test.ts</automated>
  </verify>
  <done>AMap integration is encapsulated in adapters implementing the shared provider interface.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add centralized AMap infocode mapper and typed fallback domain error</name>
  <read_first>
    - src/conversation/clarification-policy.service.ts
    - tests/constraints/waypoint-normalization.test.ts
    - .planning/phases/02-routing-baseline-and-reliability/02-RESEARCH.md
    - .planning/phases/02-routing-baseline-and-reliability/02-VALIDATION.md
    - .planning/phases/02-routing-baseline-and-reliability/02-PATTERNS.md
  </read_first>
  <files>src/map-provider/amap-error.mapper.ts, src/reliability/routing-fallback.error.ts, tests/reliability/amap-fallback-errors.test.ts</files>
  <behavior>
    - Test 1: Auth/quota/rate-limit infocodes map to deterministic fallback categories.
    - Test 2: Unknown provider errors map to a safe generic fallback category.
  </behavior>
  <action>Create `mapAmapError({ infocode, info, endpoint })` returning domain categories
  `auth_error`, `quota_error`, `rate_limited`, `provider_unavailable`, `unknown_provider_error`
  with user-facing Chinese fallback text that avoids secrets and stack traces. Add
  `RoutingFallbackError` carrying `{ category, userMessage, retryable }` and use it as the only
  outward error type from adapter failures. Keep mapper deterministic and pure following
  `src/conversation/clarification-policy.service.ts` branching style, and structure tests as
  branch-table assertions like `tests/constraints/waypoint-normalization.test.ts`, per
  `02-PATTERNS.md`.</action>
  <acceptance_criteria>
    - `rg -n "auth_error|quota_error|rate_limited|provider_unavailable|unknown_provider_error" src/map-provider/amap-error.mapper.ts` confirms mapping taxonomy.
    - `rg -n "class RoutingFallbackError|category|userMessage|retryable" src/reliability/routing-fallback.error.ts` confirms domain error contract.
    - `rg -n "RELY-01|infocode|fallback" tests/reliability/amap-fallback-errors.test.ts` confirms requirement-tagged fallback assertions.
    - `pnpm vitest run tests/reliability/amap-fallback-errors.test.ts -t "RELY-01"` passes.
  </acceptance_criteria>
  <verify>
    <automated>pnpm vitest run tests/reliability/amap-fallback-errors.test.ts -t "RELY-01"</automated>
  </verify>
  <done>Provider failures are consistently classified and rendered as safe fallback messages.</done>
</task>

<task type="auto">
  <name>Task 3: Wire adapters to fallback mapper and enforce non-leaky error surfaces</name>
  <read_first>
    - src/map-provider/amap-geocode.adapter.ts
    - src/map-provider/amap-bicycling.adapter.ts
    - src/map-provider/amap-error.mapper.ts
    - src/conversation/intake.controller.ts
    - .planning/phases/02-routing-baseline-and-reliability/02-PATTERNS.md
  </read_first>
  <files>src/map-provider/amap-geocode.adapter.ts, src/map-provider/amap-bicycling.adapter.ts, tests/reliability/amap-fallback-errors.test.ts</files>
  <action>Integrate `mapAmapError` into both adapters so any non-success AMap response or transport
  failure throws `RoutingFallbackError` only. Ensure thrown user messages do not include API key,
  full URL, raw response payload, or stack traces. Add tests asserting sanitized error messages and
  consistent category mapping across both adapters. Preserve request/response gate style similar to
  `src/conversation/intake.controller.ts` while keeping centralized mapping per
  `02-PATTERNS.md`.</action>
  <acceptance_criteria>
    - `rg -n "mapAmapError|RoutingFallbackError" src/map-provider/amap-geocode.adapter.ts src/map-provider/amap-bicycling.adapter.ts` confirms centralized error handling wiring.
    - `rg -n "should not contain|api key|stack|raw" tests/reliability/amap-fallback-errors.test.ts` confirms non-leak regression assertions.
    - `pnpm vitest run tests/reliability/amap-fallback-errors.test.ts` passes.
  </acceptance_criteria>
  <verify>
    <automated>pnpm vitest run tests/reliability/amap-fallback-errors.test.ts</automated>
  </verify>
  <done>All adapter failures return sanitized, deterministic fallback errors for RELY-01.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| external AMap API -> adapter layer | Untrusted third-party responses enter backend logic. |
| adapter errors -> chat response path | Internal provider errors are transformed into user-visible fallback text. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05 | I | `src/map-provider/amap-geocode.adapter.ts` | mitigate | Never include API key, raw provider payload, or full URL in thrown user-facing errors. |
| T-02-06 | D | `src/map-provider/amap-bicycling.adapter.ts` | mitigate | Map quota/rate-limit responses to retry-aware categories to avoid uncontrolled retry loops. |
| T-02-07 | T | `src/map-provider/amap-error.mapper.ts` | mitigate | Centralize infocode taxonomy in one mapper to avoid divergent per-endpoint behavior. |
| T-02-08 | R | `tests/reliability/amap-fallback-errors.test.ts` | mitigate | Maintain requirement-tagged tests proving fallback class and message consistency. |
</threat_model>

<verification>
- `pnpm vitest run tests/reliability/amap-fallback-errors.test.ts`
- `pnpm vitest run tests/routing/geocode-disambiguation.test.ts tests/routing/bicycling-segments.test.ts`
</verification>

<success_criteria>
- AMap geocode and bicycling integrations are adapterized behind shared provider contracts.
- RELY-01 fallback classes/messages are deterministic and sanitized.
- Routing integration no longer depends on ad-hoc provider error handling.
</success_criteria>

<output>
After completion, create `.planning/phases/02-routing-baseline-and-reliability/02-02-SUMMARY.md`
</output>
