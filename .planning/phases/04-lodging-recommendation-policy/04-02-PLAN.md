---
phase: 04-lodging-recommendation-policy
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - src/routing/lodging-policy.service.ts
  - tests/routing/lodging-policy.service.test.ts
autonomous: true
requirements:
  - LODG-02
  - LODG-03
  - LODG-04
  - LODG-05
must_haves:
  truths:
    - "Each lodging category enforces rating and price policy thresholds exactly."
    - "Fallback transitions are deterministic and auditable with stage trace."
    - "Policy output preserves fixed per-category quota and ranking order."
  artifacts:
    - path: "src/routing/lodging-policy.service.ts"
      provides: "Strict policy filter, ranking, fallback transitions, and no-match terminal behavior."
      exports: ["LodgingPolicyService"]
    - path: "tests/routing/lodging-policy.service.test.ts"
      provides: "Requirement coverage for strict filters and fallback strategy."
      contains: "LODG-02, LODG-03, LODG-04, LODG-05"
  key_links:
    - from: "src/routing/lodging-policy.service.ts"
      to: "src/map-provider/map-provider.port.ts"
      via: "policy service consumes provider lodging candidates and applies normalization/filtering."
      pattern: "searchLodgingAround"
    - from: "src/routing/lodging-policy.service.ts"
      to: "src/shared/validation/multiday-route-artifact.schema.ts"
      via: "policy output matches day-stage lodging schema contract."
      pattern: "policyStatus"
    - from: "tests/routing/lodging-policy.service.test.ts"
      to: "src/routing/lodging-policy.service.ts"
      via: "requirement-labeled tests lock strict and fallback behavior."
      pattern: "LODG-0"
---

<objective>
Implement the deterministic lodging policy engine that enforces strict category thresholds,
applies fixed fallback relaxation, and returns auditable day-level recommendation sets.

Purpose: Deliver LODG-02..LODG-05 with explicit D-13..D-26 behavior.
Output: Lodging policy service and requirement-labeled unit tests.
</objective>

<execution_context>
@/Users/franklin/.codex/get-shit-done/workflows/execute-plan.md
@/Users/franklin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-lodging-recommendation-policy/04-CONTEXT.md
@.planning/phases/04-lodging-recommendation-policy/04-RESEARCH.md
@.planning/phases/04-lodging-recommendation-policy/04-PATTERNS.md
@.planning/phases/04-lodging-recommendation-policy/04-01-PLAN.md
@.planning/phases/03-multi-day-optimization-and-stage-split/03-03-SUMMARY.md

<interfaces>
From src/map-provider/map-provider.port.ts (from Plan 04-01):
```typescript
export interface LodgingCandidate {
  providerId: string;
  name: string;
  type: LodgingCategory;
  distanceMeters: number;
  rating: number | null;
  priceCny: number | null;
  policyStage: LodgingPolicyStage;
}
```

From src/shared/validation/multiday-route-artifact.schema.ts:
```typescript
lodging: {
  policyStatus: 'compliant' | 'relaxed' | 'no_match';
  fallbackTrace: string[];
  categories: { hostel: LodgingEntry[]; guesthouse: LodgingEntry[]; hotel: LodgingEntry[] };
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add requirement-labeled tests for strict policy and fallback transitions</name>
  <read_first>
    - tests/routing/day-stage-split.test.ts
    - tests/routing/waypoint-optimization.test.ts
    - src/map-provider/map-provider.port.ts
    - .planning/phases/04-lodging-recommendation-policy/04-CONTEXT.md
    - .planning/phases/04-lodging-recommendation-policy/04-RESEARCH.md
  </read_first>
  <files>tests/routing/lodging-policy.service.test.ts</files>
  <behavior>
    - Failing tests are written before service implementation.
    - Tests cover strict stage, relaxed stage, and terminal no-match behavior.
  </behavior>
  <action>Create `tests/routing/lodging-policy.service.test.ts` with deterministic fixtures and
requirement-labeled cases:
`LODG-02 strict hostel filter`,
`LODG-03 strict guesthouse filter`,
`LODG-04 strict hotel filter`,
`LODG-05 fallback sequence`.
Assert concrete thresholds per D-15:
hostel `40 <= priceCny <= 100 && rating >= 4.0`,
guesthouse `priceCny <= 200 && rating >= 4.0`,
hotel `priceCny <= 200 && rating >= 4.0`.
Assert ranking order per D-16: distance asc, rating desc, price asc.
Assert fallback trace contains exact stage labels:
`strict_8km`, `radius_12km`, `radius_20km`, `price_relaxed_20`.
Assert terminal `policyStatus === 'no_match'` after final stage per D-22.</action>
  <acceptance_criteria>
    - `rg -n "LODG-02|LODG-03|LODG-04|LODG-05" tests/routing/lodging-policy.service.test.ts` returns all IDs.
    - `rg -n "strict_8km|radius_12km|radius_20km|price_relaxed_20|no_match" tests/routing/lodging-policy.service.test.ts` returns assertions.
    - `pnpm vitest run tests/routing/lodging-policy.service.test.ts` initially fails before implementation and passes after Task 2.
  </acceptance_criteria>
  <verify>
    <automated>pnpm vitest run tests/routing/lodging-policy.service.test.ts</automated>
  </verify>
  <done>Policy tests encode exact thresholds, fallback stages, and deterministic ranking behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement lodging policy engine with serial retrieval, cache, and fixed fallback</name>
  <read_first>
    - src/routing/day-stage-splitter.service.ts
    - src/routing/routing-orchestrator.service.ts
    - src/map-provider/map-provider.port.ts
    - src/shared/validation/multiday-route-artifact.schema.ts
    - tests/routing/lodging-policy.service.test.ts
    - .planning/phases/04-lodging-recommendation-policy/04-CONTEXT.md
  </read_first>
  <files>src/routing/lodging-policy.service.ts, tests/routing/lodging-policy.service.test.ts</files>
  <behavior>
    - Retrieval is anchored only on overnight stop points and category order is serial.
    - Output preserves fixed top-3 quota per category with no cross-category slot sharing.
  </behavior>
  <action>Create `LodgingPolicyService` with concrete policy constants:
`BASE_RADIUS_METERS=8000`, `RADIUS_STEPS=[12000,20000]`,
`PRICE_RELAX_FACTOR=1.2`, `RATING_FLOOR=4.0`,
`CACHE_TTL_MS=600000`, `MAX_PER_CATEGORY=3`.
Apply decisions directly:
D-01/D-02 use only `overnightStopPoint` anchors and skip missing anchors;
D-07 serial categories `[hostel, guesthouse, hotel]`;
D-08 dedupe by `providerId`;
D-12 cache key format `lodging:{sessionId}:{dayIndex}:{anchor.providerId}:{category}:{radius}:{stage}`
with in-memory `Map` TTL entries;
D-13 normalize `priceCny` to nightly CNY number (drop non-finite values);
D-14/D-15 strict category thresholds;
D-16 rank comparator `distance asc -> rating desc -> price asc`;
D-06 apply per-day per-category post-filter cap with `.slice(0,3)` and no overflow carry;
D-17 fixed category quotas with no cross-category slot stealing;
D-18/D-19 fallback stage order strict8 -> r12 -> r20;
D-20 price-relaxed cap `cap*1.2` while hostel min remains `40`;
D-21 keep rating floor at `4.0` in all stages;
D-22 terminal no-match status;
D-23/D-24/D-25/D-26 emit day-grouped output with fields
`name,type,distanceMeters,rating,priceCny,policyStage` and full `fallbackTrace`.</action>
  <acceptance_criteria>
    - `rg -n "BASE_RADIUS_METERS|RADIUS_STEPS|PRICE_RELAX_FACTOR|CACHE_TTL_MS" src/routing/lodging-policy.service.ts` returns matches.
    - `rg -n "hostel|guesthouse|hotel|distance.*rating.*price|slice\\(0, 3\\)" src/routing/lodging-policy.service.ts` returns policy and ranking logic.
    - `rg -n "strict_8km|radius_12km|radius_20km|price_relaxed_20|no_match" src/routing/lodging-policy.service.ts` returns stage/status constants.
    - `pnpm vitest run tests/routing/lodging-policy.service.test.ts` passes.
  </acceptance_criteria>
  <verify>
    <automated>pnpm vitest run tests/routing/lodging-policy.service.test.ts</automated>
  </verify>
  <done>Policy engine returns deterministic strict/relaxed/no-match day payloads that satisfy LODG-02..05.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Provider candidate DTOs -> policy engine | Untrusted or malformed rating/price data enters filtering/ranking logic. |
| Cached session data -> recommendation output | Session-scoped cached entries can leak across sessions if keys are weak. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-05 | T | `src/routing/lodging-policy.service.ts` | mitigate | Normalize and validate numeric rating/price before policy checks; discard invalid candidates. |
| T-04-06 | I | `src/routing/lodging-policy.service.ts` | mitigate | Enforce exact category thresholds and never relax rating floor below 4.0. |
| T-04-07 | E | `src/routing/lodging-policy.service.ts` | mitigate | Session/day/anchor/category scoped cache keys prevent cross-session recommendation reuse. |
| T-04-08 | R | `tests/routing/lodging-policy.service.test.ts` | mitigate | Requirement-tagged unit tests verify each fallback stage and no-match terminal status. |
</threat_model>

<verification>
- `pnpm vitest run tests/routing/lodging-policy.service.test.ts`
</verification>

<success_criteria>
- Policy service strictly enforces LODG-02/03/04 thresholds with deterministic ranking and quota.
- Fallback strategy exactly follows D-18..D-22 and emits auditable trace.
- Day-level payload is schema-compatible and ready for orchestrator wiring.
</success_criteria>

<output>
After completion, create `.planning/phases/04-lodging-recommendation-policy/04-02-SUMMARY.md`
</output>
