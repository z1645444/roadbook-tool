---
phase: 06-output-contract-reset
plan: "02"
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - .planning/phases/06-output-contract-reset/06-OUTPUT-v1.1.schema.json
  - .planning/phases/06-output-contract-reset/06-OUTPUT-v1.1.example.json
  - .planning/phases/06-output-contract-reset/06-OUTPUT-v1.1-CHECKLIST.md
autonomous: true
requirements:
  - OC-01
  - OC-02
must_haves:
  truths:
    - "v1.1 schema 顶层 required 仅包含 route_summary/address/eta/notes。"
    - "v1.1 主示例必须覆盖 normal/degraded 两种场景且不含二维码主字段。"
    - "验证清单必须可脚本化判定四字段存在与二维码字段缺失。"
    - "输出资产要支持后续阶段做兼容层映射，不提前回引二维码主契约。"
  artifacts:
    - path: ".planning/phases/06-output-contract-reset/06-OUTPUT-v1.1.schema.json"
      provides: "v1.1 主契约 JSON Schema"
      contains: "顶层字段白名单、required、additionalProperties 约束"
    - path: ".planning/phases/06-output-contract-reset/06-OUTPUT-v1.1.example.json"
      provides: "v1.1 normal/degraded 示例"
      contains: "四字段主结构、无二维码主字段"
    - path: ".planning/phases/06-output-contract-reset/06-OUTPUT-v1.1-CHECKLIST.md"
      provides: "v1.1 契约验证清单"
      contains: "字段存在性检查、禁用字段检查、需求追踪"
  key_links:
    - from: ".planning/phases/06-output-contract-reset/06-OUTPUT-v1.1.schema.json"
      to: ".planning/phases/06-output-contract-reset/06-OUTPUT-CONTRACT-v1.1.md"
      via: "schema 与文档主契约字段保持 1:1 对齐"
      pattern: "route_summary|address|eta|notes"
    - from: ".planning/phases/06-output-contract-reset/06-OUTPUT-v1.1.example.json"
      to: ".planning/phases/06-output-contract-reset/06-OUTPUT-v1.1.schema.json"
      via: "示例必须可被 schema 校验且不含二维码主字段"
      pattern: "normal|degraded"
---

<objective>
把 Phase 6 主契约规范落地为可自动验证资产：schema、example、checklist。

Purpose: 让 OC-01/OC-02 在执行层可机械校验，而不是停留在文档描述。
Output: `06-OUTPUT-v1.1.schema.json`、`06-OUTPUT-v1.1.example.json`、`06-OUTPUT-v1.1-CHECKLIST.md`。
</objective>

<execution_context>
@/Users/franklin/.codex/get-shit-done/workflows/execute-plan.md
@/Users/franklin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06-output-contract-reset/06-01-PLAN.md
@.planning/phases/06-output-contract-reset/06-OUTPUT-CONTRACT-v1.1.md
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 创建 v1.1 主契约 schema 并禁止二维码主字段</name>
  <files>.planning/phases/06-output-contract-reset/06-OUTPUT-v1.1.schema.json</files>
  <read_first>
    - .planning/phases/06-output-contract-reset/06-OUTPUT-v1.1.schema.json
    - .planning/phases/06-output-contract-reset/06-OUTPUT-CONTRACT-v1.1.md
    - .planning/phases/05-address-and-qr-output-protocol/05-OUTPUT.schema.json
    - .planning/REQUIREMENTS.md
  </read_first>
  <action>
    创建 `06-OUTPUT-v1.1.schema.json`（JSON Schema draft 2020-12）。
    顶层 `type=object`，`required` 固定为 `route_summary`, `address`, `eta`, `notes`，
    且 `additionalProperties=false`。分别为四字段定义最小可用结构，并在说明字段中写明
    v1.1 主契约不接收兼容层以外字段。主 schema 中不得出现任何 `qr*` 字段名（包括注释、
    `$comment`、示例片段），二维码迁移说明只允许写在 checklist 中。
  </action>
  <acceptance_criteria>
    - `node -e "JSON.parse(require('fs').readFileSync('.planning/phases/06-output-contract-reset/06-OUTPUT-v1.1.schema.json','utf8')); console.log('schema-json-ok')"` 输出 `schema-json-ok`。
    - `rg -n '"required"|"route_summary"|"address"|"eta"|"notes"|"additionalProperties"' .planning/phases/06-output-contract-reset/06-OUTPUT-v1.1.schema.json` 命中。
    - `! rg -n '"qr"|"qr_status"|"qr_payload_text"|"qr_image"|"ascii_qr"' .planning/phases/06-output-contract-reset/06-OUTPUT-v1.1.schema.json` 成立。
  </acceptance_criteria>
  <verify>
    <automated>node -e "JSON.parse(require('fs').readFileSync('.planning/phases/06-output-contract-reset/06-OUTPUT-v1.1.schema.json','utf8')); console.log('schema-json-ok')" && rg -n '"required"|"route_summary"|"address"|"eta"|"notes"|"additionalProperties"' .planning/phases/06-output-contract-reset/06-OUTPUT-v1.1.schema.json && ! rg -n '"qr"|"qr_status"|"qr_payload_text"|"qr_image"|"ascii_qr"' .planning/phases/06-output-contract-reset/06-OUTPUT-v1.1.schema.json</automated>
  </verify>
  <done>v1.1 schema 明确了四字段主契约边界并阻止二维码主字段回流。</done>
</task>

<task type="auto">
  <name>Task 2: 创建 v1.1 示例并校验 normal/degraded 双场景</name>
  <files>.planning/phases/06-output-contract-reset/06-OUTPUT-v1.1.example.json</files>
  <read_first>
    - .planning/phases/06-output-contract-reset/06-OUTPUT-v1.1.example.json
    - .planning/phases/06-output-contract-reset/06-OUTPUT-v1.1.schema.json
    - .planning/phases/06-output-contract-reset/06-OUTPUT-TEMPLATE-v1.1.md
    - .planning/REQUIREMENTS.md
  </read_first>
  <action>
    创建 `06-OUTPUT-v1.1.example.json`，包含 `normal_case` 与 `degraded_case` 两个对象。
    两个 case 顶层都只能出现四字段：`route_summary`, `address`, `eta`, `notes`。
    `normal_case` 展示标准输出；`degraded_case` 在 `notes` 中说明降级原因（例如输入缺失或
    能力受限），但仍保持四字段完整。示例禁止任何二维码主字段。
  </action>
  <acceptance_criteria>
    - `node -e "JSON.parse(require('fs').readFileSync('.planning/phases/06-output-contract-reset/06-OUTPUT-v1.1.example.json','utf8')); console.log('example-json-ok')"` 输出 `example-json-ok`。
    - `rg -n '"normal_case"|"degraded_case"|"route_summary"|"address"|"eta"|"notes"' .planning/phases/06-output-contract-reset/06-OUTPUT-v1.1.example.json` 命中。
    - `node -e "try{require.resolve('ajv/dist/2020'); console.log('ajv-available')}catch(e){console.error('ajv-missing'); process.exit(1)}"` 输出 `ajv-available`。
    - `node -e "const Ajv=require('ajv/dist/2020'); const fs=require('fs'); const base='.planning/phases/06-output-contract-reset'; const schema=JSON.parse(fs.readFileSync(base+'/06-OUTPUT-v1.1.schema.json','utf8')); const data=JSON.parse(fs.readFileSync(base+'/06-OUTPUT-v1.1.example.json','utf8')); const ajv=new Ajv({allErrors:true,strict:false}); const validate=ajv.compile(schema); for (const k of ['normal_case','degraded_case']) { if (!validate(data[k])) { console.error(k, validate.errors); process.exit(1);} } console.log('schema-validate-ok');"` 输出 `schema-validate-ok`。
    - `! rg -n '"qr"|"qr_status"|"qr_payload_text"|"qr_image"|"ascii_qr"' .planning/phases/06-output-contract-reset/06-OUTPUT-v1.1.example.json` 成立。
  </acceptance_criteria>
  <verify>
    <automated>node -e "JSON.parse(require('fs').readFileSync('.planning/phases/06-output-contract-reset/06-OUTPUT-v1.1.example.json','utf8')); console.log('example-json-ok')" && rg -n '"normal_case"|"degraded_case"|"route_summary"|"address"|"eta"|"notes"' .planning/phases/06-output-contract-reset/06-OUTPUT-v1.1.example.json && node -e "try{require.resolve('ajv/dist/2020'); console.log('ajv-available')}catch(e){console.error('ajv-missing'); process.exit(1)}" && node -e "const Ajv=require('ajv/dist/2020'); const fs=require('fs'); const base='.planning/phases/06-output-contract-reset'; const schema=JSON.parse(fs.readFileSync(base+'/06-OUTPUT-v1.1.schema.json','utf8')); const data=JSON.parse(fs.readFileSync(base+'/06-OUTPUT-v1.1.example.json','utf8')); const ajv=new Ajv({allErrors:true,strict:false}); const validate=ajv.compile(schema); for (const k of ['normal_case','degraded_case']) { if (!validate(data[k])) { console.error(k, validate.errors); process.exit(1);} } console.log('schema-validate-ok');" && ! rg -n '"qr"|"qr_status"|"qr_payload_text"|"qr_image"|"ascii_qr"' .planning/phases/06-output-contract-reset/06-OUTPUT-v1.1.example.json</automated>
  </verify>
  <done>示例证明 v1.1 在 normal/degraded 两种路径下都保持四字段主契约。</done>
</task>

<task type="auto">
  <name>Task 3: 创建 v1.1 检查清单并映射 OC-01/OC-02</name>
  <files>.planning/phases/06-output-contract-reset/06-OUTPUT-v1.1-CHECKLIST.md</files>
  <read_first>
    - .planning/phases/06-output-contract-reset/06-OUTPUT-v1.1-CHECKLIST.md
    - .planning/phases/06-output-contract-reset/06-OUTPUT-v1.1.schema.json
    - .planning/phases/06-output-contract-reset/06-OUTPUT-v1.1.example.json
    - .planning/REQUIREMENTS.md
  </read_first>
  <action>
    创建 `06-OUTPUT-v1.1-CHECKLIST.md`，表格列固定为：
    `check_id`, `rule`, `input`, `verify_command`, `pass_condition`, `mapped_requirement`。
    至少提供四项：
    `CHK-06-01` 四字段 required 校验；
    `CHK-06-02` fixed order 文档命中；
    `CHK-06-03` 示例禁止二维码字段；
    `CHK-06-04` normal/degraded 均可通过 schema 校验。
    每项附可执行命令（`rg` 或 `node -e`），并映射到 OC-01/OC-02。
  </action>
  <acceptance_criteria>
    - `rg -n 'CHK-06-01|CHK-06-02|CHK-06-03|CHK-06-04|check_id|verify_command|pass_condition|mapped_requirement|OC-01|OC-02' .planning/phases/06-output-contract-reset/06-OUTPUT-v1.1-CHECKLIST.md` 命中。
    - `rg -n 'route_summary|address|eta|notes|qr_status|qr_payload_text|schema-validate-ok' .planning/phases/06-output-contract-reset/06-OUTPUT-v1.1-CHECKLIST.md` 命中。
  </acceptance_criteria>
  <verify>
    <automated>rg -n 'CHK-06-01|CHK-06-02|CHK-06-03|CHK-06-04|check_id|verify_command|pass_condition|mapped_requirement|OC-01|OC-02|route_summary|address|eta|notes|qr_status|qr_payload_text|schema-validate-ok' .planning/phases/06-output-contract-reset/06-OUTPUT-v1.1-CHECKLIST.md</automated>
  </verify>
  <done>v1.1 验证清单可直接被执行/验证阶段复用，自动判定契约是否偏移。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| contract_doc -> schema | 文档到 schema 映射可能漏掉字段白名单或禁用字段声明 |
| schema -> examples | 示例若偏离 schema，会导致验证假阳性 |
| examples -> checklist | 清单命令若不可执行，会让覆盖率看起来达标但无法复核 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-06 | T | schema required fields | mitigate | 顶层 required 固定四字段，且 additionalProperties=false。 |
| T-06-07 | R | requirement trace | mitigate | 每个检查项绑定 OC-01/OC-02 并含可执行命令。 |
| T-06-08 | D | degraded path drift | mitigate | normal/degraded 双样例都走 schema 校验。 |
| T-06-09 | I | legacy field leakage | mitigate | 清单中加入二维码字段禁用检查命令。 |
| T-06-10 | E | premature compatibility mixing | mitigate | 明确兼容层推迟到 Phase 7，当前资产不回引二维码主字段。 |
</threat_model>

<verification>
1. schema/example/checklist 三件套创建完成。
2. normal/degraded 示例均可通过 schema 校验。
3. 禁用二维码字段检查项可执行且可判定。
</verification>

<success_criteria>
1. 主契约字段在 schema/example 中唯一且固定顺序。
2. QR 相关字段不再出现在 v1.1 主 schema 与主示例中。
</success_criteria>

<output>
After completion, create `.planning/phases/06-output-contract-reset/06-02-SUMMARY.md`
</output>
