---
phase: 01-input-contract-missing-assumptions
plan: "02"
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - .planning/phases/01-input-contract-missing-assumptions/01-NORMALIZED-OUTPUT.schema.json
  - .planning/phases/01-input-contract-missing-assumptions/01-NORMALIZED-OUTPUT.example.json
autonomous: true
requirements:
  - INT-01
  - INT-02
  - INT-03
must_haves:
  truths:
    - "用户能得到固定键顺序的机读归一化输出，供下游阶段直接消费。"
    - "输出中明确包含 assumptions 与 candidates，不丢失缺失声明和消歧信息。"
    - "自然语言优先展示与机读补充可同时成立。"
  artifacts:
    - path: ".planning/phases/01-input-contract-missing-assumptions/01-NORMALIZED-OUTPUT.schema.json"
      provides: "归一化输出 JSON Schema（含 required、类型、约束）"
      contains: "trip_intent/assumptions/candidates/output_views"
    - path: ".planning/phases/01-input-contract-missing-assumptions/01-NORMALIZED-OUTPUT.example.json"
      provides: "符合 schema 的示例输出"
      contains: "缺失处理、冲突提示、跨城标记示例"
  key_links:
    - from: ".planning/phases/01-input-contract-missing-assumptions/01-NORMALIZED-OUTPUT.example.json"
      to: ".planning/phases/01-input-contract-missing-assumptions/01-NORMALIZED-OUTPUT.schema.json"
      via: "schema validation"
      pattern: "required"
    - from: ".planning/phases/01-input-contract-missing-assumptions/01-NORMALIZED-OUTPUT.schema.json"
      to: ".planning/phases/01-input-contract-missing-assumptions/01-INPUT-CONTRACT.md"
      via: "字段定义与优先级规则一致"
      pattern: "structured_input"
---

<objective>
将输入契约转成可验证的 JSON schema + example，确保下游阶段以同一数据接口消费。

Purpose: 把文档规则落成可检验工件，避免后续阶段字段漂移。
Output: `01-NORMALIZED-OUTPUT.schema.json` 与 `01-NORMALIZED-OUTPUT.example.json`。
</objective>

<execution_context>
@/Users/franklin/.codex/get-shit-done/workflows/execute-plan.md
@/Users/franklin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-input-contract-missing-assumptions/01-CONTEXT.md
@.planning/phases/01-input-contract-missing-assumptions/01-PATTERNS.md
@.planning/phases/01-input-contract-missing-assumptions/01-01-SUMMARY.md

<interfaces>
From .planning/phases/01-input-contract-missing-assumptions/01-INPUT-CONTRACT.md (to be created in plan 01-01):
- Input channels: `natural_language`, `structured_input`
- Precedence: `structured_input > natural_language`
- Required outputs: `assumptions[]` (numbered), `candidates[]`, and dual view `human + machine-readable`
- Missing defaults: mode `cycling`, time budget `unbounded`, explicit declaration for missing origin/destination
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 定义归一化输出 JSON Schema</name>
  <files>.planning/phases/01-input-contract-missing-assumptions/01-NORMALIZED-OUTPUT.schema.json</files>
  <read_first>
    - .planning/phases/01-input-contract-missing-assumptions/01-NORMALIZED-OUTPUT.schema.json
    - .planning/phases/01-input-contract-missing-assumptions/01-INPUT-CONTRACT.md
    - .planning/phases/01-input-contract-missing-assumptions/01-CONTEXT.md
    - .planning/phases/01-input-contract-missing-assumptions/01-PATTERNS.md
  </read_first>
  <action>
    Create JSON Schema (draft 2020-12) with top-level required keys in fixed order:
    `trip_intent`, `assumptions`, `candidates`, `output_views`, `metadata`. Enforce: `trip_intent.transport_mode`
    enum includes `cycling` and default `cycling` (D-05), `trip_intent.time_budget` allows explicit
    `unbounded` sentinel (D-06), `assumptions` is non-empty array of objects containing `id` pattern `^A[0-9]+$`
    and `statement` (D-13), `candidates` array items include `city`, `reason`, `rank`, and `selected` (D-09,
    D-14), `metadata.cross_city` boolean with required note when true (D-10), location conflict payload
    `location_conflict.coordinate_wins` boolean (D-11). Include a schema section that captures dual views:
    `output_views.human_primary` and `output_views.machine_readable_appendix` (D-15). Follow
    `.planning/phases/01-input-contract-missing-assumptions/01-PATTERNS.md` mapping for
    `.planning/config.json` "JSON object convention pattern": stable top-level keys, grouped
    nested objects, and explicit scalar types for machine consumption.
  </action>
  <acceptance_criteria>
    - `rg -n "\"trip_intent\"|\"assumptions\"|\"candidates\"|\"output_views\"|\"metadata\"" .planning/phases/01-input-contract-missing-assumptions/01-NORMALIZED-OUTPUT.schema.json` returns all keys.
    - `rg -n "\"cycling\"|\"unbounded\"|\"\\^A\\[0-9\\]\\+\\$\"|\"cross_city\"|\"coordinate_wins\"|\"human_primary\"|\"machine_readable_appendix\"" .planning/phases/01-input-contract-missing-assumptions/01-NORMALIZED-OUTPUT.schema.json` returns matches.
    - `node -e "JSON.parse(require('fs').readFileSync('.planning/phases/01-input-contract-missing-assumptions/01-NORMALIZED-OUTPUT.schema.json','utf8')); console.log('schema-json-ok')"` prints `schema-json-ok`.
  </acceptance_criteria>
  <verify>
    <automated>node -e "JSON.parse(require('fs').readFileSync('.planning/phases/01-input-contract-missing-assumptions/01-NORMALIZED-OUTPUT.schema.json','utf8')); console.log('schema-json-ok')" && rg -n "\"trip_intent\"|\"assumptions\"|\"candidates\"|\"output_views\"|\"metadata\"|\"cycling\"|\"unbounded\"|\"\\^A\\[0-9\\]\\+\\$\"|\"cross_city\"|\"coordinate_wins\"|\"human_primary\"|\"machine_readable_appendix\"" .planning/phases/01-input-contract-missing-assumptions/01-NORMALIZED-OUTPUT.schema.json</automated>
  </verify>
  <done>Schema 能约束 Phase 1 归一化输出字段、默认值、编号规范和双视图结构。</done>
</task>

<task type="auto">
  <name>Task 2: 编写与 schema 对齐的 example JSON</name>
  <files>.planning/phases/01-input-contract-missing-assumptions/01-NORMALIZED-OUTPUT.example.json</files>
  <read_first>
    - .planning/phases/01-input-contract-missing-assumptions/01-NORMALIZED-OUTPUT.example.json
    - .planning/phases/01-input-contract-missing-assumptions/01-NORMALIZED-OUTPUT.schema.json
    - .planning/phases/01-input-contract-missing-assumptions/01-ASSUMPTIONS-TEMPLATE.md
    - .planning/phases/01-input-contract-missing-assumptions/01-INPUT-CONTRACT.md
  </read_first>
  <action>
    Create one canonical example JSON matching the schema and demonstrating concrete Phase 1 decisions:
    include dual-channel source input, structured-over-natural precedence result, missing destination
    declared in assumptions, transport mode defaulted to `cycling`, time budget marked `unbounded`,
    ambiguity candidates with same-city-first ordering, one coordinate-vs-text conflict with
    `coordinate_wins: true`, and one cross-city sample with `cross_city: true` plus uncertainty note.
    Ensure assumptions use `A1`, `A2`, `A3` IDs and `output_views` includes human-primary paragraph plus
    machine-readable appendix object. Follow
    `.planning/phases/01-input-contract-missing-assumptions/01-PATTERNS.md` mapping for
    `.planning/ROADMAP.md` "Ordered structure pattern": keep deterministic top-level key order to satisfy
    D-12 stable multi-agent consumption.
  </action>
  <acceptance_criteria>
    - `node -e "JSON.parse(require('fs').readFileSync('.planning/phases/01-input-contract-missing-assumptions/01-NORMALIZED-OUTPUT.example.json','utf8')); console.log('example-json-ok')"` prints `example-json-ok`.
    - `node -e "const fs=require('fs'); const p='.planning/phases/01-input-contract-missing-assumptions/01-NORMALIZED-OUTPUT.example.json'; const o=JSON.parse(fs.readFileSync(p,'utf8')); const expected=['trip_intent','assumptions','candidates','output_views','metadata']; const keys=Object.keys(o); if (JSON.stringify(keys)!==JSON.stringify(expected)) { console.error('key-order-mismatch', keys); process.exit(1);} console.log('key-order-ok');"` prints `key-order-ok`.
    - `rg -n "\"A1\"|\"A2\"|\"A3\"|\"cycling\"|\"unbounded\"|\"coordinate_wins\"|\"cross_city\"|\"human_primary\"|\"machine_readable_appendix\"" .planning/phases/01-input-contract-missing-assumptions/01-NORMALIZED-OUTPUT.example.json` returns matches.
    - `rg -n "\"candidates\"|\"selected\"|\"same_city_first\"" .planning/phases/01-input-contract-missing-assumptions/01-NORMALIZED-OUTPUT.example.json` returns matches.
    - `node -e "const Ajv = require('ajv/dist/2020'); const fs = require('fs'); const base='.planning/phases/01-input-contract-missing-assumptions'; const schema=JSON.parse(fs.readFileSync(base+'/01-NORMALIZED-OUTPUT.schema.json','utf8')); const example=JSON.parse(fs.readFileSync(base+'/01-NORMALIZED-OUTPUT.example.json','utf8')); const ajv=new Ajv({allErrors:true,strict:false}); const validate=ajv.compile(schema); if(!validate(example)){ console.error(validate.errors); process.exit(1);} console.log('schema-validate-ok');"` prints `schema-validate-ok`.
  </acceptance_criteria>
  <verify>
    <automated>node -e "JSON.parse(require('fs').readFileSync('.planning/phases/01-input-contract-missing-assumptions/01-NORMALIZED-OUTPUT.example.json','utf8')); console.log('example-json-ok')" && node -e "const fs=require('fs'); const p='.planning/phases/01-input-contract-missing-assumptions/01-NORMALIZED-OUTPUT.example.json'; const o=JSON.parse(fs.readFileSync(p,'utf8')); const expected=['trip_intent','assumptions','candidates','output_views','metadata']; const keys=Object.keys(o); if (JSON.stringify(keys)!==JSON.stringify(expected)) { console.error('key-order-mismatch', keys); process.exit(1);} console.log('key-order-ok');" && node -e "const Ajv = require('ajv/dist/2020'); const fs = require('fs'); const base='.planning/phases/01-input-contract-missing-assumptions'; const schema=JSON.parse(fs.readFileSync(base+'/01-NORMALIZED-OUTPUT.schema.json','utf8')); const example=JSON.parse(fs.readFileSync(base+'/01-NORMALIZED-OUTPUT.example.json','utf8')); const ajv=new Ajv({allErrors:true,strict:false}); const validate=ajv.compile(schema); if(!validate(example)){ console.error(validate.errors); process.exit(1);} console.log('schema-validate-ok');" && rg -n "\"A1\"|\"A2\"|\"A3\"|\"cycling\"|\"unbounded\"|\"coordinate_wins\"|\"cross_city\"|\"human_primary\"|\"machine_readable_appendix\"|\"candidates\"|\"selected\"|\"same_city_first\"" .planning/phases/01-input-contract-missing-assumptions/01-NORMALIZED-OUTPUT.example.json</automated>
  </verify>
  <done>Example JSON 可作为下游接口样例，且完整体现 D-01~D-15 的关键行为；example 必须通过 schema 校验并满足 D-12 固定顶层键顺序。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| contract_doc -> schema | 文档规则转成机读约束时可能发生遗漏或弱化 |
| schema -> example | 示例可能偏离 schema 导致误导下游实现 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-06 | T | schema constraints | mitigate | 将关键决策（D-05/D-06/D-13/D-14/D-15）编码为 required/enum/pattern，防止被篡改为可选。 |
| T-01-07 | R | assumptions IDs | mitigate | `assumptions.id` 使用 `^A[0-9]+$` 并在 example 固定 `A1..A3`，保持可追溯。 |
| T-01-08 | I | example payload | mitigate | 示例中仅包含地点与偏好示例字段，不出现 token、secret、用户身份标识。 |
| T-01-09 | D | downstream parser | mitigate | 通过 JSON parse 校验和 key presence grep，防止格式损坏导致下游解析失败。 |
| T-01-10 | E | cross-phase data use | accept | 本阶段不执行权限动作，主要风险为契约误读；通过 plan 01-01 摘要依赖和 schema 强约束降低风险。 |
</threat_model>

<verification>
1. schema 与 example 均可 `JSON.parse`。
2. schema 包含关键 required/enum/pattern 字段。
3. example 明确展示 missing、conflict、cross-city、dual-view 场景。
</verification>

<success_criteria>
1. 下游可直接按 schema 消费归一化输出。
2. example 可作为回归样例验证 INT-01/INT-02/INT-03。
3. Phase 1 的关键决策以可机读形式稳定落地。
</success_criteria>

<output>
After completion, create `.planning/phases/01-input-contract-missing-assumptions/01-02-SUMMARY.md`
</output>
