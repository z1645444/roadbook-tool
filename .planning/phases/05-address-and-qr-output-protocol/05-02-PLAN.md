---
phase: 05-address-and-qr-output-protocol
plan: "02"
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - .planning/phases/05-address-and-qr-output-protocol/05-OUTPUT.schema.json
  - .planning/phases/05-address-and-qr-output-protocol/05-OUTPUT.example.json
  - .planning/phases/05-address-and-qr-output-protocol/05-OUTPUT-VALIDATION-CHECKLIST.md
autonomous: true
requirements:
  - OUT-01
  - OUT-02
  - OUT-03
  - OUT-04
decision_refs:
  - D-04
  - D-05
  - D-06
  - D-10
  - D-11
  - D-12
  - D-13
  - D-14
  - D-15
must_haves:
  truths:
    - "输出必须有可机读 schema，防止不同 runtime 漂移。"
    - "example 需要覆盖二维码可用与不可用两类场景，并保持地址可复制。"
    - "OUT-03 一致性要求必须可用命令自动验证，不依赖人工判断。"
    - "验证清单必须可直接执行并给出通过/失败判定。"
  artifacts:
    - path: ".planning/phases/05-address-and-qr-output-protocol/05-OUTPUT.schema.json"
      provides: "Phase 5 输出结构 JSON Schema"
      contains: "address、eta、qr 状态、一致性字段、A-G 映射视图"
    - path: ".planning/phases/05-address-and-qr-output-protocol/05-OUTPUT.example.json"
      provides: "normal 与 degraded 示例"
      contains: "L0/L1..Ln、QR available/unavailable、payload 一致性样例"
    - path: ".planning/phases/05-address-and-qr-output-protocol/05-OUTPUT-VALIDATION-CHECKLIST.md"
      provides: "可执行检查项与命令"
      contains: "OUT-01..04 对应检查、判定条件、失败信号"
  key_links:
    - from: ".planning/phases/05-address-and-qr-output-protocol/05-OUTPUT.example.json"
      to: ".planning/phases/05-address-and-qr-output-protocol/05-OUTPUT.schema.json"
      via: "示例必须通过 schema 校验"
      pattern: "normal_case|degraded_case|qr"
    - from: ".planning/phases/05-address-and-qr-output-protocol/05-OUTPUT-VALIDATION-CHECKLIST.md"
      to: ".planning/phases/05-address-and-qr-output-protocol/05-OUTPUT.example.json"
      via: "检查项命令直接针对示例文档执行"
      pattern: "CHK-05-01|CHK-05-02|CHK-05-03|CHK-05-04"
---

<objective>
为 Phase 5 输出协议补齐可机读 schema、可复核示例和可执行检查清单，确保 OUT-01..04 能自动化验收。

Purpose: 把文档契约转换为“schema + example + checklist”三件套。
Output: `05-OUTPUT.schema.json`、`05-OUTPUT.example.json`、`05-OUTPUT-VALIDATION-CHECKLIST.md`。
</objective>

<execution_context>
@/Users/franklin/.codex/get-shit-done/workflows/execute-plan.md
@/Users/franklin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-address-and-qr-output-protocol/05-CONTEXT.md
@.planning/phases/05-address-and-qr-output-protocol/05-01-PLAN.md
@.planning/phases/04-amap-link-construction/04-AMAP-URI-CONTRACT.md
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 编写 Phase 5 输出 schema 并编码二维码降级约束</name>
  <files>.planning/phases/05-address-and-qr-output-protocol/05-OUTPUT.schema.json</files>
  <read_first>
    - .planning/phases/05-address-and-qr-output-protocol/05-OUTPUT.schema.json
    - .planning/phases/05-address-and-qr-output-protocol/05-01-PLAN.md
    - .planning/phases/05-address-and-qr-output-protocol/05-CONTEXT.md
    - .planning/REQUIREMENTS.md
  </read_first>
  <action>
    创建 `05-OUTPUT.schema.json`（draft 2020-12），顶层 required 字段固定为
    `route_summary`, `address`, `eta`, `notes`, `qr`, `output_views`, `metadata`，
    并设置 `additionalProperties:false`。`address` 必须包含 `primary_link` 与
    `segment_links[]`；`primary_link` 以 `https://uri.amap.com/` 开头。
    `qr.status` 枚举固定为 `available|unavailable|not_requested`。通过 `if/then`
    约束：当 `qr.status=available` 时 `qr.payload_text` 必填；当 `qr.status=unavailable`
    时 `notes.degrade_reason` 必填且 `qr.payload_text` 允许为空。新增一致性字段
    `qr.payload_equals_primary_link`（布尔）与 `qr.consistency_check_method`（固定
    `utf8_byte_compare`），用于表达 OUT-03。`output_views.ag_sections` 必须含 A..G。
    `metadata` 固定包含边界开关：`no_live_request=true`、`no_message_send=true`、
    `no_qr_image=true`，并映射 OUT-01..04。
  </action>
  <acceptance_criteria>
    - `node -e "JSON.parse(require('fs').readFileSync('.planning/phases/05-address-and-qr-output-protocol/05-OUTPUT.schema.json','utf8')); console.log('schema-json-ok')"` 输出 `schema-json-ok`。
    - `rg -n "\"route_summary\"|\"address\"|\"eta\"|\"notes\"|\"qr\"|\"primary_link\"|\"segment_links\"|\"available\"|\"unavailable\"|\"not_requested\"|\"payload_text\"|\"payload_equals_primary_link\"|\"utf8_byte_compare\"|\"ag_sections\"|\"A\"|\"B\"|\"C\"|\"D\"|\"E\"|\"F\"|\"G\"" .planning/phases/05-address-and-qr-output-protocol/05-OUTPUT.schema.json` 命中关键字段。
    - `rg -n "OUT-01|OUT-02|OUT-03|OUT-04|no_live_request|no_message_send|no_qr_image|https://uri.amap.com/" .planning/phases/05-address-and-qr-output-protocol/05-OUTPUT.schema.json` 命中需求与边界约束。
  </acceptance_criteria>
  <verify>
    <automated>node -e "JSON.parse(require('fs').readFileSync('.planning/phases/05-address-and-qr-output-protocol/05-OUTPUT.schema.json','utf8')); console.log('schema-json-ok')" && rg -n "\"route_summary\"|\"address\"|\"eta\"|\"notes\"|\"qr\"|\"primary_link\"|\"segment_links\"|\"available\"|\"unavailable\"|\"not_requested\"|\"payload_text\"|\"payload_equals_primary_link\"|\"utf8_byte_compare\"|\"ag_sections\"|\"A\"|\"B\"|\"C\"|\"D\"|\"E\"|\"F\"|\"G\"|OUT-01|OUT-02|OUT-03|OUT-04|no_live_request|no_message_send|no_qr_image|https://uri.amap.com/" .planning/phases/05-address-and-qr-output-protocol/05-OUTPUT.schema.json</automated>
  </verify>
  <done>schema 将地址主链路、二维码状态与降级规则转为可验证约束。</done>
</task>

<task type="auto">
  <name>Task 2: 编写 normal/degraded 示例并验证 schema 一致性</name>
  <files>.planning/phases/05-address-and-qr-output-protocol/05-OUTPUT.example.json</files>
  <read_first>
    - .planning/phases/05-address-and-qr-output-protocol/05-OUTPUT.schema.json
    - .planning/phases/05-address-and-qr-output-protocol/05-01-PLAN.md
    - .planning/phases/04-amap-link-construction/04-AMAP-LINK-EXAMPLES.md
    - .planning/REQUIREMENTS.md
  </read_first>
  <action>
    创建 `05-OUTPUT.example.json`，包含 `normal_case` 与 `degraded_case`。
    `normal_case`：`qr.status=available`，包含 `qr.payload_text`，并使
    `qr.payload_equals_primary_link=true`；地址同时包含 `L0` 与至少 2 个分段链接。
    `degraded_case`：`qr.status=unavailable`，`notes.degrade_reason` 写明二维码不可用原因，
    且仍完整提供 `address.primary_link` 与 `address.segment_links`。
    两个 case 都要提供 `output_views.ag_sections`（A..G）与边界标记
    `no_live_request/no_message_send/no_qr_image=true`。示例中禁止出现二维码图片字段或外部请求结果。
  </action>
  <acceptance_criteria>
    - `node -e "JSON.parse(require('fs').readFileSync('.planning/phases/05-address-and-qr-output-protocol/05-OUTPUT.example.json','utf8')); console.log('example-json-ok')"` 输出 `example-json-ok`。
    - `rg -n "\"normal_case\"|\"degraded_case\"|\"primary_link\"|\"segment_links\"|\"status\"|\"payload_text\"|\"payload_equals_primary_link\"|\"degrade_reason\"|\"ag_sections\"|\"A\"|\"B\"|\"C\"|\"D\"|\"E\"|\"F\"|\"G\"" .planning/phases/05-address-and-qr-output-protocol/05-OUTPUT.example.json` 命中示例字段。
    - `node -e "const Ajv=require('ajv/dist/2020'); const fs=require('fs'); const base='.planning/phases/05-address-and-qr-output-protocol'; const schema=JSON.parse(fs.readFileSync(base+'/05-OUTPUT.schema.json','utf8')); const data=JSON.parse(fs.readFileSync(base+'/05-OUTPUT.example.json','utf8')); const ajv=new Ajv({allErrors:true,strict:false}); const validate=ajv.compile(schema); for (const k of ['normal_case','degraded_case']) { if (!validate(data[k])) { console.error(k, validate.errors); process.exit(1);} } console.log('schema-validate-ok');"` 输出 `schema-validate-ok`。
    - `rg -n "qr_image|ascii_qr|base64_png|http_response|api_result" .planning/phases/05-address-and-qr-output-protocol/05-OUTPUT.example.json` 无命中。
  </acceptance_criteria>
  <verify>
    <automated>node -e "JSON.parse(require('fs').readFileSync('.planning/phases/05-address-and-qr-output-protocol/05-OUTPUT.example.json','utf8')); console.log('example-json-ok')" && rg -n "\"normal_case\"|\"degraded_case\"|\"primary_link\"|\"segment_links\"|\"status\"|\"payload_text\"|\"payload_equals_primary_link\"|\"degrade_reason\"|\"ag_sections\"|\"A\"|\"B\"|\"C\"|\"D\"|\"E\"|\"F\"|\"G\"" .planning/phases/05-address-and-qr-output-protocol/05-OUTPUT.example.json && node -e "const Ajv=require('ajv/dist/2020'); const fs=require('fs'); const base='.planning/phases/05-address-and-qr-output-protocol'; const schema=JSON.parse(fs.readFileSync(base+'/05-OUTPUT.schema.json','utf8')); const data=JSON.parse(fs.readFileSync(base+'/05-OUTPUT.example.json','utf8')); const ajv=new Ajv({allErrors:true,strict:false}); const validate=ajv.compile(schema); for (const k of ['normal_case','degraded_case']) { if (!validate(data[k])) { console.error(k, validate.errors); process.exit(1);} } console.log('schema-validate-ok');" && ! rg -n "qr_image|ascii_qr|base64_png|http_response|api_result" .planning/phases/05-address-and-qr-output-protocol/05-OUTPUT.example.json</automated>
  </verify>
  <done>示例覆盖二维码可用/不可用两类路径，并证明地址主链路始终可用。</done>
</task>

<task type="auto">
  <name>Task 3: 编写 Phase 5 可执行验证清单</name>
  <files>.planning/phases/05-address-and-qr-output-protocol/05-OUTPUT-VALIDATION-CHECKLIST.md</files>
  <read_first>
    - .planning/phases/05-address-and-qr-output-protocol/05-OUTPUT.schema.json
    - .planning/phases/05-address-and-qr-output-protocol/05-OUTPUT.example.json
    - .planning/REQUIREMENTS.md
  </read_first>
  <action>
    创建 `05-OUTPUT-VALIDATION-CHECKLIST.md`，以表格列出至少 4 个检查项：
    `CHK-05-01`（OUT-01，地址段主交付且 `primary_link` 为 `https://uri.amap.com/...`）；
    `CHK-05-02`（OUT-02，`normal_case` 下二维码文本存在）；
    `CHK-05-03`（OUT-03，`payload_equals_primary_link=true` 且比较方式为 `utf8_byte_compare`）；
    `CHK-05-04`（OUT-04，`degraded_case` 下二维码不可用但地址仍完整，含 degrade 说明）。
    表格列固定为：`check_id`, `rule`, `input`, `verify_command`, `pass_condition`,
    `mapped_requirement`。所有 `verify_command` 都使用可直接执行的 `rg` 或 `node -e` 命令。
  </action>
  <acceptance_criteria>
    - `rg -n "CHK-05-01|CHK-05-02|CHK-05-03|CHK-05-04|check_id|verify_command|pass_condition|mapped_requirement|OUT-01|OUT-02|OUT-03|OUT-04" .planning/phases/05-address-and-qr-output-protocol/05-OUTPUT-VALIDATION-CHECKLIST.md` 命中全部检查项与字段。
    - `rg -n "https://uri.amap.com/|available|unavailable|payload_equals_primary_link|utf8_byte_compare|degrade_reason|segment_links" .planning/phases/05-address-and-qr-output-protocol/05-OUTPUT-VALIDATION-CHECKLIST.md` 命中关键规则。
  </acceptance_criteria>
  <verify>
    <automated>rg -n "CHK-05-01|CHK-05-02|CHK-05-03|CHK-05-04|check_id|verify_command|pass_condition|mapped_requirement|OUT-01|OUT-02|OUT-03|OUT-04|https://uri.amap.com/|available|unavailable|payload_equals_primary_link|utf8_byte_compare|degrade_reason|segment_links" .planning/phases/05-address-and-qr-output-protocol/05-OUTPUT-VALIDATION-CHECKLIST.md</automated>
  </verify>
  <done>验证清单可直接被执行阶段复用，支持自动化判定 OUT-01..04。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| protocol_docs -> schema | 文档约束转 schema 时可能丢失强约束 |
| schema -> examples | 示例若不受 schema 约束会误导执行器 |
| examples -> checklist | 校验命令若不可执行将导致“看起来通过” |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-06 | T | schema downgrade logic | mitigate | 用 if/then 强制 unavailable 场景必须含 degrade_reason。 |
| T-05-07 | R | consistency assertion | mitigate | 明确 `payload_equals_primary_link` 与 `utf8_byte_compare` 双字段。 |
| T-05-08 | D | qr failure propagation | mitigate | checklist 强制验证 degraded_case 地址字段完整。 |
| T-05-09 | I | scope leakage | mitigate | schema/example 固定 `no_qr_image=true` 与无外部请求字段。 |
| T-05-10 | E | cross-runtime drift | mitigate | 通过 schema + checklist 双重检查关键字段与状态枚举。 |
</threat_model>

<verification>
1. schema 与 example 均可被 JSON.parse 解析。
2. `normal_case` 与 `degraded_case` 通过同一 schema 校验。
3. 检查清单可覆盖 OUT-01..04 且命令可执行。
</verification>

<success_criteria>
1. OUT-01：地址主链路与分段链接在 schema/example 中强制存在。
2. OUT-02：二维码可用场景输出文本载荷可验证。
3. OUT-03：载荷与地址一致性的机读字段与检查命令齐全。
4. OUT-04：二维码失败时的 address-only 降级路径可复现可校验。
</success_criteria>

<decision_coverage>
This plan operationalizes decision IDs: D-04, D-05, D-06, D-10, D-11, D-12, D-13, D-14, D-15.
Decision IDs not listed here remain covered by 05-01 plan.
</decision_coverage>

<output>
After completion, create `.planning/phases/05-address-and-qr-output-protocol/05-02-SUMMARY.md`
</output>
