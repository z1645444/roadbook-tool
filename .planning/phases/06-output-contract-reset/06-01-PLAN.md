---
phase: 06-output-contract-reset
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - AGENTS.md
  - .planning/phases/06-output-contract-reset/06-OUTPUT-CONTRACT-v1.1.md
  - .planning/phases/06-output-contract-reset/06-OUTPUT-TEMPLATE-v1.1.md
autonomous: true
requirements:
  - OC-01
  - OC-02
must_haves:
  truths:
    - "v1.1 主契约仅允许四个一等字段：route_summary、address、eta、notes。"
    - "主契约字段顺序固定，所有模板与示例必须按同一顺序输出。"
    - "二维码不再属于主契约字段，任何二维码能力只能在兼容层处理。"
    - "当前阶段不执行真实请求、不发送消息、不生成真实二维码图片。"
  artifacts:
    - path: ".planning/phases/06-output-contract-reset/06-OUTPUT-CONTRACT-v1.1.md"
      provides: "v1.1 主契约规范"
      contains: "字段定义、固定顺序、禁用字段、需求追踪"
    - path: ".planning/phases/06-output-contract-reset/06-OUTPUT-TEMPLATE-v1.1.md"
      provides: "v1.1 输出模板"
      contains: "normal/degraded 模板、固定渲染顺序、兼容层说明"
  key_links:
    - from: ".planning/phases/06-output-contract-reset/06-OUTPUT-CONTRACT-v1.1.md"
      to: ".planning/phases/05-address-and-qr-output-protocol/05-OUTPUT-PROTOCOL.md"
      via: "复用地址与降级语义，但重置主契约字段边界"
      pattern: "route_summary|address|eta|notes"
    - from: ".planning/phases/06-output-contract-reset/06-OUTPUT-TEMPLATE-v1.1.md"
      to: ".planning/phases/06-output-contract-reset/06-OUTPUT-CONTRACT-v1.1.md"
      via: "模板字段顺序必须与主契约完全一致"
      pattern: "route_summary|address|eta|notes"
---

<objective>
建立 v1.1 单一主契约口径：仅保留 `route_summary/address/eta/notes` 四字段，移除二维码主字段。

Purpose: 将 OC-01/OC-02 转成可执行文档规范，消除 v1.0 的主契约歧义。
Output: `06-OUTPUT-CONTRACT-v1.1.md` 与 `06-OUTPUT-TEMPLATE-v1.1.md`。
</objective>

<execution_context>
@/Users/franklin/.codex/get-shit-done/workflows/execute-plan.md
@/Users/franklin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-address-and-qr-output-protocol/05-OUTPUT-PROTOCOL.md
@.planning/phases/05-address-and-qr-output-protocol/05-OUTPUT-TEMPLATE.md
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 0: 更新 AGENTS 输出口径以消除 A-G 与主契约冲突</name>
  <files>AGENTS.md</files>
  <read_first>
    - AGENTS.md
    - .planning/ROADMAP.md
    - .planning/REQUIREMENTS.md
  </read_first>
  <action>
    更新 `AGENTS.md` 的 Scope 规则，将“skill 输出必须遵循 A-G 固定结构”调整为：
    “v1.1 主契约采用 `route_summary/address/eta/notes` 四字段；A-G 仅作为兼容视图，不是主契约”。
    保留原有高德 URI、`callnative=1`、URL 编码、不执行真实请求等约束不变。
    在文档中追加一行过渡说明：兼容层详细迁移在 Phase 7 定义。
  </action>
  <acceptance_criteria>
    - `rg -n "v1.1 主契约采用 route_summary/address/eta/notes 四字段|A-G 仅作为兼容视图，不是主契约|Phase 7" AGENTS.md` 命中。
    - `rg -n "https://uri.amap.com|callnative=1|URL 编码|不执行真实请求|不发送消息|不生成真实二维码图片" AGENTS.md` 仍命中。
  </acceptance_criteria>
  <verify>
    <automated>rg -n "v1.1 主契约采用 route_summary/address/eta/notes 四字段|A-G 仅作为兼容视图，不是主契约|Phase 7|https://uri.amap.com|callnative=1|URL 编码|不执行真实请求|不发送消息|不生成真实二维码图片" AGENTS.md</automated>
  </verify>
  <done>AGENTS 与 Phase 6 目标对齐，执行阶段不再出现规则冲突。</done>
</task>

<task type="auto">
  <name>Task 1: 编写 v1.1 主契约规范并固定字段顺序</name>
  <files>.planning/phases/06-output-contract-reset/06-OUTPUT-CONTRACT-v1.1.md</files>
  <read_first>
    - .planning/phases/06-output-contract-reset/06-OUTPUT-CONTRACT-v1.1.md
    - .planning/phases/05-address-and-qr-output-protocol/05-OUTPUT-PROTOCOL.md
    - .planning/REQUIREMENTS.md
    - .planning/ROADMAP.md
    - AGENTS.md
  </read_first>
  <action>
    创建 `06-OUTPUT-CONTRACT-v1.1.md`，并固定章节：
    1) 主契约字段总览（且仅有）：`route_summary`, `address`, `eta`, `notes`；
    2) 固定输出顺序声明：所有实现必须按上述顺序渲染，不允许重排；
    3) 禁用字段清单：`qr`, `qr_status`, `qr_payload_text`, `qr_image`, `ascii_qr` 等
    在 v1.1 主契约中标记为 removed；
    4) 地址字段沿用 Phase 5 的 `https://uri.amap.com/...` 与 `callnative=1` 约束，但
    不新增二维码相关主字段；
    5) 需求追踪表：`OC-01`、`OC-02` 分别映射到具体条款。
  </action>
  <acceptance_criteria>
    - `rg -n "route_summary|address|eta|notes|固定顺序|唯一主契约|OC-01|OC-02" .planning/phases/06-output-contract-reset/06-OUTPUT-CONTRACT-v1.1.md` 命中。
    - `rg -n "qr_status|qr_payload_text|qr_image|ascii_qr|removed|不属于主契约" .planning/phases/06-output-contract-reset/06-OUTPUT-CONTRACT-v1.1.md` 命中。
    - `rg -n "https://uri.amap.com|callnative=1" .planning/phases/06-output-contract-reset/06-OUTPUT-CONTRACT-v1.1.md` 命中。
  </acceptance_criteria>
  <verify>
    <automated>rg -n "route_summary|address|eta|notes|固定顺序|唯一主契约|OC-01|OC-02|qr_status|qr_payload_text|qr_image|ascii_qr|removed|不属于主契约|https://uri.amap.com|callnative=1" .planning/phases/06-output-contract-reset/06-OUTPUT-CONTRACT-v1.1.md</automated>
  </verify>
  <done>主契约规范形成单一真相源，可直接约束后续 schema/example 实现。</done>
</task>

<task type="auto">
  <name>Task 2: 编写 v1.1 输出模板并隔离二维码为兼容层</name>
  <files>.planning/phases/06-output-contract-reset/06-OUTPUT-TEMPLATE-v1.1.md</files>
  <read_first>
    - .planning/phases/06-output-contract-reset/06-OUTPUT-TEMPLATE-v1.1.md
    - .planning/phases/06-output-contract-reset/06-OUTPUT-CONTRACT-v1.1.md
    - .planning/phases/05-address-and-qr-output-protocol/05-OUTPUT-TEMPLATE.md
    - .planning/REQUIREMENTS.md
  </read_first>
  <action>
    创建 `06-OUTPUT-TEMPLATE-v1.1.md`，至少包含 `normal` 与 `degraded` 两套模板。
    两套模板都必须按同一顺序输出四字段：`route_summary` -> `address` -> `eta` -> `notes`。
    模板内不得出现 `qr` 主字段。若需保留旧格式说明，仅在“兼容层（非主契约）”章节描述
    Phase 7 会处理的 A-G 或二维码渲染，不得混入主字段列表。补充机读片段示意，确保顶层
    只有四个主字段。
  </action>
  <acceptance_criteria>
    - `rg -n "normal|degraded|route_summary|address|eta|notes|固定顺序" .planning/phases/06-output-contract-reset/06-OUTPUT-TEMPLATE-v1.1.md` 命中。
    - `rg -n "兼容层|非主契约|A-G|Phase 7" .planning/phases/06-output-contract-reset/06-OUTPUT-TEMPLATE-v1.1.md` 命中。
    - `rg -n "^.*\bqr\b.*$|qr_status|qr_payload_text|qr_image|ascii_qr" .planning/phases/06-output-contract-reset/06-OUTPUT-TEMPLATE-v1.1.md` 在主模板段落不命中（仅兼容层说明允许出现）。
  </acceptance_criteria>
  <verify>
    <automated>rg -n "normal|degraded|route_summary|address|eta|notes|固定顺序|兼容层|非主契约|A-G|Phase 7" .planning/phases/06-output-contract-reset/06-OUTPUT-TEMPLATE-v1.1.md && awk '/^## normal/{flag=1} /^## 兼容层/{flag=0} flag' .planning/phases/06-output-contract-reset/06-OUTPUT-TEMPLATE-v1.1.md | ! rg -n "\bqr\b|qr_status|qr_payload_text|qr_image|ascii_qr"</automated>
  </verify>
  <done>v1.1 文案模板与主契约完全对齐，二维码影响被隔离到兼容层说明。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| legacy_protocol -> v1.1_contract | v1.0 语义迁移到 v1.1 时，可能把二维码字段错误带入主契约 |
| contract_doc -> template_doc | 模板若未严格对齐契约顺序会导致跨 runtime 输出漂移 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | T | primary fields | mitigate | 在契约文档中硬编码四字段白名单，并列出移除字段黑名单。 |
| T-06-02 | R | requirement trace | mitigate | 为 OC-01/OC-02 建立条款追踪表，减少后续责任不清。 |
| T-06-03 | D | output drift | mitigate | 模板与契约都声明固定顺序，执行时可机械校验。 |
| T-06-04 | I | scope leakage | mitigate | 明确当前阶段不执行真实请求/发送消息/生成二维码图片。 |
| T-06-05 | E | compatibility confusion | mitigate | 将二维码与 A-G 明确标注为兼容层，不允许占据主契约字段。 |
</threat_model>

<verification>
1. `06-OUTPUT-CONTRACT-v1.1.md` 与 `06-OUTPUT-TEMPLATE-v1.1.md` 创建完成。
2. 文档中可检索到 OC-01/OC-02 与四字段固定顺序。
3. v1.1 主契约中二维码字段被显式移除。
</verification>

<success_criteria>
1. 主契约字段在协议与模板中保持唯一且固定顺序。
2. 二维码字段不再作为 v1.1 主契约的一部分。
</success_criteria>

<output>
After completion, create `.planning/phases/06-output-contract-reset/06-01-SUMMARY.md`
</output>
