---
phase: 07-compatibility-layer-migration-strategy
plan: "02"
type: execute
wave: 2
depends_on:
  - 07-01
files_modified:
  - .planning/phases/07-compatibility-layer-migration-strategy/07-COMPAT-BLOCKLIST-v1.1.json
  - .planning/phases/07-compatibility-layer-migration-strategy/07-OUTPUT-v1.1-compat.schema.json
  - .planning/phases/07-compatibility-layer-migration-strategy/07-OUTPUT-v1.1-compat.example.json
  - .planning/phases/07-compatibility-layer-migration-strategy/07-COMPAT-VALIDATION-CHECKLIST-v1.1.md
autonomous: true
requirements:
  - OC-03
  - OC-06
decision_refs:
  - D-15
  - D-16
  - D-17
  - D-18
  - D-19
  - D-20
  - D-21
must_haves:
  truths:
    - "防反污染采用 Schema + Checklist 双门禁，命中黑名单即阻断。"
    - "黑名单是单一真相源，schema 与 checklist 必须共用同一字段列表。"
    - "示例必须证明：合法四字段通过，旧字段注入失败且可定位字段路径。"
    - "Phase 7 交付命名采用 07-* + v1.1，并能直接给 Phase 8 复用。"
  artifacts:
    - path: ".planning/phases/07-compatibility-layer-migration-strategy/07-COMPAT-BLOCKLIST-v1.1.json"
      provides: "禁入字段集中清单"
      contains: "A-G 相关键、二维码历史字段、legacy 机读关键键"
    - path: ".planning/phases/07-compatibility-layer-migration-strategy/07-OUTPUT-v1.1-compat.schema.json"
      provides: "兼容层阻断 schema"
      contains: "四字段白名单、additionalProperties 约束、黑名单阻断语义"
    - path: ".planning/phases/07-compatibility-layer-migration-strategy/07-OUTPUT-v1.1-compat.example.json"
      provides: "通过/阻断双场景示例"
      contains: "pass case + fail-fast case + legacy 注入失败样例"
    - path: ".planning/phases/07-compatibility-layer-migration-strategy/07-COMPAT-VALIDATION-CHECKLIST-v1.1.md"
      provides: "可执行验证清单"
      contains: "禁旧字段检查、矩阵逐项复核、schema 校验、失败定位输出"
  key_links:
    - from: ".planning/phases/07-compatibility-layer-migration-strategy/07-OUTPUT-v1.1-compat.schema.json"
      to: ".planning/phases/06-output-contract-reset/06-OUTPUT-v1.1.schema.json"
      via: "继承四字段主契约约束并追加兼容层阻断语义"
      pattern: "route_summary|address|eta|notes|additionalProperties"
    - from: ".planning/phases/07-compatibility-layer-migration-strategy/07-COMPAT-VALIDATION-CHECKLIST-v1.1.md"
      to: ".planning/phases/07-compatibility-layer-migration-strategy/07-COMPAT-BLOCKLIST-v1.1.json"
      via: "验证命令必须读取同一黑名单源"
      pattern: "blocklist|legacy_fields|hit_path"
    - from: ".planning/phases/07-compatibility-layer-migration-strategy/07-OUTPUT-v1.1-compat.example.json"
      to: ".planning/phases/07-compatibility-layer-migration-strategy/07-OUTPUT-v1.1-compat.schema.json"
      via: "示例通过 schema 与 fail-fast 输出规则双重验证"
      pattern: "pass_case|blocked_case|E_COMPAT_AG_REMOVED"
---

<objective>
构建 Phase 7 的阻断式验证资产（blocklist/schema/example/checklist），确保旧字段回流可被机械拒绝并定位。

Purpose: 把 D-15..D-21 落地为可执行门禁，避免迁移规则停留在文档层。
Output: `07-COMPAT-BLOCKLIST-v1.1.json`、`07-OUTPUT-v1.1-compat.schema.json`、`07-OUTPUT-v1.1-compat.example.json`、`07-COMPAT-VALIDATION-CHECKLIST-v1.1.md`。
</objective>

<execution_context>
@/Users/franklin/.codex/get-shit-done/workflows/execute-plan.md
@/Users/franklin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/07-compatibility-layer-migration-strategy/07-CONTEXT.md
@.planning/phases/07-compatibility-layer-migration-strategy/07-01-PLAN.md
@.planning/phases/07-compatibility-layer-migration-strategy/07-MIGRATION-MATRIX-v1.1.md
@.planning/phases/07-compatibility-layer-migration-strategy/07-COMPAT-FAILFAST-SPEC-v1.1.md
@.planning/phases/06-output-contract-reset/06-OUTPUT-v1.1.schema.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: 创建集中黑名单并作为唯一禁入字段源</name>
  <files>.planning/phases/07-compatibility-layer-migration-strategy/07-COMPAT-BLOCKLIST-v1.1.json</files>
  <read_first>
    - .planning/phases/07-compatibility-layer-migration-strategy/07-MIGRATION-MATRIX-v1.1.md
    - .planning/phases/05-address-and-qr-output-protocol/05-OUTPUT.schema.json
    - .planning/phases/03-route-rules/03-ROUTE-OUTPUT.schema.json
  </read_first>
  <action>
    创建 `07-COMPAT-BLOCKLIST-v1.1.json`，包含：
    `version`、`legacy_fields`、`legacy_paths`、`notes`。
    `legacy_fields` 至少覆盖：`output_views`、`ag_sections`、`qr_status`、`qr_payload_text`、
    `qr_image`、`ascii_qr` 及迁移矩阵中标记 `drop` 的关键字段。
    `legacy_paths` 列出常见 JSONPath/点路径，供 checklist 直接复用。
  </action>
  <acceptance_criteria>
    - `node -e "JSON.parse(require('fs').readFileSync('.planning/phases/07-compatibility-layer-migration-strategy/07-COMPAT-BLOCKLIST-v1.1.json','utf8')); console.log('blocklist-json-ok')"` 输出 `blocklist-json-ok`。
    - `rg -n '"legacy_fields"|"legacy_paths"|"output_views"|"ag_sections"|"qr_status"|"qr_payload_text"|"qr_image"|"ascii_qr"' .planning/phases/07-compatibility-layer-migration-strategy/07-COMPAT-BLOCKLIST-v1.1.json` 命中。
  </acceptance_criteria>
  <verify>
    <automated>node -e "JSON.parse(require('fs').readFileSync('.planning/phases/07-compatibility-layer-migration-strategy/07-COMPAT-BLOCKLIST-v1.1.json','utf8')); console.log('blocklist-json-ok')" && rg -n '"legacy_fields"|"legacy_paths"|"output_views"|"ag_sections"|"qr_status"|"qr_payload_text"|"qr_image"|"ascii_qr"' .planning/phases/07-compatibility-layer-migration-strategy/07-COMPAT-BLOCKLIST-v1.1.json</automated>
  </verify>
  <done>黑名单成为 schema/checklist 共用的单一禁入源。</done>
</task>

<task type="auto">
  <name>Task 2: 创建兼容层 schema 与通过/阻断示例</name>
  <files>
    .planning/phases/07-compatibility-layer-migration-strategy/07-OUTPUT-v1.1-compat.schema.json
    .planning/phases/07-compatibility-layer-migration-strategy/07-OUTPUT-v1.1-compat.example.json
  </files>
  <read_first>
    - .planning/phases/06-output-contract-reset/06-OUTPUT-v1.1.schema.json
    - .planning/phases/07-compatibility-layer-migration-strategy/07-COMPAT-BLOCKLIST-v1.1.json
    - .planning/phases/07-compatibility-layer-migration-strategy/07-COMPAT-FAILFAST-SPEC-v1.1.md
  </read_first>
  <action>
    创建 `07-OUTPUT-v1.1-compat.schema.json`：
    顶层仅允许 `route_summary/address/eta/notes`，`additionalProperties=false`。
    添加 `blocked_case` 的 fail-fast 结构要求：`notes` 中必须包含 `E_COMPAT_AG_REMOVED` 与迁移指引片段。
    创建 `07-OUTPUT-v1.1-compat.example.json`，至少包含：
    `pass_case`（纯四字段）与 `blocked_case`（四字段 + fail-fast 提示），并提供
    `legacy_injection_samples`（故意含黑名单字段）用于负向验证。
  </action>
  <acceptance_criteria>
    - `node -e "JSON.parse(require('fs').readFileSync('.planning/phases/07-compatibility-layer-migration-strategy/07-OUTPUT-v1.1-compat.schema.json','utf8')); JSON.parse(require('fs').readFileSync('.planning/phases/07-compatibility-layer-migration-strategy/07-OUTPUT-v1.1-compat.example.json','utf8')); console.log('compat-json-ok')"` 输出 `compat-json-ok`。
    - `rg -n '"route_summary"|"address"|"eta"|"notes"|"additionalProperties"|"E_COMPAT_AG_REMOVED"|"pass_case"|"blocked_case"|"legacy_injection_samples"' .planning/phases/07-compatibility-layer-migration-strategy/07-OUTPUT-v1.1-compat.schema.json .planning/phases/07-compatibility-layer-migration-strategy/07-OUTPUT-v1.1-compat.example.json` 命中。
    - `node -e "const Ajv=require('ajv/dist/2020'); const fs=require('fs'); const base='.planning/phases/07-compatibility-layer-migration-strategy'; const schema=JSON.parse(fs.readFileSync(base+'/07-OUTPUT-v1.1-compat.schema.json','utf8')); const data=JSON.parse(fs.readFileSync(base+'/07-OUTPUT-v1.1-compat.example.json','utf8')); const ajv=new Ajv({allErrors:true,strict:false}); const validate=ajv.compile(schema); for (const k of ['pass_case','blocked_case']) { if (!validate(data[k])) { console.error(k, validate.errors); process.exit(1);} } console.log('compat-schema-validate-ok');"` 输出 `compat-schema-validate-ok`。
  </acceptance_criteria>
  <verify>
    <automated>node -e "JSON.parse(require('fs').readFileSync('.planning/phases/07-compatibility-layer-migration-strategy/07-OUTPUT-v1.1-compat.schema.json','utf8')); JSON.parse(require('fs').readFileSync('.planning/phases/07-compatibility-layer-migration-strategy/07-OUTPUT-v1.1-compat.example.json','utf8')); console.log('compat-json-ok')" && rg -n '"route_summary"|"address"|"eta"|"notes"|"additionalProperties"|"E_COMPAT_AG_REMOVED"|"pass_case"|"blocked_case"|"legacy_injection_samples"' .planning/phases/07-compatibility-layer-migration-strategy/07-OUTPUT-v1.1-compat.schema.json .planning/phases/07-compatibility-layer-migration-strategy/07-OUTPUT-v1.1-compat.example.json && node -e "const Ajv=require('ajv/dist/2020'); const fs=require('fs'); const base='.planning/phases/07-compatibility-layer-migration-strategy'; const schema=JSON.parse(fs.readFileSync(base+'/07-OUTPUT-v1.1-compat.schema.json','utf8')); const data=JSON.parse(fs.readFileSync(base+'/07-OUTPUT-v1.1-compat.example.json','utf8')); const ajv=new Ajv({allErrors:true,strict:false}); const validate=ajv.compile(schema); for (const k of ['pass_case','blocked_case']) { if (!validate(data[k])) { console.error(k, validate.errors); process.exit(1);} } console.log('compat-schema-validate-ok');"</automated>
  </verify>
  <done>兼容层 schema/example 形成可执行“通过+阻断”双路径证据。</done>
</task>

<task type="auto">
  <name>Task 3: 编写可执行 checklist 并输出命中字段定位信息</name>
  <files>.planning/phases/07-compatibility-layer-migration-strategy/07-COMPAT-VALIDATION-CHECKLIST-v1.1.md</files>
  <read_first>
    - .planning/phases/07-compatibility-layer-migration-strategy/07-COMPAT-BLOCKLIST-v1.1.json
    - .planning/phases/07-compatibility-layer-migration-strategy/07-OUTPUT-v1.1-compat.schema.json
    - .planning/phases/07-compatibility-layer-migration-strategy/07-OUTPUT-v1.1-compat.example.json
    - .planning/phases/07-compatibility-layer-migration-strategy/07-MIGRATION-MATRIX-v1.1.md
  </read_first>
  <action>
    创建 `07-COMPAT-VALIDATION-CHECKLIST-v1.1.md`，表格列固定：
    `check_id`, `rule`, `input`, `verify_command`, `pass_condition`, `mapped_requirement`。
    至少包含：
    `CHK-07-01` 黑名单字段扫描（命中即失败）；
    `CHK-07-02` 迁移矩阵 verification 列完整性；
    `CHK-07-03` pass_case/blocked_case schema 校验；
    `CHK-07-04` blocked_case 包含 `E_COMPAT_AG_REMOVED`；
    `CHK-07-05` legacy_injection_samples 输出命中字段路径（字段名+路径）。
    命令仅用 `rg`、`jq`、`node -e`，确保执行即得 PASS/FAIL。
  </action>
  <acceptance_criteria>
    - `rg -n 'CHK-07-01|CHK-07-02|CHK-07-03|CHK-07-04|CHK-07-05|check_id|verify_command|pass_condition|mapped_requirement|OC-03|OC-06' .planning/phases/07-compatibility-layer-migration-strategy/07-COMPAT-VALIDATION-CHECKLIST-v1.1.md` 命中。
    - `rg -n 'E_COMPAT_AG_REMOVED|legacy_fields|legacy_paths|hit_path|schema-validate|verification' .planning/phases/07-compatibility-layer-migration-strategy/07-COMPAT-VALIDATION-CHECKLIST-v1.1.md` 命中。
  </acceptance_criteria>
  <verify>
    <automated>rg -n 'CHK-07-01|CHK-07-02|CHK-07-03|CHK-07-04|CHK-07-05|check_id|verify_command|pass_condition|mapped_requirement|OC-03|OC-06|E_COMPAT_AG_REMOVED|legacy_fields|legacy_paths|hit_path|schema-validate|verification' .planning/phases/07-compatibility-layer-migration-strategy/07-COMPAT-VALIDATION-CHECKLIST-v1.1.md</automated>
  </verify>
  <done>验证清单可机械阻断旧字段回流，并提供可复核命中位置。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| blocklist -> schema | schema 未同步黑名单会造成阻断遗漏 |
| schema -> example | 示例若不覆盖失败路径，会掩盖 fail-fast 回归风险 |
| checklist -> execution | 清单若无法定位命中字段，问题难以复盘与修复 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-06 | T | blocklist integrity | mitigate | 黑名单集中存放并由多检查项复用。 |
| T-07-07 | R | migration verification | mitigate | 矩阵每行强制 verification 列。 |
| T-07-08 | D | legacy regression | mitigate | 命中黑名单直接失败，不降级。 |
| T-07-09 | I | error observability | mitigate | checklist 输出字段名与命中路径。 |
| T-07-10 | E | phase handoff risk | mitigate | 资产命名统一 `07-* + v1.1` 供 Phase 8 继承。 |
</threat_model>

<verification>
1. blocklist/schema/example/checklist 四件套创建完成。
2. schema 校验通过 `pass_case` 与 `blocked_case`。
3. 旧字段注入样例可被检查命令阻断并输出命中路径。
</verification>

<success_criteria>
1. 命中黑名单即阻断，且反馈包含字段与位置。
2. 迁移矩阵、schema、示例、清单之间保持同一字段口径。
</success_criteria>

<decision_coverage>
This plan operationalizes decision IDs: D-15, D-16, D-17, D-18, D-19, D-20, D-21.
Decision IDs not listed here are covered by 07-01 plan.
</decision_coverage>

<output>
After completion, create `.planning/phases/07-compatibility-layer-migration-strategy/07-02-SUMMARY.md`
</output>
