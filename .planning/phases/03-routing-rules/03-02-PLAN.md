---
phase: 03-routing-rules
plan: "02"
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - .planning/phases/03-routing-rules/03-ROUTE-OUTPUT.schema.json
  - .planning/phases/03-routing-rules/03-ROUTE-OUTPUT.example.json
autonomous: true
requirements:
  - ROUTE-01
  - ROUTE-02
  - ROUTE-03
must_haves:
  truths:
    - "路线输出结构必须可机校验，且节点顺序与分段信息一致。"
    - "normal 与缺失起终点场景都必须产出有效路线结果。"
    - "耗时估算与低置信标记字段必须可被后续阶段直接消费。"
  artifacts:
    - path: ".planning/phases/03-routing-rules/03-ROUTE-OUTPUT.schema.json"
      provides: "Phase 3 路线输出 JSON Schema"
      contains: "有序节点数组、分段方式耗时、假设与风险字段"
    - path: ".planning/phases/03-routing-rules/03-ROUTE-OUTPUT.example.json"
      provides: "符合 schema 的路线输出样例"
      contains: "normal_case 与 missing_origin_or_destination_case"
  key_links:
    - from: ".planning/phases/03-routing-rules/03-ROUTE-STRATEGY.md"
      to: ".planning/phases/03-routing-rules/03-ROUTE-OUTPUT.schema.json"
      via: "顺序编排字段映射"
      pattern: "ordered_route|node_type|assumptions"
    - from: ".planning/phases/03-routing-rules/03-DURATION-HEURISTICS.md"
      to: ".planning/phases/03-routing-rules/03-ROUTE-OUTPUT.example.json"
      via: "分段耗时估算落地"
      pattern: "mode|duration_estimate_min|duration_note"
---

<objective>
把 Phase 3 规则固化为 schema 与 example，确保路线编排输出稳定、可验证、可下游复用。

Purpose: 防止多 agent 在路线结构与分段字段上的输出漂移。
Output: `03-ROUTE-OUTPUT.schema.json` 与 `03-ROUTE-OUTPUT.example.json`。
</objective>

<execution_context>
@/Users/franklin/.codex/get-shit-done/workflows/execute-plan.md
@/Users/franklin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-routing-rules/03-ROUTE-STRATEGY.md
@.planning/phases/03-routing-rules/03-DURATION-HEURISTICS.md
@.planning/phases/02-poi/02-POI-OUTPUT.schema.json
@.planning/phases/02-poi/02-POI-OUTPUT.example.json
@AGENTS.md

<interfaces>
From 03-ROUTE-STRATEGY.md:
- `ordered_route[]` 节点必须按 origin/stops/destination 有序输出。
- 缺失起终点必须产生 assumptions 条目并继续生成。

From 03-DURATION-HEURISTICS.md:
- 每段必须含 `mode` 与 `duration_estimate_min`。
- 低置信时必须带 `estimate_confidence=low` 说明。
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 定义路线输出 JSON Schema</name>
  <files>.planning/phases/03-routing-rules/03-ROUTE-OUTPUT.schema.json</files>
  <read_first>
    - .planning/phases/03-routing-rules/03-ROUTE-OUTPUT.schema.json
    - .planning/phases/03-routing-rules/03-ROUTE-STRATEGY.md
    - .planning/phases/03-routing-rules/03-DURATION-HEURISTICS.md
    - .planning/phases/02-poi/02-POI-OUTPUT.schema.json
    - .planning/REQUIREMENTS.md
  </read_first>
  <action>
    Create `03-ROUTE-OUTPUT.schema.json` using draft-2020-12 with strict
    `required` and `additionalProperties:false`. Top-level required keys must be:
    `route_context`, `ordered_route`, `segment_summaries`, `assumptions`, `metadata`.
    Encode ROUTE-01 by requiring `ordered_route` minItems 2 and per-item fields
    `node_index`, `node_type` enum(`origin`,`stop`,`destination`), `poi_ref`, `city`.
    Encode ROUTE-02 by requiring `segment_summaries` items with
    `segment_index`, `from_node_index`, `to_node_index`, `mode` enum(`walking`,`cycling`,`driving`),
    `duration_estimate_min` integer >=1, `duration_note` string.
    Encode ROUTE-03 by requiring `assumptions` minItems 1 when
    `metadata.missing_endpoint_handled=true`, and forcing `assumption_id` pattern
    `^A-(origin|destination)-[0-9]+$`.
    Add `metadata` fields: `candidate_count_mode`, `low_confidence_mode`,
    `missing_endpoint_handled`, `total_estimate_min`, `non_realtime_estimate=true`.
    Keep schema explicitly scoped: no URI/no QR/no external request fields.
  </action>
  <acceptance_criteria>
    - `node -e "JSON.parse(require('fs').readFileSync('.planning/phases/03-routing-rules/03-ROUTE-OUTPUT.schema.json','utf8')); console.log('schema-json-ok')"` prints `schema-json-ok`.
    - `rg -n "\"route_context\"|\"ordered_route\"|\"segment_summaries\"|\"assumptions\"|\"metadata\"|\"origin\"|\"stop\"|\"destination\"|\"walking\"|\"cycling\"|\"driving\"|\"missing_endpoint_handled\"|\"non_realtime_estimate\"" .planning/phases/03-routing-rules/03-ROUTE-OUTPUT.schema.json` returns matches.
    - `rg -n "ROUTE-01|ROUTE-02|ROUTE-03|A-(origin|destination)-[0-9]+|no URI|no QR|external request" .planning/phases/03-routing-rules/03-ROUTE-OUTPUT.schema.json` returns matches.
  </acceptance_criteria>
  <verify>
    <automated>node -e "JSON.parse(require('fs').readFileSync('.planning/phases/03-routing-rules/03-ROUTE-OUTPUT.schema.json','utf8')); console.log('schema-json-ok')" && rg -n "\"route_context\"|\"ordered_route\"|\"segment_summaries\"|\"assumptions\"|\"metadata\"|\"origin\"|\"stop\"|\"destination\"|\"walking\"|\"cycling\"|\"driving\"|\"missing_endpoint_handled\"|\"non_realtime_estimate\"|ROUTE-01|ROUTE-02|ROUTE-03|A-(origin|destination)-[0-9]+|no URI|no QR|external request" .planning/phases/03-routing-rules/03-ROUTE-OUTPUT.schema.json</automated>
  </verify>
  <done>Schema 可稳定约束路线顺序、分段耗时与缺失兜底字段，并保持 Phase 3 范围边界。</done>
</task>

<task type="auto">
  <name>Task 2: 编写路线输出示例并覆盖缺失起终点场景</name>
  <files>.planning/phases/03-routing-rules/03-ROUTE-OUTPUT.example.json</files>
  <read_first>
    - .planning/phases/03-routing-rules/03-ROUTE-OUTPUT.example.json
    - .planning/phases/03-routing-rules/03-ROUTE-OUTPUT.schema.json
    - .planning/phases/03-routing-rules/03-ROUTE-STRATEGY.md
    - .planning/phases/03-routing-rules/03-DURATION-HEURISTICS.md
    - .planning/phases/02-poi/02-POI-OUTPUT.example.json
  </read_first>
  <action>
    Create `03-ROUTE-OUTPUT.example.json` with deterministic top-level order and two samples:
    `normal_case` and `missing_origin_or_destination_case`.
    `normal_case` must contain one origin, at least one stop, one destination, and segment entries
    with mode+minutes that match route adjacency.
    `missing_origin_or_destination_case` must set `metadata.missing_endpoint_handled=true`, include
    at least one assumption id (`A-origin-1` or `A-destination-1`), and still provide完整
    ordered_route + segment_summaries output.
    Both cases must include `total_estimate_min` and low-confidence annotation when applicable.
    Ensure sample contains no `https://uri.amap.com`, no `amapuri://`, no QR payload fields, and no
    API request payload.
  </action>
  <acceptance_criteria>
    - `node -e "JSON.parse(require('fs').readFileSync('.planning/phases/03-routing-rules/03-ROUTE-OUTPUT.example.json','utf8')); console.log('example-json-ok')"` prints `example-json-ok`.
    - `rg -n "\"normal_case\"|\"missing_origin_or_destination_case\"|\"ordered_route\"|\"segment_summaries\"|\"assumptions\"|\"total_estimate_min\"|\"missing_endpoint_handled\"|\"A-origin-1\"|\"A-destination-1\"" .planning/phases/03-routing-rules/03-ROUTE-OUTPUT.example.json` returns matches.
    - `node -e "const Ajv=require('ajv/dist/2020'); const fs=require('fs'); const base='.planning/phases/03-routing-rules'; const schema=JSON.parse(fs.readFileSync(base+'/03-ROUTE-OUTPUT.schema.json','utf8')); const data=JSON.parse(fs.readFileSync(base+'/03-ROUTE-OUTPUT.example.json','utf8')); const ajv=new Ajv({allErrors:true,strict:false}); const validate=ajv.compile(schema); for (const key of ['normal_case','missing_origin_or_destination_case']) { if (!validate(data[key])) { console.error(key, validate.errors); process.exit(1);} } console.log('schema-validate-ok');"` prints `schema-validate-ok`.
    - `rg -n "uri\.amap\.com|amapuri://|androidamap://|qr|二维码|request_payload|api_call" .planning/phases/03-routing-rules/03-ROUTE-OUTPUT.example.json` returns no match.
  </acceptance_criteria>
  <verify>
    <automated>node -e "JSON.parse(require('fs').readFileSync('.planning/phases/03-routing-rules/03-ROUTE-OUTPUT.example.json','utf8')); console.log('example-json-ok')" && rg -n "\"normal_case\"|\"missing_origin_or_destination_case\"|\"ordered_route\"|\"segment_summaries\"|\"assumptions\"|\"total_estimate_min\"|\"missing_endpoint_handled\"|\"A-origin-1\"|\"A-destination-1\"" .planning/phases/03-routing-rules/03-ROUTE-OUTPUT.example.json && ! rg -n "uri\.amap\.com|amapuri://|androidamap://|qr|二维码|request_payload|api_call" .planning/phases/03-routing-rules/03-ROUTE-OUTPUT.example.json</automated>
  </verify>
  <done>示例在正常与缺失场景均可稳定输出完整路线分段，并与 schema 一致。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| route_rules -> schema | 规则到 schema 映射遗漏会导致执行偏差 |
| schema -> example | 示例偏离 schema 会误导后续执行者 |
| phase3_output -> phase4_link_builder | 字段不一致会影响后续 URI 构造阶段 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-06 | T | schema constraints | mitigate | required + enum + conditional 规则锁定 ROUTE-01/02/03 字段。 |
| T-03-07 | R | fallback assumptions | mitigate | 缺失场景强制 assumption_id 规范和 missing flag，确保可审计。 |
| T-03-08 | I | output payload | mitigate | schema/example 禁止 URI、二维码、请求载荷字段越界进入本阶段。 |
| T-03-09 | D | partial route output | mitigate | ordered_route 和 segment_summaries 均设最小约束，避免空路由。 |
| T-03-10 | E | downstream misuse | accept | 本阶段仅生成文档/样例，不执行权限动作，执行风险后续复核。 |
</threat_model>

<verification>
1. schema 与 example 均可被 JSON.parse，且关键字段命中检查通过。
2. 示例覆盖正常场景与缺失起终点场景，并保持完整分段。
3. 输出保持 Phase 3 范围，不包含 URI/二维码/外部请求字段。
</verification>

<success_criteria>
1. ROUTE-01: 有序路线结构在 schema/example 中可验证。
2. ROUTE-02: 每段都有 mode + duration_estimate_min，并可汇总总耗时。
3. ROUTE-03: 缺失起终点时有 assumptions 与 non-blocking 输出。
</success_criteria>

<output>
After completion, create `.planning/phases/03-routing-rules/03-02-SUMMARY.md`
</output>
