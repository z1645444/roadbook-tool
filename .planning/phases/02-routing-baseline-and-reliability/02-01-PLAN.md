---
phase: 02-routing-baseline-and-reliability
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/map-provider/map-provider.port.ts
  - src/routing/segment-routing.service.ts
  - src/shared/validation/route-artifact.schema.ts
  - tests/routing/geocode-disambiguation.test.ts
  - tests/routing/bicycling-segments.test.ts
autonomous: true
requirements:
  - ROUT-01
  - ROUT-02
must_haves:
  truths:
    - "Ambiguous geocode outcomes are surfaced as a blocking clarification state before routing."
    - "Ordered points produce ordered bicycling route segments with deterministic totals."
    - "Routing artifacts are validated by schema before downstream persistence."
  artifacts:
    - path: "src/map-provider/map-provider.port.ts"
      provides: "Typed provider contracts for geocode and bicycling route operations."
      exports: ["MapProvider", "GeocodeCandidate", "BicyclingSegmentResult"]
    - path: "src/routing/segment-routing.service.ts"
      provides: "Deterministic segment aggregation and route metric totals."
      exports: ["buildOrderedSegments", "summarizeRouteSegments"]
    - path: "src/shared/validation/route-artifact.schema.ts"
      provides: "Runtime validation contract for generated route artifacts."
      exports: ["safeParseRouteArtifact"]
  key_links:
    - from: "src/routing/segment-routing.service.ts"
      to: "src/map-provider/map-provider.port.ts"
      via: "segment builder consumes provider response contract."
      pattern: "BicyclingSegmentResult"
    - from: "src/routing/segment-routing.service.ts"
      to: "src/shared/validation/route-artifact.schema.ts"
      via: "aggregated output is validated before return."
      pattern: "safeParseRouteArtifact"
---

<objective>
Define routing contracts and deterministic segment aggregation foundations for Phase 2.

Purpose: Create executable interfaces and regression tests that lock ROUT-01/ROUT-02 behavior
before AMap adapter integration.
Output: Provider port contracts, segment aggregation service, and requirement-tagged routing tests.
</objective>

<execution_context>
@/Users/franklin/.codex/get-shit-done/workflows/execute-plan.md
@/Users/franklin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-routing-baseline-and-reliability/02-RESEARCH.md
@.planning/phases/02-routing-baseline-and-reliability/02-VALIDATION.md
@.planning/phases/02-routing-baseline-and-reliability/02-PATTERNS.md
@.planning/phases/01-conversation-intake-and-constraint-model/01-03-SUMMARY.md

<interfaces>
From `src/constraints/constraint-draft.model.ts`:
```typescript
export interface CanonicalPoint {
  provider: 'amap';
  providerId: string;
  name: string;
  lng: number;
  lat: number;
  confidence: number;
}
```

From `src/conversation/clarification-policy.service.ts`:
```typescript
export const evaluateClarificationNeed = (
  slot: string,
  confidence: number,
  candidateCount: number
): ClarificationDecision;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define map-provider port and geocode ambiguity regression contract</name>
  <read_first>
    - src/conversation/clarification-policy.service.ts
    - src/conversation/constraint-extractor.interface.ts
    - src/constraints/constraint-draft.model.ts
    - tests/constraints/waypoint-normalization.test.ts
    - .planning/phases/02-routing-baseline-and-reliability/02-PATTERNS.md
  </read_first>
  <files>src/map-provider/map-provider.port.ts, tests/routing/geocode-disambiguation.test.ts</files>
  <behavior>
    - Test 1: Multiple geocode candidates for one point produce a blocking clarification outcome.
    - Test 2: Single high-confidence candidate is accepted and exposed as a resolved canonical point.
  </behavior>
  <action>Create `MapProvider` port with explicit methods `geocodePoint(input)` and
  `routeBicyclingSegment(input)` using concrete types for `providerId`, `name`, `lng`, `lat`,
  `confidence`, and `infocode`. In `tests/routing/geocode-disambiguation.test.ts`, add
  `describe('ROUT-01 ...')` cases that assert candidateCount-based blocking behavior with no
  auto-selection when candidates > 1. Follow `src/conversation/constraint-extractor.interface.ts`
  for interface-first contract shape and `tests/constraints/waypoint-normalization.test.ts` for
  requirement-tagged ambiguity assertions, per `02-PATTERNS.md`.</action>
  <acceptance_criteria>
    - `rg -n "export interface MapProvider|geocodePoint|routeBicyclingSegment" src/map-provider/map-provider.port.ts` returns all port contracts.
    - `rg -n "ROUT-01|blocking clarification|candidate" tests/routing/geocode-disambiguation.test.ts` returns requirement-tagged assertions.
    - `pnpm vitest run tests/routing/geocode-disambiguation.test.ts -t "ROUT-01"` passes.
  </acceptance_criteria>
  <verify>
    <automated>pnpm vitest run tests/routing/geocode-disambiguation.test.ts -t "ROUT-01"</automated>
  </verify>
  <done>Provider contract and ROUT-01 ambiguity behavior are concretely test-locked.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement deterministic ordered segment aggregation for ROUT-02</name>
  <read_first>
    - src/map-provider/map-provider.port.ts
    - src/recap/recap-projection.service.ts
    - tests/conversation/intake-confirmation.test.ts
    - .planning/phases/02-routing-baseline-and-reliability/02-VALIDATION.md
    - .planning/phases/02-routing-baseline-and-reliability/02-PATTERNS.md
  </read_first>
  <files>src/routing/segment-routing.service.ts, tests/routing/bicycling-segments.test.ts</files>
  <behavior>
    - Test 1: Ordered points produce segments in exact `P0->P1`, `P1->P2` order.
    - Test 2: Total distance and duration are deterministic sums of segment metrics.
  </behavior>
  <action>Implement `buildOrderedSegments(points, provider)` and
  `summarizeRouteSegments(segments)` so legs are generated in index order only (no reorder in Phase
  2), and totals are numeric sums from provider segment metrics. Add `describe('ROUT-02 ...')`
  tests with fixed fixtures verifying strict leg order and aggregate totals. Follow deterministic
  section-aggregation style from `src/recap/recap-projection.service.ts` and gate-style progression
  assertions from `tests/conversation/intake-confirmation.test.ts`, per `02-PATTERNS.md`.</action>
  <acceptance_criteria>
    - `rg -n "buildOrderedSegments|summarizeRouteSegments" src/routing/segment-routing.service.ts` returns deterministic aggregation exports.
    - `rg -n "ROUT-02|P0->P1|P1->P2|totalDistance|totalDuration" tests/routing/bicycling-segments.test.ts` confirms ordered-route assertions.
    - `pnpm vitest run tests/routing/bicycling-segments.test.ts -t "ROUT-02"` passes.
  </acceptance_criteria>
  <verify>
    <automated>pnpm vitest run tests/routing/bicycling-segments.test.ts -t "ROUT-02"</automated>
  </verify>
  <done>Segment construction is deterministic and bound to ordered-point input behavior.</done>
</task>

<task type="auto">
  <name>Task 3: Add route artifact schema boundary for generated segment outputs</name>
  <read_first>
    - src/shared/validation/constraint-draft.schema.ts
    - src/recap/recap-projection.service.ts
    - src/routing/segment-routing.service.ts
    - tests/routing/bicycling-segments.test.ts
    - .planning/phases/02-routing-baseline-and-reliability/02-PATTERNS.md
  </read_first>
  <files>src/shared/validation/route-artifact.schema.ts, src/routing/segment-routing.service.ts</files>
  <action>Create `route-artifact.schema.ts` with zod objects for route points, segments, and route
  totals, then export `safeParseRouteArtifact(payload)` and call it in
  `segment-routing.service.ts` before returning aggregated artifacts. Reject invalid segment shapes
  with explicit errors instead of returning partially-formed objects. Mirror
  `src/shared/validation/constraint-draft.schema.ts` for zod + `safeParse` boundary structure and
  keep aggregation handoff deterministic like `src/recap/recap-projection.service.ts`, per
  `02-PATTERNS.md`.</action>
  <acceptance_criteria>
    - `rg -n "safeParseRouteArtifact|z\\.object|segments|totalDistanceMeters|totalDurationSeconds" src/shared/validation/route-artifact.schema.ts` confirms schema fields.
    - `rg -n "safeParseRouteArtifact" src/routing/segment-routing.service.ts` confirms validation boundary use.
    - `pnpm vitest run tests/routing/geocode-disambiguation.test.ts tests/routing/bicycling-segments.test.ts` passes.
  </acceptance_criteria>
  <verify>
    <automated>pnpm vitest run tests/routing/geocode-disambiguation.test.ts tests/routing/bicycling-segments.test.ts</automated>
  </verify>
  <done>Generated route artifacts have a strict runtime schema boundary before downstream usage.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| canonical points -> provider port | Internal normalized points are converted into outbound provider requests. |
| provider response -> route artifact schema | Untrusted external payload crosses into canonical route artifacts. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | T | `src/map-provider/map-provider.port.ts` | mitigate | Keep strict typed contracts for point and segment payloads; reject missing coordinate fields at compile/runtime boundaries. |
| T-02-02 | D | `src/routing/segment-routing.service.ts` | mitigate | Fail fast on invalid/empty segment lists and cap loop behavior to ordered point count minus one. |
| T-02-03 | R | `tests/routing/geocode-disambiguation.test.ts` | mitigate | Lock ambiguity gate behavior with requirement-tagged test assertions to prevent silent auto-selection regressions. |
| T-02-04 | I | `src/shared/validation/route-artifact.schema.ts` | mitigate | Validate outbound route artifacts to prevent leaking malformed provider payload details downstream. |
</threat_model>

<verification>
- `pnpm vitest run tests/routing/geocode-disambiguation.test.ts`
- `pnpm vitest run tests/routing/bicycling-segments.test.ts`
</verification>

<success_criteria>
- ROUT-01 and ROUT-02 contracts are encoded in provider ports and passing routing tests.
- Segment generation is deterministic for ordered points and schema-validated before return.
- No ambiguity auto-accept path exists for multi-candidate geocode results.
</success_criteria>

<output>
After completion, create `.planning/phases/02-routing-baseline-and-reliability/02-01-SUMMARY.md`
</output>
