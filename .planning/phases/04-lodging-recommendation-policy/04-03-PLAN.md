---
phase: 04-lodging-recommendation-policy
plan: 03
type: execute
wave: 3
depends_on:
  - 04-01
  - 04-02
files_modified:
  - src/routing/routing-orchestrator.service.ts
  - src/conversation/intake.controller.ts
  - tests/routing/lodging-integration.test.ts
  - tests/conversation/intake-confirmation.test.ts
autonomous: true
requirements:
  - LODG-01
  - LODG-05
must_haves:
  truths:
    - "Lodging recommendations are attached per day only when overnight stops exist."
    - "Single-day flows do not trigger lodging retrieval and remain behaviorally stable."
    - "Chat response contains day-grouped category comparisons with fallback trace visibility."
  artifacts:
    - path: "src/routing/routing-orchestrator.service.ts"
      provides: "Integrated day-level lodging attachment on multiday path."
      exports: ["RoutingOrchestratorService"]
    - path: "src/conversation/intake.controller.ts"
      provides: "Chat response contract carrying day lodging recommendations."
      exports: ["IntakeController"]
    - path: "tests/routing/lodging-integration.test.ts"
      provides: "End-to-end coverage for lodging injection and no-match fallback output."
      contains: "LODG-01, LODG-05"
  key_links:
    - from: "src/routing/routing-orchestrator.service.ts"
      to: "src/routing/lodging-policy.service.ts"
      via: "multiday day-stage output enriched with per-day lodging payload."
      pattern: "buildLodgingRecommendations"
    - from: "src/routing/routing-orchestrator.service.ts"
      to: "src/conversation/intake.controller.ts"
      via: "controller relays enriched routePlan day payload to user-facing response."
      pattern: "routePlan"
    - from: "tests/routing/lodging-integration.test.ts"
      to: "src/routing/routing-orchestrator.service.ts"
      via: "integration assertions verify overnight-stop guard and fallback trace propagation."
      pattern: "overnightStopPoint"
---

<objective>
Wire lodging policy into the routing/chat flow so Phase 4 behavior is available end-to-end without
regressing existing routing fallback and one-day guards.

Purpose: Deliver user-visible LODG-01 and LODG-05 outcomes in orchestration output.
Output: Orchestrator integration, controller response extension, and phase-level integration tests.
</objective>

<execution_context>
@/Users/franklin/.codex/get-shit-done/workflows/execute-plan.md
@/Users/franklin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-lodging-recommendation-policy/04-CONTEXT.md
@.planning/phases/04-lodging-recommendation-policy/04-RESEARCH.md
@.planning/phases/04-lodging-recommendation-policy/04-PATTERNS.md
@.planning/phases/04-lodging-recommendation-policy/04-01-PLAN.md
@.planning/phases/04-lodging-recommendation-policy/04-02-PLAN.md
@.planning/phases/03-multi-day-optimization-and-stage-split/03-03-SUMMARY.md

<interfaces>
From src/routing/routing-orchestrator.service.ts:
```typescript
export interface RoutingOrchestratorResult {
  routingStatus: 'ready' | 'needs_clarification' | 'fallback';
  routePlan: RouteDayPlan[] | null;
}
```

From src/conversation/intake.controller.ts:
```typescript
export interface IntakeTurnResponse {
  routePlan: Array<{ dayIndex: number; overnightStopPoint: ...; segments: ... }> | null;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing integration tests for day-level lodging injection and no-match trace</name>
  <read_first>
    - tests/routing/phase3-multiday-integration.test.ts
    - tests/routing/single-day-guard.test.ts
    - tests/conversation/intake-confirmation.test.ts
    - src/routing/routing-orchestrator.service.ts
    - .planning/phases/04-lodging-recommendation-policy/04-CONTEXT.md
  </read_first>
  <files>tests/routing/lodging-integration.test.ts, tests/conversation/intake-confirmation.test.ts</files>
  <behavior>
    - Tests fail before orchestrator/controller lodging wiring exists.
    - Tests cover both multiday lodging attachment and single-day retrieval skip.
  </behavior>
  <action>Create `tests/routing/lodging-integration.test.ts` using existing harness pattern.
Add cases:
`LODG-01` multiday route returns `routePlan[n].lodging` for days with `overnightStopPoint`;
single-day route (`tripDays=1`) returns `routePlan[0].lodging` as `null` and never calls
`searchLodgingAround`;
`LODG-05` no-match path includes `policyStatus='no_match'` and non-empty `fallbackTrace`.
In `tests/conversation/intake-confirmation.test.ts`, add response assertions that
`routePlan[].lodging.categories.hostel|guesthouse|hotel` and `fallbackTrace` are present when
routing is ready on multiday input.</action>
  <acceptance_criteria>
    - `rg -n "LODG-01|LODG-05|fallbackTrace|no_match|searchLodgingAround" tests/routing/lodging-integration.test.ts` returns matches.
    - `rg -n "routePlan.*lodging|fallbackTrace|categories" tests/conversation/intake-confirmation.test.ts` returns new assertions.
    - `pnpm vitest run tests/routing/lodging-integration.test.ts` fails before Task 2 and passes after Task 2.
  </acceptance_criteria>
  <verify>
    <automated>pnpm vitest run tests/routing/lodging-integration.test.ts tests/conversation/intake-confirmation.test.ts</automated>
  </verify>
  <done>Integration tests lock expected orchestration and API response behavior for lodging recommendations.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Integrate lodging policy into orchestrator and expose in controller response</name>
  <read_first>
    - src/routing/routing-orchestrator.service.ts
    - src/conversation/intake.controller.ts
    - src/routing/lodging-policy.service.ts
    - src/routing/day-stage-splitter.service.ts
    - src/map-provider/map-provider.port.ts
    - tests/routing/lodging-integration.test.ts
  </read_first>
  <files>src/routing/routing-orchestrator.service.ts, src/conversation/intake.controller.ts, tests/routing/lodging-integration.test.ts, tests/conversation/intake-confirmation.test.ts</files>
  <behavior>
    - Lodging retrieval runs only for days with overnight anchors and only on multiday path.
    - Existing fallback and clarification behavior remains unchanged for non-lodging failures.
  </behavior>
  <action>Inject `LodgingPolicyService` into `RoutingOrchestratorService` constructor and call it
only when `tripDays > 1` and day has `overnightStopPoint` per D-01/D-02.
Pass `sessionId`, `dayIndex`, and day anchor into policy service.
Attach lodging payload to each route day:
`lodging: { policyStatus, fallbackTrace, categories }` or `null` when skipped.
Keep routing fallback path unchanged; if lodging provider throws `RoutingFallbackError`,
set day lodging to `{ policyStatus: 'no_match', fallbackTrace: ['provider_fallback'], categories: empty }`
without failing whole route response.
Extend `IntakeTurnResponse.routePlan[]` type in controller to include optional `lodging` field with
exact nested category arrays and entry fields from D-24.
Ensure payload remains Chinese-safe and does not expose provider secrets/raw payloads.</action>
  <acceptance_criteria>
    - `rg -n "LodgingPolicyService|tripDays > 1|overnightStopPoint|lodging" src/routing/routing-orchestrator.service.ts` returns matches.
    - `rg -n "routePlan.*lodging|policyStatus|fallbackTrace|categories" src/conversation/intake.controller.ts` returns response type mapping.
    - `pnpm vitest run tests/routing/lodging-integration.test.ts tests/conversation/intake-confirmation.test.ts` passes.
    - `pnpm vitest run tests/routing/single-day-guard.test.ts -t "ROUT-05"` still passes.
  </acceptance_criteria>
  <verify>
    <automated>pnpm vitest run tests/routing/lodging-integration.test.ts tests/conversation/intake-confirmation.test.ts tests/routing/single-day-guard.test.ts</automated>
  </verify>
  <done>Routing and chat response surfaces day-level lodging recommendations with stable one-day and fallback behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Policy service output -> orchestrator routePlan | Internal recommendation payload becomes user-facing content. |
| Orchestrator result -> intake controller response | Route payload (including lodging metadata) crosses API boundary to client. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-09 | I | `src/routing/routing-orchestrator.service.ts` | mitigate | Restrict lodging retrieval to `tripDays > 1` days with non-null `overnightStopPoint`; single-day stays lodging-null. |
| T-04-10 | D | `src/routing/routing-orchestrator.service.ts` | mitigate | Use cached policy service output and per-day safe fallback to avoid full-route failure on lodging lookup issues. |
| T-04-11 | I | `src/conversation/intake.controller.ts` | mitigate | Return only defined lodging DTO fields; exclude provider raw payloads and credentials. |
| T-04-12 | R | `tests/routing/lodging-integration.test.ts` | mitigate | Requirement-labeled integration tests assert no-match trace and overnight-stop guard behavior. |
</threat_model>

<verification>
- `pnpm vitest run tests/routing/lodging-integration.test.ts`
- `pnpm vitest run tests/conversation/intake-confirmation.test.ts`
- `pnpm vitest run tests/routing/single-day-guard.test.ts --reporter=dot`
</verification>

<success_criteria>
- Multiday route output includes per-day lodging recommendation payload grouped by category.
- Single-day flow skips lodging retrieval and remains stable.
- No-match and fallback trace are visible in the routePlan payload for downstream Markdown rendering.
</success_criteria>

<output>
After completion, create `.planning/phases/04-lodging-recommendation-policy/04-03-SUMMARY.md`
</output>
