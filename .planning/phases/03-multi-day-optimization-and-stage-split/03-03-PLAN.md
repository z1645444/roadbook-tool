---
phase: 03-multi-day-optimization-and-stage-split
plan: 03
type: execute
wave: 3
depends_on:
  - 03-01
  - 03-02
files_modified:
  - src/routing/routing-orchestrator.service.ts
  - src/conversation/intake.controller.ts
  - tests/routing/phase3-multiday-integration.test.ts
  - tests/conversation/intake-confirmation.test.ts
  - tests/routing/single-day-guard.test.ts
autonomous: true
requirements:
  - ROUT-03
  - ROUT-04
must_haves:
  truths:
    - "Routing orchestrator applies waypoint optimization and day stage split when tripDays > 1."
    - "One-day behavior remains stable and does not regress."
    - "Chat response exposes explicit day boundaries and overnight stop points."
  artifacts:
    - path: "src/routing/routing-orchestrator.service.ts"
      provides: "Integrated multiday orchestration with optimizer and stage splitter."
      exports: ["RoutingOrchestratorService"]
    - path: "tests/routing/phase3-multiday-integration.test.ts"
      provides: "Phase-level integration coverage for ROUT-03 and ROUT-04."
      contains: "ROUT-03, ROUT-04"
    - path: "src/conversation/intake.controller.ts"
      provides: "User-visible route payload carrying day boundary fields."
      exports: ["IntakeController"]
  key_links:
    - from: "src/routing/routing-orchestrator.service.ts"
      to: "src/routing/waypoint-optimizer.service.ts"
      via: "orchestrator optimizes intermediate waypoint order before segment routing."
      pattern: "optimizeWaypointSequence"
    - from: "src/routing/routing-orchestrator.service.ts"
      to: "src/routing/day-stage-splitter.service.ts"
      via: "orchestrator partitions optimized route into day stages."
      pattern: "splitRouteIntoDayStages"
    - from: "src/conversation/intake.controller.ts"
      to: "src/routing/routing-orchestrator.service.ts"
      via: "controller returns routePlan with day boundaries from orchestrator output."
      pattern: "routePlan"
---

<objective>
Integrate Phase 3 logic into the chat flow so users receive optimized waypoint order and feasible
multi-day stage boundaries while preserving stable one-day behavior.
</objective>

<execution_context>
@/Users/franklin/.codex/get-shit-done/workflows/execute-plan.md
@/Users/franklin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-multi-day-optimization-and-stage-split/03-RESEARCH.md
@.planning/phases/03-multi-day-optimization-and-stage-split/03-VALIDATION.md
@.planning/phases/03-multi-day-optimization-and-stage-split/03-PATTERNS.md
@.planning/phases/03-multi-day-optimization-and-stage-split/03-01-PLAN.md
@.planning/phases/03-multi-day-optimization-and-stage-split/03-02-PLAN.md
@.planning/phases/02-routing-baseline-and-reliability/02-04-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Integrate optimizer + day splitter into routing orchestrator multi-day path</name>
  <read_first>
    - src/routing/routing-orchestrator.service.ts
    - src/routing/segment-routing.service.ts
    - src/routing/waypoint-optimizer.service.ts
    - src/routing/day-stage-splitter.service.ts
    - src/constraints/constraint-draft.model.ts
  </read_first>
  <files>src/routing/routing-orchestrator.service.ts, tests/routing/phase3-multiday-integration.test.ts, tests/routing/single-day-guard.test.ts</files>
  <behavior>
    - For `tripDays.normalized > 1`: apply optimizer then split route into day stages.
    - For `tripDays.normalized === 1`: preserve previous one-day route path.
  </behavior>
  <action>Update orchestrator flow to branch on `draft.slots.tripDays?.normalized`:
when greater than 1, call `optimizeWaypointSequence` on accepted points, build segments on optimized
order, then call `splitRouteIntoDayStages` with ride-window and intensity constraints.
Map split output to `routePlan[]` with per-day boundaries.
Retain existing fallback and clarification behaviors.
Keep one-day branch returning exactly one day artifact for ROUT-05 compatibility.</action>
  <acceptance_criteria>
    - `rg -n "tripDays|optimizeWaypointSequence|splitRouteIntoDayStages" src/routing/routing-orchestrator.service.ts` confirms integrated multiday branch.
    - `rg -n "ROUT-03|ROUT-04|multiday|day boundary" tests/routing/phase3-multiday-integration.test.ts` confirms requirement-tagged integration tests.
    - `pnpm vitest run tests/routing/phase3-multiday-integration.test.ts` passes.
    - `pnpm vitest run tests/routing/single-day-guard.test.ts -t "ROUT-05"` passes.
  </acceptance_criteria>
  <verify>
    <automated>pnpm vitest run tests/routing/phase3-multiday-integration.test.ts tests/routing/single-day-guard.test.ts</automated>
  </verify>
  <done>Orchestrator supports multiday optimization/splitting without regressing one-day behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expose day boundary fields through intake controller response contracts</name>
  <read_first>
    - src/conversation/intake.controller.ts
    - src/routing/routing-orchestrator.service.ts
    - tests/conversation/intake-confirmation.test.ts
    - tests/routing/phase2-routing-integration.test.ts
  </read_first>
  <files>src/conversation/intake.controller.ts, tests/conversation/intake-confirmation.test.ts, tests/routing/phase3-multiday-integration.test.ts</files>
  <behavior>
    - `routePlan[]` items include day boundary fields in controller response.
    - Confirmation gate behavior remains unchanged before route generation.
  </behavior>
  <action>Extend `IntakeTurnResponse.routePlan` day items to include
`startPoint`, `endPoint`, and `overnightStopPoint` while preserving existing segment/totals fields.
Ensure `processTurn` maps these fields from orchestrator multiday output.
Keep pre-confirmation responses identical to existing behavior.
Add/adjust tests asserting boundary fields are present for multiday responses.</action>
  <acceptance_criteria>
    - `rg -n "startPoint|endPoint|overnightStopPoint" src/conversation/intake.controller.ts` confirms response contract update.
    - `rg -n "startPoint|endPoint|overnightStopPoint" tests/routing/phase3-multiday-integration.test.ts tests/conversation/intake-confirmation.test.ts` confirms assertions.
    - `pnpm vitest run tests/conversation/intake-confirmation.test.ts` passes.
  </acceptance_criteria>
  <verify>
    <automated>pnpm vitest run tests/conversation/intake-confirmation.test.ts</automated>
  </verify>
  <done>Chat-facing payload surfaces explicit day boundaries required by Phase 3 success criteria.</done>
</task>

<task type="auto">
  <name>Task 3: Add full Phase 3 requirement regression suite and stable command surface</name>
  <read_first>
    - tests/routing/phase3-multiday-integration.test.ts
    - tests/routing/waypoint-optimization.test.ts
    - tests/routing/day-stage-split.test.ts
    - .planning/phases/03-multi-day-optimization-and-stage-split/03-VALIDATION.md
  </read_first>
  <files>tests/routing/phase3-multiday-integration.test.ts, tests/routing/waypoint-optimization.test.ts, tests/routing/day-stage-split.test.ts</files>
  <action>Ensure each Phase 3 test suite has stable requirement-labeled `describe`/`it` names for
`ROUT-03` and `ROUT-04`, deterministic fixtures (no time/network randomness), and command
compatibility with validation strategy.
Keep fast smoke targeting optimization+split logic and full routing sweep for wave-end checks.</action>
  <acceptance_criteria>
    - `rg -n "ROUT-03|ROUT-04" tests/routing/waypoint-optimization.test.ts tests/routing/day-stage-split.test.ts tests/routing/phase3-multiday-integration.test.ts` returns both requirement IDs.
    - `pnpm vitest run tests/routing/waypoint-optimization.test.ts tests/routing/day-stage-split.test.ts --reporter=dot` passes in task-level smoke scope (<30s target).
    - Wave-end verification command remains `pnpm vitest run tests/routing --reporter=dot`.
  </acceptance_criteria>
  <verify>
    <automated>pnpm vitest run tests/routing/waypoint-optimization.test.ts tests/routing/day-stage-split.test.ts --reporter=dot</automated>
  </verify>
  <done>Phase 3 has stable regression command surface with full requirement traceability.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| orchestrator multiday logic -> controller response | Internal stage artifacts become user-visible route output. |
| phase3 tests -> release confidence | Requirement coverage evidence gates phase verification decisions. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-09 | T | `src/routing/routing-orchestrator.service.ts` | mitigate | Preserve one-day path guard and isolate multiday branch by trip-day condition. |
| T-3-10 | I | `src/conversation/intake.controller.ts` | mitigate | Expose only required boundary fields; avoid leaking raw provider internals. |
| T-3-11 | R | `tests/routing/phase3-multiday-integration.test.ts` | mitigate | Requirement-tagged integration assertions for ROUT-03/ROUT-04. |
| T-3-12 | D | `tests/routing/phase3-multiday-integration.test.ts` | mitigate | Use deterministic doubles and bounded fixtures to prevent flaky regressions. |
</threat_model>

<verification>
- `pnpm vitest run tests/routing/phase3-multiday-integration.test.ts`
- `pnpm vitest run tests/conversation/intake-confirmation.test.ts`
- `pnpm vitest run tests/routing --reporter=dot`
</verification>

<success_criteria>
- ROUT-03 and ROUT-04 behaviors are integrated end-to-end in the chat flow.
- Day boundaries and overnight stops are explicit in route output.
- One-day behavior remains stable under regression tests.
</success_criteria>

<output>
After completion, create `.planning/phases/03-multi-day-optimization-and-stage-split/03-03-SUMMARY.md`
</output>
