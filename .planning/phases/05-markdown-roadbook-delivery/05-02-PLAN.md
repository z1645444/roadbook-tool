---
phase: 05-markdown-roadbook-delivery
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - src/conversation/intake.controller.ts
  - src/roadbook/markdown-roadbook.renderer.ts
  - src/roadbook/roadbook-render.types.ts
  - tests/conversation/intake-confirmation.test.ts
autonomous: true
requirements:
  - BOOK-01
  - BOOK-03
must_haves:
  truths:
    - "Routing-ready responses include a markdown roadbook field built from canonical route output."
    - "Overnight days include lodging shortlist details (score/price) in the rendered markdown."
    - "Single-day routing responses avoid synthetic lodging sections."
  artifacts:
    - path: "src/conversation/intake.controller.ts"
      provides: "Additive `roadbookMarkdown` response contract wired to routing-ready path."
      exports: ["IntakeController", "IntakeTurnResponse"]
    - path: "src/roadbook/markdown-roadbook.renderer.ts"
      provides: "Lodging section formatting integrated with day rendering."
      exports: ["renderMarkdownRoadbook"]
    - path: "tests/conversation/intake-confirmation.test.ts"
      provides: "Integration assertions that controller returns markdown and lodging details on multiday responses."
      contains: "BOOK-01, BOOK-03"
  key_links:
    - from: "src/conversation/intake.controller.ts"
      to: "src/routing/routing-orchestrator.service.ts"
      via: "Routing-ready branch maps routePlan + recap + metadata into roadbook renderer input."
      pattern: "routingStatus === 'ready'"
    - from: "src/conversation/intake.controller.ts"
      to: "src/roadbook/markdown-roadbook.renderer.ts"
      via: "Controller composes response field `roadbookMarkdown` from renderer."
      pattern: "roadbookMarkdown"
    - from: "tests/conversation/intake-confirmation.test.ts"
      to: "src/conversation/intake.controller.ts"
      via: "Integration tests assert response includes markdown with lodging shortlist details."
      pattern: "BOOK-03"
---

<objective>
Integrate markdown roadbook rendering into the intake response contract for routing-ready flows and
ensure lodging-rich multiday output is reflected in the markdown.

Purpose: Deliver API-level BOOK-01 and BOOK-03 behavior from canonical route artifacts.
Output: Controller integration, additive response contract update, and integration regressions.
</objective>

<execution_context>
@/Users/franklin/.codex/get-shit-done/workflows/execute-plan.md
@/Users/franklin/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-markdown-roadbook-delivery/05-RESEARCH.md
@.planning/phases/05-markdown-roadbook-delivery/05-VALIDATION.md
@.planning/phases/04-lodging-recommendation-policy/04-03-SUMMARY.md
@src/conversation/intake.controller.ts
@src/routing/routing-orchestrator.service.ts
@src/recap/recap-projection.service.ts
@tests/conversation/intake-confirmation.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing controller integration tests for roadbookMarkdown and lodging sections</name>
  <read_first>
    - tests/conversation/intake-confirmation.test.ts
    - src/conversation/intake.controller.ts
    - src/routing/routing-orchestrator.service.ts
    - .planning/phases/04-lodging-recommendation-policy/04-03-SUMMARY.md
  </read_first>
  <files>tests/conversation/intake-confirmation.test.ts</files>
  <action>Add BOOK-01 and BOOK-03 labeled integration assertions in routing-ready tests:
`roadbookMarkdown` is non-null on `status='routing_ready'` and contains day heading plus route
overview lines; multiday response with lodging payload includes lodging shortlist entries and each
entry includes rating/price text; single-day response does not include lodging shortlist section.
Keep current `routePlan` assertions unchanged to preserve additive-contract stability.</action>
  <acceptance_criteria>
    - Existing routing-ready tests now assert `roadbookMarkdown` content.
    - Tests cover both multiday lodging-present and single-day lodging-absent markdown behavior.
    - `pnpm vitest run tests/conversation/intake-confirmation.test.ts -t "BOOK-01|BOOK-03"` fails before Task 2 and passes after Task 2.
  </acceptance_criteria>
  <verify>
    <automated>pnpm vitest run tests/conversation/intake-confirmation.test.ts -t "BOOK-01|BOOK-03"</automated>
  </verify>
  <done>Controller integration tests codify routing-ready markdown presence and lodging detail behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire roadbook renderer into routing-ready controller response with lodging details</name>
  <read_first>
    - src/conversation/intake.controller.ts
    - src/roadbook/roadbook-render.types.ts
    - src/roadbook/markdown-roadbook.renderer.ts
    - tests/conversation/intake-confirmation.test.ts
    - src/recap/recap-projection.service.ts
  </read_first>
  <files>src/conversation/intake.controller.ts, src/roadbook/markdown-roadbook.renderer.ts, src/roadbook/roadbook-render.types.ts, tests/conversation/intake-confirmation.test.ts</files>
  <action>Extend `IntakeTurnResponse` with additive field `roadbookMarkdown: string | null`.
In fallback/clarification/idle paths return `roadbookMarkdown: null`.
In routing-ready branch, call renderer with recap summary/assumptions, `routePlan`, and metadata
from orchestrator; assign the result to `roadbookMarkdown`.
Update renderer contracts/helpers as needed to format lodging section per overnight day using
existing `lodging.categories` entries (`name`, `rating`, `priceCny`, `policyStage`) from Phase 4
output. Keep structured `routePlan` response unchanged and do not expose raw provider payloads.</action>
  <acceptance_criteria>
    - `IntakeTurnResponse` includes `roadbookMarkdown`.
    - Controller returns markdown only when `routingStatus` is `ready`.
    - Multiday lodging entries appear in markdown with score/price details from route day lodging payload.
    - `pnpm vitest run tests/conversation/intake-confirmation.test.ts -t "BOOK-01|BOOK-03"` passes.
  </acceptance_criteria>
  <verify>
    <automated>pnpm vitest run tests/conversation/intake-confirmation.test.ts -t "BOOK-01|BOOK-03"</automated>
  </verify>
  <done>`IntakeController` returns additive `roadbookMarkdown` on ready responses with BOOK-03 lodging details and no regressions in structured output.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Routing orchestrator output -> intake response | Structured route/lodging data becomes user-facing markdown output. |
| Intake endpoint response -> chat client | New markdown field crosses API boundary and can leak internals if unconstrained. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04 | I | `src/conversation/intake.controller.ts` | mitigate | Return only curated markdown text + existing structured fields; never include raw provider payload or secrets. |
| T-05-05 | T | `src/roadbook/markdown-roadbook.renderer.ts` | mitigate | Render lodging sections from validated day-lodging DTO fields only and escape text content. |
| T-05-06 | R | `tests/conversation/intake-confirmation.test.ts` | mitigate | Integration tests prove markdown appears only in routing-ready path and contains BOOK-03 details. |
</threat_model>

<verification>
- `pnpm vitest run tests/conversation/intake-confirmation.test.ts -t "BOOK-01|BOOK-03"`
- `pnpm vitest run tests/routing/lodging-integration.test.ts`
</verification>

<success_criteria>
- `routing_ready` responses include deterministic `roadbookMarkdown` without regressing existing `routePlan`.
- Multiday markdown includes lodging shortlist entries with rating and price details.
- Non-ready/fallback responses keep `roadbookMarkdown` null.
</success_criteria>

<output>
After completion, create `.planning/phases/05-markdown-roadbook-delivery/05-02-SUMMARY.md`
</output>
